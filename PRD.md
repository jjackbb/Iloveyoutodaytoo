[Product Requirement Document]

---

## 패치 기록 (구현 변경 이력)

**이 표는 커밋 하나에 한 줄씩 쌓인다.** 실제로 앱에 반영된 변경만 적는다 —
팀원이 이 문서만 보고도 "지금 앱이 PRD와 어디가 다른지"를 알 수 있게 하기 위해서다.
아래 `문서 수정 이력`은 **기획 문구가 바뀐 기록**이고, 이 표는 **구현이 바뀐 기록**이다.

커밋 번호(해시) 대신 **커밋 제목**을 적는다 — 해시는 커밋을 고쳐 쓰면 바뀌어서
표만 옛 번호로 남는다. 제목으로는 `git log --grep "제목 일부"`로 바로 찾을 수 있다.

| 날짜 | 커밋 제목 | 무엇이 바뀌었나 | 왜 |
| :--- | :--- | :--- | :--- |
| 2026-08-19 | "실기기 필요"로 미뤄뒀던 3건 전부 브라우저에서 확인 | "실기기 필요"로 미뤄뒀던 3건(커버 크롭 핀치, 초대 연락처 대체 경로, 녹음 60초 자동 정지)을 브라우저 자동화로 전부 확인 | 확인 못 한 채 남겨두면 실제로 되는지 아무도 모른다 |
| 2026-08-19 | PRD에서 관계 유형 폐기 — 캡처 기준·구현과 일치시킴 | PRD에서 관계 유형(가족/연인/친구/나 자신) 폐기 | 캡처 기준·구현은 이미 안 묻고 있었다. 문서만 남아 어긋나 있었다 |
| 2026-08-20 | 앨범방 이름 변경 (노션 IA 6.7) | 앨범방 이름 변경 기능 추가 (방장 전용, `rooms.name` 수정) | 노션 IA 6.7. **아래 항목에서 방식이 바뀌었다** |
| 2026-08-20 | 댓글 수정 (노션 IA 3.9) | 댓글 수정 — 텍스트 댓글만, 작성자 본인만, "수정됨" 표시와 함께 | 가족이 주고받은 말이 아무 흔적 없이 바뀌면 안 된다. 음성은 고치는 것이 곧 갈아끼우는 것이라 막았다 |
| 2026-08-20 | 사서함 정리 모드 (노션 IA 2.2) | 사서함 정리 모드 — 고른 마음을 **내 화면에서만** 치운다(실제 삭제 아님) | 진짜로 지우면 상대의 보낸함에서도 사라진다. 그래서 문구도 "삭제"가 아니라 "치우기" |
| 2026-08-20 | 앨범방 이름·커버를 각자 자기 화면에서만 바꾸게 (노션 IA 6.7 개정) | **앨범방 이름·커버를 각자 자기 화면에서만 바꾸는 방식으로 전환.** 방장 전용 이름 변경(`632540f`)을 걷어내고, `room_members.custom_name / custom_cover_preset / custom_cover_path`에 사람별로 저장한다. 방 설정의 [앨범방 이름] 칸이 [내 화면에서 부를 이름]으로 바뀌었고 모든 구성원에게 보인다 | 카카오톡 단체방과 같은 방식(사용자 결정). 방장이 이름을 바꾸면 모두의 화면이 바뀌는데, 시니어 사용자에게는 "어제 있던 방이 없어진" 것으로 읽힌다 |
| 2026-08-20 | 앨범방 이름·커버를 각자 자기 화면에서만 바꾸게 (노션 IA 6.7 개정) | 보안: 구성원이 스스로 `room_members.role`을 admin으로 바꾸는 길을 트리거로 차단 | RLS 정책은 줄 단위라 컬럼을 가릴 수 없다. 내 별명을 고치는 통로로 방장 권한까지 가져갈 수 있었다 |

1. Document Info
Service Name: 오늘도 사랑해
Author: 기획 담당자
문서 수정 이력|수정 날짜|수정한 사람|수정한 내용 요약|| :--- | :--- | :--- ||2026-07-10|기획 담당자|최초 작성 (MVP 기능 범위 확정)||2026-08-19|기획 담당자|[MSG-01] 음성 메시지 최대 길이 30초 → 60초로 확정 (구현·DB가 60초로 이미 동작 중이었고, 그쪽에 맞춤)|||2026-08-19|기획 담당자|[ALBUM-01] 관계 유형(가족/연인/친구/반려동물/나 자신) 선택을 폐기. 방을 만들 때 관계를 규정짓게 하지 않고 개방적으로 간다 (캡처 기준 개정과 동일한 방향, 구현도 이미 그렇게 되어 있음)|
2. Executive Summary (서비스 개요)
One-line Definition: 쑥스러운 마음을 추억으로 전하는 곳, 우리 가족 비밀 감정 사서함
Core Problem: 독립, 취업 등으로 인해 자녀와 부모 간의 물리적·심리적 거리가 멀어지며, 평소 서로에게 안부나 진심 어린 애정을 표현할 계기(핑계)가 부족함. 카카오톡 등의 메신저 대화는 휘발되기 쉽고 깊이 있는 소통을 이끌어내기 어려움.
Value Proposition: 사진과 짧은 영상 아카이빙을 매개체로 삼아 자연스러운 대화의 물꼬를 트고, '답장 미션'이라는 따뜻한 강제 장치를 통해 표현이 서툰 가족 간의 지속적이고 깊이 있는 교류를 유도함.
3. Target User & Persona (핵심 사용자)
Primary Persona:
자녀 (2030 직장인): 따로 살며 바쁘다는 핑계로 부모님께 자주 연락해 다정한 말을 건네지 못하는 것에 부채감을 느끼는 유저.
부모 (5060 시니어): 자녀의 일상이 궁금하지만 방해가 될까 봐 먼저 연락하기 조심스럽고, 디지털 기기나 긴 텍스트 입력이 다소 서툰 유저.
Core Needs & Pain Points:
"부모님/자녀에게 사랑한다는 말을 하고 싶지만 쑥스럽고 어색하다."
"가족들과 일상을 공유하고 싶지만 인스타그램 같은 SNS는 너무 개방적이라 부담스럽다."
User Scenario & Goal: 자녀가 앨범방을 개설해 부모님을 초대하고, 일상 사진을 올리면 부모님이 이를 매개로 음성이나 텍스트로 마음을 표현함. 발신자의 메시지가 쌓이면 답장을 해야만 다음 메시지가 열리는 구조를 통해, 가족 구성원 모두가 이탈 없이 지속적으로 따뜻한 안부를 주고받는 것을 목표로 함.
4. MVP Feature List (MVP 기능 목록)

5. Functional Specifications & Policy (상세 동작 및 정책)
[ALBUM-01] 앨범방 생성• 상세 동작 (Overview):1. 사용자는 홈 화면에서 '앨범방 만들기' 버튼을 선택한다.2. 앨범방 이름을 입력한다.3. 대표 이미지 또는 기본 테마를 선택한다.4. 생성 완료 시 홈 화면에 앨범방 카드가 추가되며 상세 화면으로 이동한다.• 입력 (Input):◦ 필수: 앨범방 이름◦ 선택: 대표 이미지, 앨범 설명, 기본 테마, 공개 범위 설정• 출력 (Output):◦ 앨범방 생성 완료 화면 및 홈 화면 내 신규 앨범방 카드 생성◦ 해당 방 전용 '기본 감정 사서함' 자동 생성• 예외 처리 (Exception):◦ [예외 1] 앨범방 이름 미입력 시 $\rightarrow$ "앨범방 이름을 입력해주세요." 얼럿 노출.◦ [예외 2] 무료 사용자가 생성 가능 개수(기본 1개)를 초과한 경우 $\rightarrow$ "추가 앨범방은 프리미엄에서 이용할 수 있어요." 팝업 노출.• 기능 정책 (Policy):◦ 로그인한 회원만 생성 가능하며, 생성자는 기본 관리자 권한을 가짐.◦ 앨범방 이름은 2~20자 입력을 권장함.[ALBUM-02] 사진 및 짧은 영상 업로드• 상세 동작 (Overview):1. 사용자는 활성화된 앨범방에 입장하여 '추억 올리기' 버튼을 누른다.2. 디바이스 내 사진 또는 짧은 영상 파일을 선택한다.3. 날짜, 설명, 함께한 사람 등 정보를 입력한 후 업로드한다.4. 업로드 완료 시 피드에 콘텐츠가 등록되며 구성원에게 알림이 발송된다.• 입력 (Input):◦ 필수: 사진 또는 영상 파일◦ 선택: 날짜, 설명, 위치, 함께한 사람 태그, 공개 범위• 출력 (Output):◦ 앨범방 피드 내 카드 생성 및 상세 화면 구성◦ 해당 콘텐츠 하단에 '마음 메시지 남기기' 영역 활성화• 예외 처리 (Exception):◦ [예외 1] 지원하지 않는 파일 형식인 경우 $\rightarrow$ "지원하지 않는 파일 형식이에요." 안내.◦ [예외 2] MVP 기준 영상 길이(2~3초) 초과 시 $\rightarrow$ "무료 버전에서는 2~3초 영상만 업로드할 수 있어요." 차단 안내.• 기능 정책 (Policy):◦ 초대를 통해 가입된 해당 앨범방의 구성원만 업로드 권한을 가짐.◦ 업로드된 모든 미디어 데이터는 클라우드 저장 구조로 운영됨.[MSG-01] 사진/영상별 마음 메시지 남기기• 상세 동작 (Overview):1. 사용자는 사진/영상 상세 화면에서 '마음 남기기' 버튼을 선택한다.2. 메시지 유형(텍스트 한마디, 음성 메시지, 짧은 영상 메시지)을 선택한다.3. 내용 작성 또는 녹음/촬영 후 저장한다.4. 저장 완료 시 해당 콘텐츠 하단에 답글 형태로 노출되며, 동시에 감정 사서함으로 자동 전송된다.• 입력 (Input):◦ 필수: 메시지 내용 또는 음성/영상 파일◦ 선택: 감정 태그(사랑해, 고마워 등), 공개 범위, 이모지 리액션• 출력 (Output):◦ 콘텐츠 하단 답글 등록, 감정 사서함 적재, 상대방에게 푸시 알림 발송• 예외 처리 (Exception):◦ [예외 1] 빈 메시지(텍스트 미입력, 녹음 데이터 없음 등) 저장 시도 시 $\rightarrow$ "내용을 입력해주세요." 안내하며 저장 불가.◦ [예외 2] 녹음/촬영 권한 거부 시 $\rightarrow$ 권한 허용 유도 팝업 노출 및 텍스트 입력 대체 안내.• 기능 정책 (Policy):◦ 실시간 채팅 창이 아닌, 특정 사진/영상을 타겟으로 하는 비동기식 답글 구조로 운영함.◦ 분량 제한: 텍스트 최대 300자, 음성 메시지 최대 60초, 영상 메시지 최대 2~3초 제한.[INVITE-01] 초대 링크 및 QR 생성• 상세 동작 (Overview):1. 앨범방 관리자가 방 상세 설정에서 '구성원 초대'를 선택한다.2. 초대할 상대방과의 관계(엄마, 아빠, 연인 등)를 선택한다.3. 초대자가 상대방에게 보낼 첫 '초대 메시지(음성/텍스트/영상)'를 작성한다.4. 작성이 완료되면 공유용 고유 초대 링크와 QR 코드가 시스템적으로 생성된다.• 입력 (Input):◦ 필수: 초대 메시지, 초대 대상 관계◦ 선택: 초대 대상 이름, 링크 만료 기간, QR 코드 생성 여부• 출력 (Output):◦ 생성된 초대 링크 번들 및 QR 코드 팝업◦ 카카오톡, 문자 메시지 등 외부 앱 공유 컴포넌트 노출• 예외 처리 (Exception):◦ [예외 1] 초대 메시지를 작성하지 않고 링크 생성을 시도할 때 $\rightarrow$ "초대 메시지를 먼저 작성해야 링크를 만들 수 있어요." 안내.• 기능 정책 (Policy):◦ QR 코드는 디지털 취약 계층(부모님 세대)이 오프라인 모임 등에서 카메라만 켜서 쉽게 진입할 수 있도록 돕는 필수 보조 수단으로 활용함.[INVITE-02] 답장 후 앨범 입장• 상세 동작 (Overview):1. 초대받은 사용자가 웹 링크나 QR을 통해 랜딩 페이지에 접속한다.2. 초대자가 남겨둔 감성 메시지(음성, 영상 등)가 화면에 선재생된다.3. 전체 앨범 조회를 위해 회원가입 및 로그인을 진행한다.4. 가입 후 반드시 초대 메시지에 대한 '답장 메시지'를 작성해야 한다.5. 답장 저장이 완료되면 정식 구성원으로 전환되며 비로소 앨범방에 입장한다.• 입력 (Input):◦ 필수: 회원가입/로그인 정보, 답장 메시지◦ 선택: 프로필 사진, 관계명 설정• 출력 (Output):◦ 초대 메시지 플레이어 화면 $\rightarrow$ 가입 폼 $\rightarrow$ 답장 작성 폼 순차 노출◦ 완료 시 초대한 사용자에게 "답장이 도착했어요" 알림 발송• 예외 처리 (Exception):◦ [예외 1] 가입 후 답장을 미작성하고 이탈하려 하거나 방 진입을 시도할 때 $\rightarrow$ 입장 제한 팝업을 노출하며 답글 작성을 강제함.◦ [예외 2] 시니어 유저가 음성/영상 답장이 곤란한 상황일 때 $\rightarrow$ 편리한 소통을 위해 텍스트 입력 탭으로 쉽게 전환할 수 있도록 UI 제공.• 기능 정책 (Policy):◦ 첫 답장 완료 프로세스는 서비스 가입을 완료한 '준회원'이 실제 방의 '구성원' 권한을 획득하기 위한 필수 관문임.◦ 이때 작성된 첫 답장 데이터 역시 피드 및 감정 사서함에 자동 보관됨.[BOX-01] 감정 사서함 저장• 상세 동작 (Overview):1. 서비스 내 모든 사진/영상 카드에 구성원들이 남긴 '마음 메시지'가 등록되는 순간 동작한다.2. 시스템은 해당 메시지 데이터를 추출하여 방별 '감정 사서함' 탭에 백그라운드로 자동 적재한다.3. 사용자는 사서함 메뉴 내에서 주고받은 내역을 필터링하여 상시 조회할 수 있다.• 입력 (Input):◦ 필수: 자동 연동되는 메시지 데이터셋◦ 선택: 사용자가 지정한 감정 태그, 즐겨찾기(보관함) 여부• 출력 (Output):◦ '받은 마음', '보낸 마음' 리스트 화면 및 날짜별/구성원별 소트(Sort) 필터◦ 리스트 클릭 시 해당 메시지 데이터 및 원본 사진 링크 상세 재생• 예외 처리 (Exception):◦ [예외 1] 원본 사진/영상이 삭제된 경우 $\rightarrow$ "원본 게시물이 삭제된 마음입니다. 메시지는 계속 보관됩니다." 안내 텍스트와 함께 텍스트/음성 데이터는 유지.• 기능 정책 (Policy):◦ 데이터는 프라이빗 보안 정책에 따라 본인이 발신했거나 본인이 수신한 메시지만 사서함에서 열람할 수 있음.◦ 기간 만료 등으로 인한 자동 삭제는 없으며, 사용자가 직접 수동 삭제하는 것을 원칙으로 함.[MISSION-01] 답장 미션 및 열람 제한• 상세 동작 (Overview):1. 사용자가 특정 구성원(발신자 A)이 보낸 마음 메시지를 순차적으로 열람한다.2. 시스템은 발신자별 메시지 확인 카운트를 체크하며, 정상 열람은 5개까지 허용한다.3. 사용자가 해당 발신자의 6번째 메시지를 클릭하여 열람을 시도하는 순간 미션 화면(Lock)을 활성화한다.4. 미션 화면에서 텍스트, 음성, 영상 중 하나의 수단으로 상대방에게 답장을 전송한다.5. 답장 저장이 완료되면 시스템은 락을 해제하고 6번째 메시지의 내용을 보여준다.• 입력 (Input):◦ 필수: 미션 클리어를 위한 답장 메시지 데이터◦ 선택: 답장 미디어 수단 선택• 출력 (Output):◦ 6번째 메시지 진입 시 "상대방에게 마음을 표현해보세요! 답장 후 확인이 가능합니다" 라는 안내와 함께 입력 폼 노출◦ 완료 시 락 해제 애니메이션 및 메시지 오픈• 예외 처리 (Exception):◦ [예외 1] 미션을 완료하지 않고 팝업을 닫거나 이탈할 경우 $\rightarrow$ 해당 메시지는 계속 잠금(Lock) 상태를 유지함.◦ [예외 2] 여러 명의 구성원이 있을 경우 $\rightarrow$ 발신자 B가 보낸 메시지는 카운트를 완전히 별개로 처리하여 발신자 A의 락이 B에게 영향을 주지 않음.• 기능 정책 (Policy):◦ 사용자에게 과도한 피로감을 주지 않기 위해 앱 전반적인 기능(사진 보기, 사서함 조회 등)을 막는 것이 아니라, 오직 '해당 발신자의 다음 메시지 내용을 확인하는 행동'에만 락을 적용함.

6. Information Architecture (정보 구조도)
• Depth 1: 홈 (Home)◦ Depth 2: 앨범방 리스트 카드▪ Depth 3: 방 만들기 버튼 (-> 앨범방 생성 프로세스 진입)◦ Depth 2: 앨범방 메인 피드 (방 진입 시)▪ Depth 3: 추억 올리기 버튼 (사진/영상 업로드)▪ Depth 3: 초대하기 버튼 (초대 링크/QR 생성)▪ Depth 3: 사진/영상 카드 리스트◦ Depth 2: 콘텐츠 상세 화면▪ Depth 3: 사진/영상 뷰어▪ Depth 3: 마음 남기기 영역 (텍스트/음성/영상 입력 컴포넌트)▪ Depth 3: 답장 미션 팝업 (조건 충족 시 락 화면 노출)• Depth 1: 감정 사서함 (Maeum Box)◦ Depth 2: 받은 마음 탭▪ Depth 3: 메시지 리스트 (날짜별/구성원별 필터링 기능)▪ Depth 3: 메시지 상세 모달 플레이어◦ Depth 2: 보낸 마음 탭▪ Depth 3: 내가 보낸 메시지 이력 리스트• Depth 1: 마이페이지 (My Page)◦ Depth 2: 계정 관리 및 설정◦ Depth 2: 알림 설정
실습 프롬프트3 - PRD 개선
1. 개발자가 코드 짜다가 되물어볼 모호한 표현
[ALBUM-01] 앨범방 생성
PRD 내용: "동일 이름 생성 시 허용하되 구분용 생성일 표시"
QA의 태클: 구분용 생성일을 어디에, 어떤 포맷으로 표시하라는 건가요?
홈 화면 카드 타이틀 옆에 (2026-07-10) 처럼 붙이는 건지, 아니면 카드 하단에 작게 타임스탬프를 찍는 건지 명확하지 않습니다.
데이터베이스 단에서 DB 유니크 키(Unique Key)를 방 이름이 아닌 별도의 room_id로 파야 한다는 힌트는 되지만, UI/UX 관점의 출력이 흐릿합니다.
[MISSION-01] 답장 미션 및 열람 제한
PRD 내용: "동일 발신자의 메시지를 5개까지 읽으면 답장 미션이 활성화된다."
QA의 태클: 이 '5개'의 기준이 누적인가요, 아니면 미답장 상태로 연속된 개수인가요?
만약 누적이라면, 사용자가 서비스 가입 후 평생 동안 상대방에게 5번 메시지를 읽고 답장을 다 했어도 6번째 메시지부터 영원히 락이 걸린다는 뜻이 됩니다.
개발자는 is_read뿐만 아니라 has_replied 상태 값이나 unread_message_count 같은 카운팅 로직을 설계해야 하므로, 이 조건은 "답장을 하지 않은 채 쌓인 상대방의 미확인/미답장 메시지가 연속 5개일 때" 처럼 물리적으로 정의되어야 합니다.
2. 누락된 예외 처리 및 기획적 구멍 (Edge Case)
💣 초대 링크/QR과 회원가입 흐름의 모순 ([INVITE-01], [INVITE-02])
상황: "초대 메시지 작성 후에만 링크가 생성" 되며, 초대받은 사람은 "가입 후 반드시 초대 메시지에 대한 답장을 작성해야 방에 진입" 합니다.
엣지 케이스: 만약 '엄마'를 초대하기 위해 링크를 생성해서 카톡으로 보냈는데, 엄마가 스마트폰 조작 실수로 링크를 누르고 가입 폼까지만 진행한 뒤 앱을 강제 종료했다면 어떻게 되나요?
다시 카톡 링크를 누르고 들어왔을 때, 기획상 가입은 이미 되어 있으니 준회원 상태일 텐데, 이때도 초대 메시지가 선재생되고 답장 폼이 정상 작동하나요?
만약 이 링크를 엄마가 아닌 '아빠'가 잘못 눌러서 가입했다면, 시스템은 아빠를 '엄마' 권한으로 방에 매핑하게 되나요? (초대 생성 시 이미 관계를 '엄마'로 지정했으므로)
💣 미디어 업로드 실패 및 비동기 업로드 정책 ([ALBUM-02])
상황: 사진/영상을 업로드하면 피드에 등록되고 구성원에게 알림이 갑니다.
엣지 케이스: 시니어 가구 특성상 와이파이가 불안정한 환경이 많습니다. 3초짜리 고용량 영상을 업로드하다가 80% 시점에서 네트워크가 끊기거나 백그라운드로 전환되면 어떻게 처리하나요?
UI에 '업로드 중...'인 프로그레스 바를 계속 띄워둘 것인지, 실패 시 로컬(Local DB)에 임시 저장을 해두고 재연결 시 자동 재시도(Retry) 알고리즘을 돌릴 것인지 백엔드/클라이언트 간 싱크 정책이 전무합니다.
💣 삭제된 원본의 '감정 사서함' 보존 법칙 ([BOX-01])
상황: "원본 사진/영상이 삭제되어도 사서함 내 메시지 데이터는 유지" 한다고 했습니다.
엣지 케이스: 앨범 피드에서 원본 사진이 삭제되었습니다. 사서함에서 그 메시지를 클릭하면 "원본 게시물이 삭제된 마음입니다"라고 뜹니다.
그런데 그 메시지가 [MISSION-01]의 답장 미션 카운트(5개)에 포함되어 있던 메시지라면? 원본이 삭제되었으니 사용자는 그 메시지에 답장을 남길 수 없게 되나요? 답장을 못 남기면 다음 메시지 열람 락(Lock)은 영원히 안 풀리나요? 미션 카운트에서 삭제된 원본의 메시지는 제외해 주는 로직이 누락되었습니다.
3. 개발자를 춤추게 할 구체적인 수정 제안 (PRD 보완 리팩토링)
PRD 스펙을 명확한 개발 스펙으로 전환하기 위해 아래와 같이 조항을 수정 및 추가해 주세요.
수정안 1: [MISSION-01] 미션 활성화 조건 논리 정립
기존: 동일 발신자의 메시지를 5개까지 읽으면 미션 활성화
변경: "특정 발신자가 보낸 메시지 중, '수신자가 읽었으나 답장을 하지 않은 메시지'가 누적 5개가 되는 시점에 락(Lock) 스케줄러가 발동한다. 6번째 메시지 데이터는 서버에서 클라이언트로 보낼 때 본문(Body)을 블러(Blur) 처리하고 마스킹된 상태(is_locked: true)로 내려주며, 답장 API가 성공 수신(200 OK)되어야만 해당 발신자의 락 카운트가 0으로 초기화되고 메시지가 복호화된다."
수정안 2: [INVITE-02] 초대 검증 및 예외 스펙 추가
추가할 예외 처리:
[예외 3] 준회원 상태에서 이탈 후 재진입 시: 가입 세션은 유지하되, has_replied_first_invite: false인 경우 홈 화면 진입을 차단하고 웰컴 페이지(초대 메시지 재생 및 최초 1회 답장 필수 폼)로 강제 리다이렉트(Redirect)한다.
[예외 4] 관계 설정 미스매치 방지: 초대 링크 생성 시 고유 토큰(invite_token)을 발행하며, 가입 단계에서 유저가 자신의 최종 프로필 관계명(예: 엄마 -> 아빠)을 수동으로 수정할 수 있는 방어 UI를 한 단계 제공한다.
수정안 3: [BOX-01] 원본 삭제 시 미션 예외 정책 추가
추가할 기능 정책:
[제한정책] 앨범방 피드에서 사진/영상이 삭제될 경우, 사서함 내 텍스트/음성 메시지는 보존되나 [MISSION-01]의 미션 제한 카운팅 대상에서는 즉시 제외 처리한다. 만약 6번째 잠겨있던 메시지의 원본이 삭제된 경우, 해당 락은 무조건 자동 해제(is_locked: false)된다.
