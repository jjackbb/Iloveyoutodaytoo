# 오늘도 사랑해 — PRD

> **이 문서를 읽는 법**
> - **§0 지금 상태**만 보면 오늘 이 서비스가 어디까지 왔는지 알 수 있다.
> - **§4 상세 명세**의 각 항목에는 `✅구현됨 / ⏸보류 / ❗미구현 / 🗑폐기` 상태가 붙어 있다.
>   **상태가 없는 문장은 없다.** "PRD에 써 있으니 되는 줄 알았다"를 막기 위해서다.
> - **§8 패치 기록**은 커밋 하나에 한 줄. **§9 문서 수정 이력**은 기획 문구가 바뀐 기록.
>
> **2026-08-25 전면 정리.** 그전 문서는 제목이 하나뿐이고 표가 한 줄로 뭉개져 있었으며,
> 뒤쪽 절반이 강의 실습 과제 잔해였다. 그 잔해는 `frontend/_workspace/16_prd_qa_legacy.md`
> 로 옮겼다(지우지 않았다).

---

## 0. 지금 상태 (2026-09-06)

| | |
| :--- | :--- |
| **한 줄 정의** | 쑥스러운 마음을 목소리로 남기는 곳, 우리끼리의 비밀 감정 사서함 |
| **형태** | **안드로이드 앱만.** 웹 서비스는 🗑폐기(2026-09-06) — §6⑧·⑮ |
| **배포 주소** | https://iloveyoutodaytoo.vercel.app/ — 앱 전환까지의 개발용. 서비스로는 안 연다 |
| **스택** | Next.js 16 + Supabase(ap-northeast-2) + Tailwind v4 + Pretendard<br>**Capacitor + 원격 URL** 로 감싼다(2026-09-06 확정 — §6⑮) |
| **가입자** | 0명 (2026-08-24 전체 초기화 — 깨끗한 상태에서 유저 테스트 시작) |
| **로그인** | 지금은 아이디/비밀번호. **최종은 휴대폰·카카오·구글**(2026-09-06 확정) — 아이디 방식은 그때까지의 임시 |
| **강조색** | **선명한 번트 `#BF3F0D`** (2026-09-06 확정) — 새 로고에서 뽑았다. 토큰 4줄 전부 WCAG AA 통과. 근거는 `design.md` §2 |
| **북극성** | **"숙제가 아닌 하루 중 즐거운 일과"** (2026-09-06 확정 — §1) |
| **지금 하는 일** | ① **브랜드 재정의** — 로고부터 새로 뽑고 거기서 색을 뽑는다 ② **앱 전환 준비** |

> **⚠️ 지금 앱이 무채색인 것은 고장이 아니다.**
> 남보라를 `globals.css` 토큰에서 걷어내고 **임시 중성 회색**(`#3a3a3c` 계열)을 넣었다.
> **색이 비어 있는 상태**를 그대로 보이게 두는 것이 목적이다 — 어중간한 색을 임시로
> 채워두면 그게 굳어버린다.
> 새 로고에서 강조색이 나오면 **토큰 네 줄만** 고친다. 나머지 219곳은 따라온다.

**당면 목표는 기능 추가가 아니라 "유저 테스트로 검증하고 고치는 사이클"을 도는 것이다.**

> **⚠️ 2026-09-06에 큰 결정이 셋 있었다. 이 문서 곳곳이 아직 그 전 상태로 적혀 있을 수 있다.**
> ① **웹 폐기, 안드로이드 앱만** (§6⑧ 뒤집힘) ② **손글씨 도입, 음성 필수 해제** (§6⑯)
> ③ **답장 미션의 락 제거** (§4 MISSION-01). 어긋난 문장을 발견하면 새 결정이 옳다.

**지금 막고 있는 것 (사람이 해줘야 진행됨)**

| 무엇 | 왜 멈춰 있나 |
| :--- | :--- |
| GA4 측정 ID | 계측 코드는 다 붙었다. `NEXT_PUBLIC_GA_ID` 가 없으면 스크립트를 아예 안 붙이므로 **지금은 아무 숫자도 안 쌓인다.** 보관 기간 14개월 설정도 함께 필요 |
| SOLAPI 계정 | 보호자 문자 인증(만 14세 미만)의 마지막 조각 |
| 구글 플레이 개발자 계정 | 스토어 등록에 필요하다(1회 등록비). 없으면 앱을 올릴 수 없다 |
| ~~로고 선택~~ | ✅ **끝(2026-09-06).** 봉투에서 나오는 하트 마크 확정, 강조색 `#BF3F0D` 확정, `design.md` 신설. 다음 상류는 DESIGN-03(아이콘 한 벌·규격) |
| ~~디자인 시안 선택~~ | 🗑 무효. 3안(A 조용한 선 / B 포근한 면 / C 목소리가 주인공)은 몽실이가 있다는 전제로 뽑은 것이라 다시 뽑는다 |

---

## 1. 서비스 개요

> **2026-09-06 신설.** 이 절에는 "무엇을 만드나"만 있고 **"왜 만드나"가 없었다.**
> 그 '왜'는 7월 디자인 스프린트가 이미 정해놓고 피그마·노션에만 남아 있었다.
> 아래는 **원문 그대로** 옮긴 것이다. 사본을 늘리지 않으려고 결론만 가져오고,
> HMW 35개와 Can We? 17개는 옮기지 않았다 — 피그마 보드 PRD 섹션에 있다.

### 왜 만드는가

- **핵심 문제** — 독립·취업으로 자녀와 부모 사이가 물리적·심리적으로 멀어지면서,
  서로에게 안부나 애정을 표현할 **계기(핑계)** 가 사라졌다. 카카오톡 대화는 휘발되고
  깊이 있는 소통으로 이어지지 않는다.
- **스프린트의 문제 정의(원문)** — "소중한 존재(가족·연인·친구)에게 마음을 표현하고
  싶지만, 쑥스러움과 미룸 때문에 못 하고, **'나중에 못 만나게 됐을 때 후회할까' 두려운 사람**"
- **제공 가치** — 마음을 남길 계기를 매일 만들어주고, 남긴 것이 **쌓여서** 관계의 밀도로
  돌아오게 한다. 표현 수단이 아니라 **주고받음·현장감·쌓임의 경험**이 차별점이다.

### 북극성과 핵심 축 (스프린트 투표로 확정, 2026-07-08)

| | 축 | 지금 상태 |
| :--- | :--- | :--- |
| ⭐ | **숙제가 아닌 '하루 중 즐거운 일과'** — 북극성 | 두 사람 모두 표를 준 유일한 항목 |
| ② | **양방향으로 주고받는 관계** | 댓글·사서함이 정면으로 다룬다 |
| ③ | **살아있는 현장감 = 손길** | 손글씨 도입으로 되살린다(§6⑯) |

한 문장으로: **"부담 없이 즐겁게, 서로 주고받으며, 생생하게 남는 관계 공간."**

> **⚠️ 북극성이 답장 미션과 충돌한다.** Can We? 17개 중 3개가 "숙제처럼 느끼지 않게"를
> 반복해서 걱정하는데, 정작 만든 지속 장치는 **안 하면 잠기는 락**이었다.
> 2026-09-06에 **북극성을 택하고 락을 없애기로** 정리했다 — §4 MISSION-01.

### 장기 목표 (2년 뒤, 스프린트 확정문)

> "2년 뒤, 우리 앱을 쓰는 사람들은 소중한 존재에게 매일 자연스럽게 마음을 표현하며
> 추억을 함께 쌓아, **'우리가 이렇게 많은 마음을 나눴구나'라는 관계의 밀도**를 실감하게
> 되었다. 그리고 겉으로는 무심했던 자신에게서 피어나던 후회와 두려움을 더 이상 겪지 않게 되었다."

한 줄로는 **"사랑과 추억을 가장 오래 보관하는, 디지털 감정 아카이브."**

> 이 정의는 **상대가 답하지 않아도 성립한다.** 혼자 쓰는 사람을 버리지 않기 위한
> 안전장치가 여기서 나온다 — §4 HEART-01.

---

## 2. 타깃 사용자

**주 사용자 — 자녀 (2030 직장인)**
따로 살며 바쁘다는 핑계로 부모님께 다정한 말을 건네지 못하는 것에 **부채감**을 느낀다.
**"후회하고 싶지 않다"** 가 이 사람을 움직이는 감정이다.
앱에 능숙하고 인스타·틱톡의 제스처가 몸에 익어 있다.

**동반 사용자 — 부모 (5060), 초대받아 들어온다**
자녀의 일상이 궁금하지만 방해가 될까 봐 먼저 연락하기 조심스럽다.
**카카오톡 등 앱을 무리 없이 쓴다** — 사진을 보내고, 꾹 눌러 답장하고, 프로필을 바꾼다.
새 개념은 낯설어도 익숙한 동작은 그대로 한다.

**대상이 아닌 사용자** — 70대 이상 디지털 취약 계층.

> **⚠️ 이 구조가 모든 판단의 근거다.**
> **획득(가입)은 2030에게, 활성화(실사용)는 5060에게** 일어난다.
> 그래서 ① 퍼널을 **둘로 나눠** 측정하고 ② 부모 쪽 단계를 **가장 짧게** 유지한다.
>
> **개정(2026-09-06) — ②의 전제를 바꾼다. 장벽은 설치가 아니라 신뢰다.**
>
> 원래 ②는 **"설치 장벽을 부모에게 지우지 않는다"**(그래서 웹으로 먼저 검증한다)였다.
> **이 전제 자체를 버린다.** 사용자 판단: *"요즘 시대에 '설치 장벽'은, 스팸이나 사기가
> 아닐까 하는 걱정만 없으면 그런 건 없다."* 부모 세대는 카톡·유튜브·은행 앱을 이미
> 깔아 쓴다. 설치를 못 해서 못 들어오는 게 아니다.
>
> **스팸·사기 걱정도 이 서비스에는 해당하지 않는다.** 부모에게 앱이 닿는 길이
> 둘뿐인데 **둘 다 자녀를 거치기 때문이다**(사용자 판단):
>
> > ① 자녀가 먼저 쓰다가 **초대**한다.
> > ② 자녀가 쓰는 걸 보거나 듣고 부모가 **먼저 하자고 한다.**
>
> 낯선 앱을 부모가 혼자 찾아 설치하는 장면이 없다. **신뢰는 자녀가 담보한다.**
> 그래서 "부모가 믿게 만들기"를 따로 설계할 필요가 없다.
>
> **남는 것은 하나뿐 — 부모 쪽 걸음을 짧게 유지한다.** 그중 실제로 위험한 것은
> 걸음 수가 아니라 **맥락이 끊기는 지점**이다: 설치하고 앱을 열었을 때
> 방금 누른 초대가 무엇이었는지 앱이 모르면 부모는 "그래서 뭘 하라는 거지"가 된다.
> → **설치 후 초대 복원(딥링크)** 은 반드시 붙인다.
>
> **⚠️ 아이폰 사용자는 쓸 수 없다.** 웹을 폐기했고 iOS도 만들지 않기 때문이다(§6⑮).
> 이건 감수한 것이지 놓친 것이 아니다 — §7⑥.
>
> **UX 기준선은 "시니어"가 아니라 카카오톡이다.**
> 2026-08-20 개정 — 그전에는 부모를 "디지털이 서툰 시니어"로 적어두고 그 한 줄로
> 글자 크기·제스처 금지까지 정당화했다. 조심할 대상은 어르신이지 부모님이 아니다.
> → `frontend/_workspace/12_ux_baseline.md`

---

## 3. MVP 기능 목록

> 이전 문서에서 **이 절이 통째로 비어 있었다.** 상세 명세(§4)와 실제 코드를 대조해 채웠다.

> **노션 IA 열은 2026-09-06 신설이다.** 세 기획서(피그마 · 노션 · 이 문서)를 하나로
> 합치는 대신 **번호로 잇는다.** 내용을 복사해 세 벌로 만들면 또 갈라지기 때문이다.
> 번호는 노션 '오늘도사랑해' IA 문서의 `1.1`~`6.11` 체계다.

| ID | 기능 | 노션 IA | 상태 |
| :--- | :--- | :--- | :--- |
| ALBUM-01 | 앨범방 생성 | `3.1` | ✅ |
| ALBUM-02 | 사진·영상 업로드 | `3.7` | ✅ (영상 길이 정책은 §6-③ 개정) |
| MSG-01 | 사진/영상별 마음 메시지 (텍스트·음성·영상) | `3.9` | ✅ (감정 태그는 🗑폐기) |
| **WRITE-01** | **손글씨로 마음 남기기** (획 좌표 저장) | — | ❗**미구현 · 신규** — §4 명세 확정 |
| **WRITE-02** | **쓰는 과정 타임랩스 재생** (스프린트 `S5`) | — | ❗**미구현** — WRITE-01 **바로 다음**. 핵심 축 ③현장감의 본체 |
| **WIDGET-01** | **홈 화면 위젯 + 톡톡** (스프린트 `S1` `S2`) | — | ❗**미구현 · 1급 기능**(2026-09-06 결정) — §6⑰ |
| INVITE-01 | 초대 링크·QR 생성 | `3.2` | ✅ |
| INVITE-02 | 초대 링크로 가입 후 앨범 입장 | `3.3` | ✅ (준회원 관문은 🗑폐기) |
| BOX-01 | 감정 사서함 | `2.2` | ✅ |
| **HEART-01** | **마음 보내기 — 나에게 · 랜덤 · 방 전체 · 개별** | `2.2` `4.1` `4.2` | ✅ **구현됨** — 이 문서에만 빠져 있었다 |
| ~~MISSION-01~~ | ~~답장 미션 · 열람 락~~ | — | 🗑**폐기 완료**(2026-09-06). 코드에서 걷어냈다 — §4 |
| AUTH-01 | 아이디 가입 + 만 14세 미만 보호자 동의 | `1.2` | ✅ (문자 확인은 작업 중) |
| AUTH-02 | 소셜 로그인 (**휴대폰 · 카카오 · 구글**) | `1.1` | ❗**예정** — 보류에서 올라옴(2026-09-06). 최종 로그인 방식 |
| **ACCOUNT-01** | **회원 탈퇴** (사유 선택 · 데이터 보존 안내) | `5.2` | ✅ **구현됨** — 이 문서에만 빠져 있었다 |
| **BLOCK-01** | **차단 목록** | 피그마 IA | ✅ **구현됨** — 이 문서에만 빠져 있었다 |
| PUSH-01 | 알림 | — | ⚠️ **웹푸시 코드 완료·실기기 미검증.** 웹 폐기로 **FCM이 본체가 된다**(§6④). 락을 뺀 자리를 메워야 해서 **지금 가장 급하다** |
| PROMPT-01 | 오늘의 질문 (주간 순환) | — | ✅ **주 단위 유지 확정** |
| **DAILY-01** | **매일 바뀌는 자리** — "작년 오늘" · "이번 달 N번 남겼어요" | — | ❗**미구현 · 신규** — 질문이 주 단위라 매일 열 이유를 이쪽이 맡는다 |
| ANALYTICS-01 | GA4 계측 | — | ⚠️ 코드 완료 · **측정 ID 없어 수집 안 됨** |
| **APP-01** | **안드로이드 앱 전환** — Capacitor + 원격 URL | — | ❗**미착수** — 래핑 방식 확정됨(§6⑮) |
| ~~COMPOSE-01~~ | ~~몽실이 작성 흐름~~ | — | 🗑**폐기** (2026-09-06 팀 회의) — §6② |
| DESIGN-01 | 브랜드 팔레트·로고·서체 | `1.1` | ❗**재작업** — 로고·색을 새로 뽑는다. 서체(Pretendard 700)만 확정 |
| DESIGN-03 | 디자인 시스템 (아이콘 한 벌·모서리·여백 규격) | — | ❗**대기** — DESIGN-01(로고·색) 확정 전에는 시작하지 않는다 |
| SIGNUP-01~03 | 가입 폼 — 살아 있는 규칙 체크·랜덤 이름·아이디 중복확인 | `1.3` | ✅ |

> **⚠️ 이 표에 없지만 돌아가는 화면이 더 있을 수 있다.** 2026-09-06에 코드와 대조해
> 세 개(HEART-01 · ACCOUNT-01 · BLOCK-01)를 찾아 넣었지만, 전수 조사는 하지 않았다.
> 갤러리 뷰(`3.6`)·숨김 피드 관리(`3.5`) 등은 아직 대조하지 못했다.

---

## 4. 상세 기능 명세

### [ALBUM-01] 앨범방 생성 ✅ · 노션 IA `3.1`

1. 홈에서 '앨범방 만들기' → 2. 이름 입력 → 3. 대표 이미지/기본 테마 선택 →
4. 생성 완료 시 홈에 카드 추가 + 상세 화면 이동. 방 전용 **기본 감정 사서함**이 함께 생긴다.

- **입력** — 필수: 앨범방 이름 / 선택: 대표 이미지, 설명, 기본 테마
- **예외 1** — 이름 미입력 → "앨범방 이름을 입력해주세요."
- **예외 2** — 🗑 **폐기(2026-09-06).** 원래는 "무료 1개 초과 시 프리미엄 안내"였다.
  **개수 제한을 두지 않는 것이 정식 정책이다** — "구독이 보류라 임시로 풀렸다"가 아니다.
  가족·연인·친구 방을 따로 만드는 것이 자연스럽고, **방이 많을수록 쓸 이유가 늘어난다.**
- **정책** — 로그인 회원만 생성, 생성자가 관리자. 이름 2~20자 권장.
- **🗑폐기** — 관계 유형(가족/연인/친구/나 자신) 선택. 구현이 이미 안 묻고 있었고 문서만 남아 있었다.
- **개정** — 앨범방 이름·커버는 **각자 자기 화면에서만** 바꾼다(카카오톡 단체방 방식).
  방장이 바꿔서 모두의 화면이 바뀌면 "어제 있던 방이 없어진" 것으로 읽힌다.

### [ALBUM-02] 사진 및 짧은 영상 업로드 ✅ · 노션 IA `3.7`

1. 방에서 '추억 올리기' → 2. 사진/영상 선택 → 3. 설명 입력 → 4. 업로드 완료 시 피드 등록 + 구성원 알림.

- **입력** — **개정됨(2026-08-25).** 필수는 **목소리 3초 이상** 하나뿐이다.
  사진·영상·날짜·문구·함께한 사람은 전부 **선택**이다 — 그전에는 사진 1장 이상을 함께
  요구했으나, 목소리가 중심인 앱에서 사진을 강제하면 "지금 찍을 사진이 없어서"
  마음을 못 남긴다(§6-⑫).
- **예외 1** — 지원하지 않는 형식 → "지원하지 않는 파일 형식이에요."
- **예외 2** — **개정됨.** 원래 "2~3초 초과 차단"이었으나 **최대 30초**로 바꿨다(§6-③).
  Supabase 무료 플랜의 **파일당 50MB 상한**도 함께 걸리므로 **길이·용량 두 가지를 모두** 검사하고,
  막힐 때는 실제 용량을 숫자로 보여준다.
- **정책** — 해당 방 구성원만 업로드. 미디어는 클라우드(Supabase Storage) 저장.

### [MSG-01] 사진/영상별 마음 메시지 ✅ · 노션 IA `3.9`

1. 상세 화면에서 '마음 남기기' → 2. 유형 선택(텍스트/음성/영상) → 3. 작성·녹음 →
4. 저장 시 답글로 노출 + **감정 사서함 자동 적재**.

- **분량** — 텍스트 300자, **음성 60초**, **영상 30초**
- **예외 1** — 빈 메시지 저장 시도 → "내용을 입력해주세요."
- **예외 2** — 녹음·촬영 권한 거부 → 권한 안내 + 텍스트 입력으로 대체
- **정책** — 실시간 채팅이 아니라 **특정 사진/영상을 겨눈 비동기 답글** 구조
- **🗑폐기** — 감정 태그(사랑해·고마워 등). 고르는 부담만 늘고 쓰이지 않았다.

### [INVITE-01] 초대 링크 및 QR 생성 ✅ · 노션 IA `3.2`

1. 방 설정에서 '구성원 초대' → 2. 초대 메시지(음성/텍스트/영상) 작성 →
3. 작성 완료 시 고유 초대 링크와 QR 생성.

- **예외 1** — 메시지 없이 링크 생성 시도 → "초대 메시지를 먼저 작성해야 링크를 만들 수 있어요."
- **정책** — QR은 오프라인 모임에서 카메라만 켜서 들어올 수 있게 하는 보조 수단.
  (원문의 "디지털 취약 계층" 표현은 타깃 개정에 따라 삭제)
- **🗑폐기** — 초대 시 관계(엄마·아빠·연인) 선택. ALBUM-01의 관계 유형 폐기와 같은 이유.

### [INVITE-02] 초대 링크로 가입 후 앨범 입장 ✅ · 노션 IA `3.3`

1. 링크·QR로 랜딩 접속 → 2. 초대자의 메시지 선재생 → 3. 회원가입 → 4. **바로 입장**.

- **출력** — 답장을 남기면 초대한 사람에게 "답장이 도착했어요" 알림
- **예외 1** — 음성·영상 답장이 곤란하면 텍스트 탭으로 전환
- **정책** — 첫 답장은 **권하되 막지 않는다.** 남긴 답장은 피드·사서함에 함께 보관된다.

> **🗑 '준회원 → 구성원' 관문은 폐기했다** (2026-08-25).
> PRD에는 있었지만 `member_role` 은 처음부터 `admin | member` 뿐이었다 —
> **코드가 옳았고 문서만 남아 어긋나 있었다.** 이제 가입하면 바로 구성원이 된다.
>
> **폐기한 이유.** 초대로 들어온 사람을 문 앞에서 한 번 더 막는 구조인데,
> 그 문을 통과해야 하는 쪽이 **활성화가 일어나야 할 부모(5060)** 다(§2).
> 여기서 막으면 이탈이 가장 크게 난다.
> 답장은 이미 **답장 미션([MISSION-01])** 이 뒤에서 유도하고 있으므로 관문이 이중이었다.

### [BOX-01] 감정 사서함 ✅ · 노션 IA `2.2`

메시지가 등록되는 순간 방별 사서함에 자동 적재된다. '받은 마음'·'보낸 마음' 탭,
날짜별·구성원별 정렬.

- **예외 1** — 원본이 삭제된 경우 → "원본 게시물이 삭제된 마음입니다. 메시지는 계속 보관됩니다."
  (실제 구현은 **소프트 삭제**다 — 행을 지우지 않고 `deleted_at` 을 찍어 조회에서 뺀다)
- **정책** — 내가 보냈거나 내가 받은 메시지만 열람. 자동 삭제 없음, 수동 삭제 원칙.
- **개정** — 사서함 **정리 모드**는 "내 화면에서만 치우기"다. 진짜로 지우면 상대의 보낸함에서도
  사라지므로 문구도 "삭제"가 아니라 **"치우기"**.

### [~~MISSION-01~~] 답장 미션 — 🗑 **열람 락 폐기 완료** (2026-09-06)

**있었던 것.** 발신자별로 **"읽었지만 답장하지 않은 메시지"** 가 5개가 되면,
그 사람의 다음 메시지에 락이 걸렸다. 답장을 보내면 풀렸다.

**걷어낸 것(앱 계층).** `lib/mission.ts` 삭제 · `actions/mailbox.ts` 의 락 계산 ·
사서함 카드의 잠긴 자리(`LockedHeart`) · `MailboxItem` 의 `locked`/`unrepliedCount`.
타입체크·빌드 통과.

**남긴 것 둘.**
- **읽음 표시(`markHeartRead`)는 살렸다.** 나중에 "○○님이 이번 주 3번 남겼어요" 같은
  신호를 만들 때 쓸 데이터가 이것이다. 지금 지우면 그때 데이터가 없다.
- **DB 함수 `locked_senders()` 는 그대로 뒀다.** 쓰이지 않으니 해가 없고,
  **DB 스키마는 임의로 바꾸지 않는다**(`frontend/CLAUDE.md` 🚨). 정리하려면 따로 결정한다.

**왜 없애기로 했나.** 두 가지 이유가 겹쳤다.

1. **북극성과 정면으로 어긋난다.** 스프린트 투표 1위가 **"숙제가 아닌 즐거운 일과"**였고
   Can We? 17개 중 3개가 "숙제처럼 느끼지 않게"를 반복 걱정했다(§1). 락은 정확히 그 감각이다.
2. **락은 일방 소통 문제를 못 푼다. 오히려 악화시킨다.** 락은 *답을 안 한 사람*을 잠근다.
   그래서 A가 계속 보내고 B가 답을 안 하면 **B가 A의 다음 메시지를 못 열게 된다** —
   들려주고 싶었던 A의 목소리가 오히려 안 들린다.

**🚨 지금 되돌아오게 하는 장치가 사실상 없다.**
원래 계획은 "앱 푸시나 위젯이 확인된 뒤에 락을 뺀다"였지만, **사용자 지시로 먼저 뺐다.**
남은 것은 오늘의 질문(주 단위 고정)과 웹푸시(실기기 검증 없음, §7④)뿐이다.
**이 구간이 비어 있다는 것을 알고 지나간다** — 유저 테스트에서 리텐션 숫자가 낮게 나와도
그것이 서비스의 문제인지 이 공백 때문인지 구분되지 않는다.

> **그래서 앱 푸시(FCM)와 위젯이 다음 우선순위다.** 락을 대신할 것이 그 둘이다.

**락 대신 무엇을 두는가** — 막지 않고 알려만 준다.
`○○님이 이번 주 3번 마음을 남겼어요` 정도의 신호. 앱으로 가면 이 신호가 **앱 밖에서**
계속 보이므로 훨씬 세진다(스프린트가 위젯을 고른 이유가 이것이다).

### [HEART-01] 마음 보내기 — 나에게 · 랜덤 · 방 전체 · 개별 ✅ · 노션 IA `2.2` `4.1` `4.2`

**2026-09-06 신설.** 이미 구현돼 돌아가고 있었는데 **이 문서에만 없었다.**

받는 사람 후보는 네 갈래다(`lib/heart-send.ts`).

| 갈래 | 뜻 |
| :--- | :--- |
| `self` | **나에게** 보내는 마음 |
| `random` | 서버가 **무작위로 한 사람**을 고른다 |
| `room` | 그 방 멤버 **전원에게 각각 한 통씩** |
| `member` | 개별 멤버 |

- **정책** — 화면은 문자열 id(`self` · `random` · `room:{id}` · `member:{room}:{user}`)만
  서버에 넘기고, **누구인지는 서버가 다시 읽어 확인한다.** 객체를 통째로 받으면 화면이
  방·사람 id를 지어낼 수 있다. 마지막 방어선은 RLS다.

> **🔑 이것이 "한 명만 소통하는 경우"의 답이다.**
> 스프린트가 <code>Q1</code>(응답 유도)과 <code>Q8</code>(반응 없어도 혼자서도 계속 쓰게)을
> **"한 쌍"이자 앱의 안전장치**로 설계해뒀고, 피그마 고객여정맵에서도 **개인 사서함**이
> 초록 스티키로 강조돼 있었다. 상대가 답하지 않아도 **마음을 둘 곳이 남는다.**
>
> **그래서 화면에서도 앞으로 꺼내야 한다.** 락을 없애는 것과 한 몸인 작업이다 —
> 락이 하던 "혼자 남지 않게 한다"를 이쪽이 대신 맡는다.
> 함께: 가치를 **'주고받음'이 아니라 '쌓임'** 으로 읽히게 한다.
> `이번 달 12번 주고받았어요` 는 혼자면 실패로 읽히지만
> **`이번 달 12번 남겼어요` 는 혼자여도 성립한다.**
> 한 명만 쓰는 상태를 **실패가 아니라 정상**으로 취급하는 것이 핵심이다.

### [WRITE-01] 손글씨로 마음 남기기 ❗미구현 · 노션 IA `—`

**2026-09-06 신설.** 스프린트가 `S4`로 뽑아 솔루션 스케치까지 그려놓고 IA로 넘어오지
못한 것을 되살린다(§6⑯). **2026-09-06에 정할 것을 전부 정했다.**

- **왜** — 지금 작성 필수가 **음성 3초 하나**뿐이라, 쑥스러움이 문제인 앱이
  가장 쑥스러운 일을 강제하고 있다. 손글씨가 그 구멍을 메운다.
- **정책(정해진 것)** — 손글씨가 들어오면 **음성 필수를 푼다.**
  음성 · 손글씨 · 사진/영상 중 **하나 이상**이면 올릴 수 있다.

**정해진 것 (2026-09-06 전부 확정).**

| | 결정 | 왜 |
| :--- | :--- | :--- |
| **저장 형식** | **획 좌표** | 타임랩스 재생(WRITE-02)이 여기 걸려 있다. 그림 파일로 만들었다가 바꾸려면 **저장된 손글씨를 전부 버려야 한다.** 용량도 그림보다 훨씬 작아 50MB 벽에 유리하다 |
| **넣는 화면** | **앨범방 작성(`3.7`)만** | 음성 필수를 푸는 목적이 여기서 달성된다. 좁게 시작해 반응을 보고 넓힌다 |
| **배경 고르기** | **1차에서 뺀다** | 쓰기 전에 고를 것이 하나 늘면, 부담을 줄이려는 손글씨의 목적과 어긋난다 |
| **타임랩스** | **WRITE-01 바로 다음** | → WRITE-02 |

- **저장** — 획마다 점 배열 + 시간. 재생에 쓸 수 있는 모양이어야 한다.
- **정책** — 손글씨가 들어오면 **음성 필수를 푼다.**
  음성 · 손글씨 · 사진/영상 중 **하나 이상**이면 올릴 수 있다.

### [WRITE-02] 쓰는 과정 타임랩스 재생 ❗미구현

스프린트 `S5`("그린 획의 궤적을 비디오처럼 재생 → 그 순간의 손길 재현").
**핵심 축 ③현장감의 본체다** — 지금 이 축에 대응하는 기능이 하나도 없다.

- **WRITE-01 바로 다음에 만든다.** 손글씨만 있고 재생이 없으면 그냥 그림이다.
- 획 좌표가 이미 쌓이고 있으므로 재생은 비교적 작은 작업이다.
- 열람할 때 **빠른 타임랩스가 지나간 뒤 결과물**을 보여준다(스프린트 Crazy8 6번).

### [WIDGET-01] 홈 화면 위젯 + 톡톡 ❗미구현 · **1급 기능**

**2026-09-06 결정 — 처음부터 넣는다.** 사용자 판단:
*"이게 다시 서비스로 돌아오게 하는 매개체가 되기 때문."*

**왜 1급인가.** 답장 미션 락을 걷어내면서 **되돌아오게 하는 장치가 비었다**(MISSION-01).
그 자리를 메우는 것이 이것이다. 그리고 서비스 정의 문장의 ③단계가 통째로 위젯이다 —
*"① 매일 리마인드+톡톡이로 운을 떼고 → ② 다양한 방식으로 마음을 남기고 →
③ **위젯에서 표현이 되살아나고** 다양한 코멘트로 응답"*(피그마 원문).
**기능 하나가 아니라 서비스 정의의 3분의 1이다.**

**두 가지를 만든다.**

| | 무엇 | 맡은 역할 |
| :--- | :--- | :--- |
| **보여주기** `S2` | 홈 화면 위젯에 **최근 표현**을 띄운다 — *"앱 안 켜도 마음이 보임"* | 지속(`Q3` 즐거운 일과) · **혼자여도 존재감**(`Q8`) |
| **톡톡** `S1` | 위젯을 **꾹 누르면** 상대 폰이 진동한다 — *"○○님이 톡톡했어요"* | 계기(`Q6` 첫 표현 장벽↓) · 응답 유도(`Q1`) |

> **왜 푸시로 대신할 수 없나.** 위젯은 **거기 있는 것**(ambient)이고 푸시는
> **밀어내는 것**(interrupt)이다. 북극성이 **"숙제가 아닌 즐거운 일과"**인데
> 푸시는 숙제 쪽 감각에 가깝다. Can We? 17개 중 3개가 그 감각을 걱정하고 있었다.

**🚨 구현 메모 — 위젯은 웹뷰 밖에서 산다.**
Capacitor로 감싸도 위젯은 **네이티브 안드로이드 코드**(AppWidgetProvider)다.
웹뷰가 가진 로그인 세션·데이터를 **자동으로 보지 못한다.** 그래서 필요한 것 둘:

1. **위젯 전용 API 엔드포인트** — 최근 표현 한 건을 돌려준다. 인증 토큰을 네이티브 저장소에 둔다
2. **톡톡 → 푸시 경로** — 위젯의 누름이 서버를 거쳐 상대에게 알림으로 간다

Locket을 벤치마킹한 것이며, 회의록에 원안이 있다 —
*"상대 폰 홈 위젯에 내 사진/영상 실시간 노출 + 노크 진동 + 내 위젯은 나만 커스텀."*
**'내 위젯 디자인은 나만 바꾼다'(상대는 못 바꾼다)** 는 원안을 지킨다 —
앨범방 이름·커버를 각자 화면에서만 바꾸는 규칙(§4 ALBUM-01)과 같은 사상이다.

### [AUTH-01] 가입과 만 14세 미만 보호자 동의 ✅ / 문자 확인 작업 중 · 노션 IA `1.2`

- 가입하는 **모든 사람에게 생년월일**을 받는다. 만 14세 미만인지 알아야 하기 때문이다
- 만 14세 미만이면 **보호자 성함·연락처·동의**를 추가로 받아야 가입이 완료된다
  (개인정보보호법 §22조의2)
- **🚨 `users.birth_date` 의 NOT NULL 을 풀지 말 것.** 이 제약은 나이 제한용이 아니라
  **보호자 동의 분기를 태우기 위한 것**이다. 풀면 그 분기가 통째로 죽는다.
  소셜 로그인을 붙일 때도 추가 정보 화면에서 생년월일을 받아 **같은 블록을 재사용**한다
- **작업 중** — 지금은 보호자 번호를 받아 저장만 하고 문자를 보내지 않는다.
  **동의 요청 문자 + 인증번호 확인**을 붙이는 중이다(§6-⑤)

---

## 5. 정보 구조 (IA)

> **번호는 노션 IA(`1.1`~`6.11`)와 같은 것을 가리킨다.** 화면 목록의 원본은 노션이고
> 여기는 요약이다. **화면이 늘거나 줄면 노션을 먼저 고친다.**

- **시작 · 가입** `1.1` `1.2` `1.3`
  - 로그인 (→ 휴대폰·카카오·구글, §6⑥) / 생년월일·보호자 동의 / 부를 이름 정하기
- **홈** `2.1`
  - 앨범방 리스트 카드 → 방 만들기 `3.1`
  - 앨범방 메인 피드 `3.4` → 추억 올리기 `3.7` / 초대하기 `3.2` / 사진·영상 카드 리스트
  - 우측 서랍 `3.5` → 갤러리 `3.6` / 좋아요 모아보기 / 방 별명 / 숨김 피드
  - 콘텐츠 상세 `3.9` → 뷰어 `6.5` / 마음 남기기 / 댓글
- **감정 사서함** `2.2`
  - 받은 마음 → 리스트(날짜·구성원·**1:1·랜덤** 필터) → 상세 모달 플레이어
  - 보낸 마음 → 내가 보낸 이력
  - **마음 보내기** `4.1` → 받는 사람 고르기 `4.2` (**나에게 · 랜덤** · 방 전체 · 개별) — HEART-01
- **마이페이지** `2.3`
  - 내 정보 `5.1` / 알림 설정 / **차단 목록** — BLOCK-01
  - **회원 탈퇴** `5.2` — ACCOUNT-01
  - ⏸ 저장 용량 · 구독 관리 / 🗑 테마 설정 `6.10` 폐기(§6⑲ 아래 폐기 목록)

> **답장 미션 락 화면이 여기 있었는데 없앴다.** 문서와 코드 **양쪽에서 걷어냈다**(2026-09-06).
> → §4 MISSION-01

---

## 6. 최근 방향 조정 (2026-08 ~ 09)

> 이 절이 **"지금 무엇이 왜 바뀌었나"** 를 모아둔 곳이다. 결정의 상세 근거는
> `frontend/_workspace/14_decisions_2026-08-24.md`, 과정 해설은 `15_process_notes.md`.

**① UX 기준선: 시니어 → 카카오톡** (2026-08-20)
"시니어라서 안 된다"로 기능을 깎지 않는다. 부모 세대는 카카오톡을 잘 쓴다.

**② 🗑 몽실이 — 캐릭터와 작성 흐름 모두 폐기** (2026-09-06, 팀 회의 결정)
구름 캐릭터 '몽실이'와, 그를 길게 눌러 녹음하고 위로 던져 올리는 작성 흐름을
**둘 다 쓰지 않는다.** 앱에 실제로 붙어 있던 곳은 시작 화면(`/start`) 한 곳뿐이고,
작성 흐름은 시안(`13_mongsil_prototype.html`)에서 멈춰 있어 앱에 적용된 적이 없다.
**되돌릴 수 있게 기존 작성 화면을 남겨둔 판단이 여기서 값을 했다** — 걷어낼 것이
시작 화면 하나뿐이다.

> **다시 제안하지 않는다.** 시안 파일과 `_workspace/` 기록은 지우지 않고 남긴다
> (왜 그만뒀는지가 사라지면 같은 아이디어가 몇 달 뒤 다시 올라온다).

**③ 영상 최대 30초** (기존 2~3초)
비트윈처럼 **다운로드 없이 바로 재생되는 짧은 영상**을 목표로 한다.
2~3초는 너무 짧다는 판단. 답장 영상까지 전부 적용.
단, Supabase 무료 플랜의 **파일당 50MB** 벽이 있으므로 길이와 용량을 함께 검사한다.

**④ 알림 — 개정(2026-09-06): 웹푸시 → 앱 푸시(FCM)**
앱 밖에서 다시 불러올 통로가 없어서 웹푸시를 먼저 붙였다(코드는 완료, 실기기 검증 없음).
**웹을 폐기했으므로(§6⑮) 이제 FCM이 본체다** — 원래는 "추가"였지만 사실상 **교체**가 된다.
웹푸시 코드는 지우지 않고 둔다(개발용 `vercel.app` 에서 쓸 수 있다).
카카오 알림톡은 여전히 뒤로.

> **🚨 지금 이게 가장 급하다.** 답장 미션 락을 먼저 걷어내서
> **되돌아오게 할 장치가 비어 있다**(§4 MISSION-01). FCM이 그 자리를 메워야 한다.

**⑤ 만 14세 미만 — 막지 않고 보호자 문자로 동의받는다**
법은 14세 미만 가입을 **금지하지 않는다.** 법정대리인 동의를 받으라고 할 뿐이다.
가족 앨범방이라 **손주가 할머니에게 음성을 보내는 것**이 핵심 장면에 가깝다.
문자는 단순 인증번호가 아니라 **무엇에 동의하는지 적은 요청문**으로 보낸다.
Firebase Phone Auth는 쓰지 않는다(유료 + 용도 불일치 + 국내 발신번호 사전등록제).

**⑥ 소셜 로그인 — 보류에서 '예정'으로** (2026-09-06 개정)
**최종 로그인 방식은 휴대폰 · 카카오 · 구글로 확정했다.** 애플은 제거(iOS를 안 하므로 §6⑮).
지금 열려 있는 아이디/비밀번호는 **그때까지의 임시**다.

> **왜 올라왔나.** §2 개정과 한 몸이다. 부모를 막는 것이 신뢰라면,
> **처음 보는 앱에서 새 아이디와 비밀번호를 만들라는 요구**가 바로 그 의심을 키운다.
> 이미 쓰는 계정으로 들어가는 것이 설치보다 훨씬 중요한 신뢰 신호다.

**🚨 붙일 때 반드시 지킬 것 — 생년월일은 소셜이 대신해주지 않는다.**
어느 제공자도 나이 확인을 대신하지 않는다(카카오 공식 답변: "단순 편의 기능일 뿐,
법적 효력을 원하면 서비스가 직접 검증하라"). 그래서 가입을 **2단계**로 나눠
**추가 정보 화면에서 생년월일을 받고 AUTH-01의 보호자 동의 블록을 그대로 재사용한다.**
`users.birth_date` 의 NOT NULL 을 풀어서 우회하지 마라 —
2026-08-24에 실제로 그렇게 뚫었다가 전부 되돌린 일이 있다.


카카오 작업분은 보관해뒀다.

**⑦ GA4 계측 도입** — 포트폴리오 목적
이탈·병목·자주 쓰는 지점을 숫자로 보고 개선까지 잇는다.
**계측이 디자인 개편보다 먼저다**(개정 2026-09-06 — 원문은 "몽실이보다") — GA4는 소급
집계가 안 되므로 **브랜드를 갈아엎기 전 숫자**를 지금 확보해야 한다. 안 그러면 새 로고·새
색이 나아졌는지 영영 증명할 수 없다.
도구는 Firebase SDK가 아니라 `@next/third-parties/google`(웹에서 Firebase Analytics = GA4).
**DB는 Supabase 그대로 간다.**

**⑧ 🗑 검증은 웹으로, 앱은 그다음** — **2026-09-06 뒤집힘. §6⑮ 로 대체됐다.**

~~유저 테스트는 카톡 링크로 3초 안에 들어오는 웹이 압도적으로 유리하다.
앱은 설치 단계에서 테스터가 증발하고, 그 설치를 **부모가** 해야 한다.~~
앱화 경로는 재작성이 아니라 **Capacitor 래핑**이며 그때 바뀌는 것은 푸시와 햅틱뿐이다.
(마지막 문장은 살아 있다 — 그 경로로 간다.)

> **왜 뒤집혔나.** 이 판단의 근거는 **"부모에게 설치를 시키면 증발한다"** 하나였는데,
> §2에서 그 전제를 버렸다. 설치는 장벽이 아니고 **신뢰가 장벽**이다.
> 전제가 무너지자 웹을 고집할 이유가 남지 않았고, 반대로 **앱이라야 되는 것**이 많았다 —
> 위젯 · 손글씨 · 제대로 된 푸시 · 햅틱.

**⑨ 햅틱** — 앱 전환으로 열린다 (2026-09-06 개정)
웹에서는 `navigator.vibrate` 가 iOS Safari에서 안 되고 안드로이드 브라우저도 제각각이라
시각 반응으로 대신하고 있었다. **앱으로 가면 정상적으로 쓸 수 있다.**
아이폰은 어차피 만들지 않으므로(§6⑮) 안드로이드 햅틱만 보면 된다.

**⑩ 🗑 강조색 분홍 → 남보라, UI는 조용히** (2026-08-25 · 색 부분은 2026-09-06 폐기)
바탕·카드·그림자까지 전부 분홍으로 칠해 강조색이 배경에 묻혔고, 분홍 캐릭터(몽실이)와
UI가 같은 색이라 서로 밀어냈다. Airbnb 원본은 흰 바탕에 코랄을 **점으로** 쓰는데
우리는 **면으로** 칠한 것이 문제였다.

> **원칙(개정 2026-09-06) — UI는 조용히 있고, 색은 점으로만 쓴다.**
> 원래 문장은 "색과 성격은 **몽실이가** 낸다"였다. 캐릭터를 폐기했으므로
> **성격을 낼 주체가 비었다.** 이 빈자리를 무엇으로 채울지가 §6⑭의 과제다.
> Notion·Apple·Toss·당근·스테이폴리오의 DESIGN.md가 전부 같은 말을 하고 있었다.

~~강조색 `#5B4BE0` 은 로고의 보라 끝(`#a211c2`)에서 파랑 쪽으로 43도 당긴 값이다 —
로고와 이어지면서 몽실이의 분홍과는 부딪히지 않는다.~~ 흰 글자 대비 5.96:1(WCAG AA 통과).
토큰만 고쳐 219곳이 따라왔다. 브랜드 마크도 직접 그린 하트 SVG에서 **실제 로고 이미지**로
바꿨다(좋아요·즐겨찾기의 하트는 기능 아이콘이라 그대로 둔다).

> **🗑 남보라 `#5B4BE0` 폐기** (2026-09-06). 이 색을 고른 근거 자체가
> **① 기존 로고의 보라와 잇는다 ② 몽실이의 분홍과 안 부딪힌다** 두 가지였는데,
> **로고를 새로 뽑고 몽실이를 없애기로 하면서 두 근거가 모두 사라졌다.**
> 남는 것은 살아 있는 교훈 셋뿐이다 — **면이 아니라 점으로 쓴다 / 바탕은 중립으로
> 둔다 / 흰 글자 대비 4.5:1 이상(WCAG AA)을 지킨다.** 새 색도 이 셋을 지켜야 한다.

**⑪ 서체 Noto Sans KR → Pretendard, 굵기 900 → 700** (2026-08-25)
제목을 900으로 눌러 쓰던 화면이 **"공지·커머스"의 목소리**로 읽혔다. 다루는 감정이
쑥스러움·미안함·후회인데 소리를 지르고 있었다. 버튼만 600(토스 규율 17px/600).
dynamic-subset 92조각으로 자체 호스팅한다 — 화면에 실제로 나온 글자의 조각만 받는다.

**⑫ 사진은 선택 사항** (2026-08-25, 사용자 결정)
전에는 사진 1장 이상 + 음성 3초 이상을 **모두** 채워야 올릴 수 있었다.
목소리가 중심인 앱에서 사진을 강제하면 "지금 찍을 사진이 없어서" 마음을 못 남긴다.
DB에 사진 필수 제약이 없고 화면도 이미 0장을 처리하고 있어 서버 검사만 걷어냈다.

**⑬ 디자인 관문 — UI 작업의 필수 절차** (2026-08-26)
화면·컴포넌트 작업은 **기준 확인 → 방향 → 시안 비교 → 구현 → 마감 → 스크린샷 검증**
순서를 건너뛰지 않는다. 시안 HTML을 손으로 짜지 않고 **`design` 캔버스**(2026-09-06에
superdesign 에서 교체 — 크레딧이 들어 작업 도중 막혔다)에 올려
눈으로 비교해 고른다. 규칙 전문은 `frontend/CLAUDE.md`.
→ 건너뛰었다가 같은 화면을 세 번 고쳐 세 번 다 반려된 일이 있었다(§8 참조).

**⑭ 브랜드를 로고부터 다시 뽑는다** (2026-09-06, 팀 회의 결정)
몽실이(캐릭터·작성 흐름)와 남보라를 한꺼번에 걷어내기로 했다. **셋이 서로를 근거로
물려 있었기 때문에** 하나만 뺄 수 없었다 — 색은 캐릭터에 맞춰 골랐고, 시안 3안은
캐릭터가 있다는 전제로 뽑았고, "UI는 조용히"라는 원칙은 캐릭터가 성격을 낸다는
조건 위에 서 있었다.

> **그래서 지금 비어 있는 것은 로고가 아니라 "성격을 내는 주체"다.**
> UI를 조용히 두기로 한 결정은 유지한다. 그렇다면 무엇이 이 서비스의 목소리를 내는가 —
> 가장 유력한 후보는 **목소리·파형** 그 자체다. 앱의 핵심 행동이고, 이미 화면에 있고
> (`[UI-04]` 파형 재생), 한 줄 정의("쑥스러운 마음을 **목소리로** 남기는 곳")와 그대로 겹친다.

**작업 순서 — 위에서부터 닫는다. 건너뛰지 않는다.**

| 순서 | 무엇 | 정해지면 따라 정해지는 것 |
| :--- | :--- | :--- |
| 1 | 방향 잡기 (`frontend-design-direction`) | 톤앤매너 |
| 2 | 로고 시안 비교 (**`design`** 캔버스) | 브랜드 마크 |
| 3 | 로고 확정 → **강조색 추출** | 팔레트 |
| 4 | 토큰 교체 (`globals.css`) | 219곳이 따라온다 |
| 5 | 아이콘 한 벌·모서리·여백 (DESIGN-03) | 26개 화면 |

**바뀌지 않는 것** — 서체 Pretendard 700(§6⑪), UX 기준선 카카오톡(§6①),
디자인 관문 절차(§6⑬), WCAG AA 대비.

**⑮ 안드로이드 앱 하나로 간다. 웹 서비스는 폐기한다** (2026-09-06 결정)

**웹 서비스는 폐기한다. 안드로이드 앱 하나로 간다.**
한때 "무신사처럼 앱에 맞춘 보조 웹을 남기자"는 안을 검토했으나 **접었다**(2026-09-06).
근거는 사용자의 한마디였다 — **"카카오톡이 웹 서비스를 하나? 아니지."**
매일 쓰는 메신저형 서비스는 앱 하나로 간다. 웹을 같이 끌면 화면을 두 벌 맞춰야 하고,
**앱에서만 되는 것(위젯·햅틱·FCM·손글씨)을 웹이 못 하니 기능이 갈라진다.**

**래핑 방식 — Capacitor + 원격 URL로 확정한다(2026-09-06).**
Capacitor가 안드로이드 프로젝트를 만들고, 그 WebView가 배포된 Next.js 서버를 불러온다.

| 왜 이걸 골랐나 | |
| :--- | :--- |
| **서버 액션 18개를 안 건드린다** | 정적 전환(`output: 'export'`)은 서버 액션·미들웨어를 못 쓴다. 전부 클라이언트+API로 재작성하면 몇 주가 걸리고 **서버에서만 하던 검증이 무너지지 않는지 보안 재검토**까지 필요하다. 유저 테스트가 그만큼 밀린다 |
| **네이티브를 정식으로 다룬다** | 위젯(WIDGET-01)이 1급 기능이 됐다. 햅틱·FCM도 플러그인으로 붙는다. TWA에는 이 브릿지가 없다 |
| **스토어 반려 위험이 낮다** | "단순 웹뷰 래퍼" 정책은 **네이티브 기능이 없는 앱**을 겨눈다. 홈 위젯·푸시·햅틱이 있으면 해당하지 않는다 |
| **나중에 정적 전환도 가능** | Capacitor 안에서 점진적으로 옮길 수 있다. TWA에서는 갈아엎어야 한다 |

**감수하는 것 — 오프라인이 안 된다.** 다만 이 앱은 앨범방·사서함·음성이 전부 서버
데이터라 **오프라인에서 할 수 있는 일이 원래 거의 없다.** 실질 손실이 작다.

`vercel.app` 주소는 **앱이 불러올 서버**로 계속 살아 있다. 다만 사람에게
**웹 주소로 홍보하지 않는다** — 들어오는 길은 앱이다.

**왜 안드로이드만인가.** iOS도 개발 자체는 할 수 있다. 막는 것은 **테스트 비용**이다 —
실기기와 개발자 계정이 필요하고, **포트폴리오 목적에는 그 비용이 맞지 않는다**(사용자 판단).
기술적 한계가 아니라 **비용 판단**이라는 점을 적어둔다. 사정이 바뀌면 다시 볼 수 있다.

**앱으로 가면 열리는 것** — 위젯(스프린트 `S2`) · 햅틱(§6⑨) · FCM 푸시 · 손글씨.
**앱으로 가면 새로 생기는 숙제** — 설치 후 초대를 복원하는 **딥링크**(안 붙이면 부모가
앱을 열고 "그래서 뭘 하라는 거지"가 된다) · 플레이스토어 등록 · GA4 계측 재정비.

**⑯ 손글씨를 넣고, 음성 필수를 푼다** (2026-09-06 결정)

지금 마음을 남기려면 **음성 3초 이상이 유일한 필수**다(`compose-form.tsx` 의 `canSubmit`).
사진·영상·문구는 전부 선택인데 목소리만 강제다.

> **이 앱이 푸는 문제가 여기서 벽이 된다.** 문제 정의가 **"쑥스러움 때문에 표현을 못 하는
> 사람"**인데, 플로우는 **가장 쑥스러운 일(자기 목소리 녹음)을 가장 이른 시점에** 요구한다.
> 초대받은 부모가 앱에서 처음 하는 일이 그것이다.

스프린트도 이미 알고 있었다 — `Q6 부담 없이 첫 마음을 남기게 할 수 있을까?`,
`Q5 사진·영상 말고도 마음을 잘 전달할 수 있을까?`, 그리고 Can We? 의
*"음성녹음에 부담을 느끼는 사용자도 편하게 참여하게"*. **셋 다 답이 없던 자리를 손글씨가 메운다.**

**확정(2026-09-06)** — 상세는 §4 WRITE-01 · WRITE-02.

- **저장은 획 좌표로.** 나중에 못 바꾸는 결정이라 먼저 못 박았다 — 타임랩스가 여기 걸려 있다
- **앨범방 작성 화면에만** 넣는다. 배경(편지지) 고르기는 1차에서 뺀다
- **음성 필수 해제** — 음성 · 손글씨 · 사진/영상 중 **하나 이상**
- **타임랩스 재생(`S5`)은 손글씨 바로 다음.** 미루지 않는다 — 손글씨만 있고 재생이 없으면
  그냥 그림이고, 핵심 축 ③현장감은 여전히 빈 채로 남는다

> **이것이 폐기한 몽실이와 다른 점.** 몽실이는 팀 기획 문서 어디에도 없던,
> 구현 중에 나온 아이디어였다. 손글씨는 **스프린트가 투표로 뽑아 솔루션 스케치까지
> 그려놓고 IA로 넘어오지 못한 것**이다. 되살리는 것이지 새로 만드는 것이 아니다.

**⑰ 위젯을 1급으로 넣는다** (2026-09-06 결정)

**답장 미션 락을 없앤 자리를 위젯이 메운다.** 사용자 판단:
*"이게 다시 서비스로 돌아오게 하는 매개체가 되기 때문."* 상세는 §4 WIDGET-01.

한때 "위젯은 나중에, 지금은 푸시로"를 검토했으나 **접었다.** 이유 둘:

1. **푸시와 위젯은 성격이 다르다.** 위젯은 거기 있는 것(ambient), 푸시는 밀어내는 것
   (interrupt). 북극성이 "숙제가 아닌 즐거운 일과"인데 푸시는 숙제 쪽 감각이다.
2. **위젯은 서비스 정의의 3분의 1이다.** ③단계가 통째로 위젯이라, 빼면 다른 서비스가 된다.

이 결정 때문에 래핑 방식이 **Capacitor**로 굳었다(§6⑮) — 네이티브를 다뤄야 하기 때문이다.

**⑱ 매일 열 이유를 '질문'이 아니라 '쌓인 것'이 만든다** (2026-09-06 결정)

오늘의 질문은 **주 단위 고정을 유지한다** — 매일 바뀌면 "어제 답하려던 것"을 놓친다는
원래 이유가 여전히 유효하다. 대신 **매일 바뀌는 자리를 하나 더 만든다**(DAILY-01):
`작년 오늘` · `이번 달 12번 남겼어요` 처럼 **쌓인 것을 보여주는 자리**.
스케치에 이미 *"이번 달 12번 주고받았어요"* 가 있었다.

> **이 자리가 '혼자 쓰는 사람'도 받아준다.** "12번 주고받았어요"는 상대가 없으면
> 실패로 읽히지만 **"12번 남겼어요"는 혼자여도 성립한다**(§4 HEART-01과 같은 사상).

**⑲ 보관 정책 — 지우기 전까지 영구 보관** (2026-09-06 결정)

자동 삭제를 두지 않는다. 사용자가 지우기 전까지 사진·음성·영상을 계속 보관한다.

> **장기목표가 "사랑과 추억을 **가장 오래 보관**하는 디지털 감정 아카이브"다**(§1).
> 자동 삭제는 그 정의와 정면으로 부딪힌다. 가족이 주고받은 목소리가 시간이 지나
> 사라지는 것은 이 서비스에서 치명적이다.
>
> **원가는 시간이 아니라 용량으로 받는다** — 언젠가 상한을 두게 되더라도
> "오래돼서 지운다"가 아니라 "많이 써서 더 내거나 정리한다"로 간다.
> 개인정보 처리방침에도 이 문장으로 적는다.

**🗑 폐기 목록** — 테마 설정 4종(2026-09-06. 로고·색을 새로 뽑는 중에 그걸 갈아끼우는
기능을 만드는 건 서로를 깎는다. 26개 화면을 네 벌로 유지해야 하는 부담도 크다.
다크모드만 필요해지면 그때 따로 본다), 감정 태그, 관계 유형, 준회원 관문, 몽실이.

**⏸ 보류 목록** — 저장 용량·구독 모델(가입자 0명이라 방어할 원가가 없다. 결제를 붙이는
시간이 아깝고, 사용자가 쌓인 뒤에 정해도 늦지 않다), 카카오 알림톡, 첫 24시간 온보딩,
작성 경로를 하나로 합칠지(§7⑧ — 유저 테스트로 근거를 보고 정한다).
**"안 함"으로 끝난 것** — 댓글을 사서함에 자동 적재.

---

## 7. 열린 질문 · 미구현

**① ✅ 보관 정책 — 정해졌다(2026-09-06).** 지우기 전까지 영구 보관. 자동 삭제 없음.
→ §6⑲. 개인정보 처리방침 문구도 여기에 맞춰야 한다. **파일당 50MB 벽은 그대로 남아 있다** —
길이·용량 검사는 계속 필요하다(§4 ALBUM-02).

**② 보호자 인증 행 정리 — 아직 안 했다.** 가입을 끝내지 않고 버려진 행에도 보호자
번호가 남는다. 개인정보 최소보관 원칙상 **24시간 후 삭제**가 필요하다.
**이건 결정이 아니라 할 일이다** — 물어볼 것이 없고, 안 하면 개인정보를 필요 이상으로
들고 있는 상태가 계속된다. 앱 출시 전에 붙인다.

**③ 디자인 시스템이 아직 없다** — `design.md` 가 없고 토큰만 `globals.css` 에 있다.
그래서 아이콘을 화면마다 손으로 그렸고 지금 상태는 이렇다.

| | 실제 |
| :--- | :--- |
| 손으로 그린 SVG | **63개** |
| 획 굵기 | **9종** (1.7 / 1.8 / 1.9 / 2 / 2.2 / 2.4 / 3 / 3.2 / 3.4) |
| 아이콘 크기 | **8종** |
| 모서리 하드코딩 | **6종** (`rounded-[14px]` 만 23곳 — 이미 있는 `rounded-inner` 토큰과 중복) |
| 화면 수 | **26개** |

→ 하나의 브랜드로 안 읽히는 원인이 여기 있다. **개정(2026-09-06)** — 시안 3안은
몽실이 전제라 무효다. **로고를 새로 뽑아 강조색이 나온 뒤에** `ui/Icon.tsx` 로 아이콘을
한 벌로 묶고 모서리·여백 눈금을 정한다(§6⑭ 표의 3→5번).

**④ 웹푸시 런타임 검증** — 타입체크·빌드만 통과했다. 실제 기기에서 구독·수신까지
확인한 적이 **한 번도 없다.** 앱으로 가면 FCM이 추가되므로 **둘 다** 봐야 한다.

**⑤ GA4가 아무것도 안 쌓고 있다** — 이벤트 코드는 붙었지만 측정 ID가 없다.
남은 이벤트도 있다: `start_view` · `invite_open` · `invite_signup_complete` ·
`first_reply_done` · `room_view` · `push_permission` · `push_opened`.
**GA4는 소급 집계가 안 된다** — 개선 전 숫자를 지금 확보하지 않으면
"개선했다"를 영영 증명할 수 없다.

---

**⑥ 아이폰 사용자는 아예 못 쓴다** — 웹을 폐기하고 iOS도 만들지 않기로 했다(§6⑮).
**감수한 것이지 놓친 것이 아니다.** 다만 대가가 얼마인지는 아직 모른다 —
초대받은 사람이 아이폰이면 그 자리에서 끝난다. 2030 자녀는 아이폰 비율이 높은 편이라
**획득 쪽에서 먼저 걸릴 수 있다**(§2의 구조상 자녀가 먼저 들어온다).
유저 테스트 때 "아이폰이라 못 했다"가 몇 건 나오는지 세어두면 나중에 판단할 근거가 된다.

**⑦ ✅ 질문 주기 — 정해졌다(2026-09-06).** 질문은 주 단위 그대로 두고,
**매일 바뀌는 자리를 따로 만든다**(DAILY-01) — "작년 오늘" · "이번 달 N번 남겼어요".
→ §6⑱.

**⑧ 마음을 남기는 길이 두 벌이다** — `rooms/[id]/compose`(앨범방 피드)와
`mailbox/send`(사서함). 노션 IA에도 `3.7`과 `4.1`로 따로 있다.
**사용자는 마음이 생길 때마다 "이건 어디에 남기지?"를 먼저 판단해야 한다.**
카톡에는 이 갈림길이 없다. 북극성이 "즐거운 일과"인데 **일과의 첫 동작이 분류 작업**이면
즐겁기 어렵다. → 합칠 재료는 이미 있다. `heart-send.ts` 가 **방 전체·개별 멤버·나에게·랜덤**을
한 목록에서 고르게 해뒀다(HEART-01). 입구를 하나로 두고 받는 사람 고르는 자리에서
갈라도 되는지 확인해볼 것.

---

## 8. 패치 기록 (구현 변경 이력)

**커밋 하나에 한 줄씩 쌓는다.** 실제로 앱에 반영된 변경만 적는다 —
팀원이 이 문서만 보고도 "지금 앱이 PRD와 어디가 다른지"를 알 수 있게 하기 위해서다.

커밋 해시 대신 **커밋 제목**을 적는다. 해시는 커밋을 고쳐 쓰면 바뀌지만,
제목은 `git log --grep "제목 일부"` 로 바로 찾을 수 있다.

**🗑 표시가 붙은 줄은 나중에 폐기된 작업이다. 줄을 지우지는 않는다** — 무엇을 왜
그만뒀는지가 사라지면 같은 아이디어가 몇 달 뒤 그대로 다시 올라온다.

| 날짜 | 커밋 제목 | 무엇이 바뀌었나 | 왜 |
| :--- | :--- | :--- | :--- |
| 2026-08-19 | "실기기 필요"로 미뤄뒀던 3건 전부 브라우저에서 확인 | "실기기 필요"로 미뤄뒀던 3건(커버 크롭 핀치, 초대 연락처 대체 경로, 녹음 60초 자동 정지)을 브라우저 자동화로 전부 확인 | 확인 못 한 채 남겨두면 실제로 되는지 아무도 모른다 |
| 2026-08-19 | PRD에서 관계 유형 폐기 — 캡처 기준·구현과 일치시킴 | PRD에서 관계 유형(가족/연인/친구/나 자신) 폐기 | 캡처 기준·구현은 이미 안 묻고 있었다. 문서만 남아 어긋나 있었다 |
| 2026-08-20 | UX 기준선을 시니어에서 카카오톡으로 (사용자 개정) | 주 사용자를 2030으로, 부모(5060)를 **카카오톡을 무리 없이 쓰는 동반 사용자**로 다시 정의했다. 어르신(70+)은 대상이 아니다. 기본 글자 17px→16px, 큰 글자 토글은 접근성 선택으로 유지, 터치 44px는 그대로. 롱프레스·스와이프·아이콘 버튼 금지 규칙 해제. 기준 전문은 `frontend/_workspace/12_ux_baseline.md` | "요즘 부모님들도 카카오톡 잘 써서 그렇게 배려 안 해도 돼. 내가 조심했던 건 어르신들이었어"(사용자). 부모와 어르신을 하나로 묶은 것이 과했고, 그 한 문장이 코드 주석 55곳에서 글자 크기·제스처 금지까지 정당화하고 있었다 |
| 2026-08-20 | 댓글 수정 (노션 IA 3.9) | 댓글 수정 — 텍스트 댓글만, 작성자 본인만, "수정됨" 표시와 함께 | 가족이 주고받은 말이 아무 흔적 없이 바뀌면 안 된다. 음성은 고치는 것이 곧 갈아끼우는 것이라 막았다 |
| 2026-08-20 | 더보기 서랍 맨 위에 [함께하는 분] | 아바타 줄 + 인원수 + [초대]. 줄을 누르면 구성원 목록으로 간다 | 카톡 채팅방 서랍과 대조하니 이것만 빠져 있었다. 방에 누가 있는지는 서랍에서 가장 자주 보게 되는 정보인데, 그전에는 방 설정까지 들어가야 했다 |
| 2026-08-20 | 사서함 정리 모드 (노션 IA 2.2) | 사서함 정리 모드 — 고른 마음을 **내 화면에서만** 치운다(실제 삭제 아님) | 진짜로 지우면 상대의 보낸함에서도 사라진다. 그래서 문구도 "삭제"가 아니라 "치우기" |
| 2026-08-20 | 서비스 진단 보고서 + 작업 계획 | `frontend/_workspace/06_service_report.html` — 서비스 성질 분석, 유사 서비스(FamilyAlbum·Marco Polo·Storyworth·BeReal/Locket·효도폰) 비교, 잘하는 것/필요한 것/필요 없는 것, 7단계 작업 계획, 몽실이 아이디어 구현 가능성 판정 | 화면은 거의 다 찼는데 **연결(첫 24시간·앱 밖 알림·전화번호 로그인)이 비어 있다.** 무엇을 먼저 할지 정하려면 근거가 필요했다 |
| 2026-08-20 | 시작 화면 + 이름 짓기 (캡처 01~03) | `/start` 신설 — 카카오·휴대폰·Google·Apple 네 버튼을 캡처대로 두되 **[준비 중]** 표시를 붙였다(외부 등록 미완). 실제로 되는 [아이디로 시작하기]가 맨 위. 로그인 안 한 사람은 이제 `/login`이 아니라 여기로 온다. 가입 화면의 이름 칸은 "어떻게 불러드릴까요?"로 바뀌고 규칙 2줄 + [아무 이름이나 넣어주세요] 버튼이 붙었다 | 곧장 로그인 폼을 들이밀면 계정 없는 분이 막힌다. 소셜 버튼을 감추면 나중에 켤 때 화면을 다시 짜야 하고, 그냥 눌리게 두면 고장으로 읽힌다 |
| 2026-08-20 | 앨범방 이름 변경 (노션 IA 6.7) | 앨범방 이름 변경 기능 추가 (방장 전용, `rooms.name` 수정) | 노션 IA 6.7. **아래 항목에서 방식이 바뀌었다** |
| 2026-08-20 | 앨범방 이름·커버를 각자 자기 화면에서만 바꾸게 (노션 IA 6.7 개정) | **앨범방 이름·커버를 각자 자기 화면에서만 바꾸는 방식으로 전환.** 방장 전용 이름 변경(`632540f`)을 걷어내고, `room_members.custom_name / custom_cover_preset / custom_cover_path`에 사람별로 저장한다. 방 설정의 [앨범방 이름] 칸이 [내 화면에서 부를 이름]으로 바뀌었고 모든 구성원에게 보인다 | 카카오톡 단체방과 같은 방식(사용자 결정). 방장이 이름을 바꾸면 모두의 화면이 바뀌는데, 시니어 사용자에게는 "어제 있던 방이 없어진" 것으로 읽힌다 |
| 2026-08-20 | 앨범방 이름·커버를 각자 자기 화면에서만 바꾸게 (노션 IA 6.7 개정) | 보안: 구성원이 스스로 `room_members.role`을 admin으로 바꾸는 길을 트리거로 차단 | RLS 정책은 줄 단위라 컬럼을 가릴 수 없다. 내 별명을 고치는 통로로 방장 권한까지 가져갈 수 있었다 |
| 2026-08-20 | 전체화면 사진 뷰어 (노션 IA 6.5) | 상세·갤러리에서 사진을 누르면 전체화면으로 크게 본다. 자르지 않고(object-contain) 보여주고, 핀치·더블탭·[크게] 버튼 세 가지로 확대한다 | 피드의 사진은 자리에 맞춰 잘려 있어 원본을 볼 길이 없었다. 확대 버튼을 굳이 화면에 둔 이유는 핀치·더블탭에는 화면에 흔적이 없어서다(시니어) |
| 2026-08-20 | 찾기·갤러리를 카톡과 대조해 보완 | 앨범방 찾기에 **글자로 찾기** 추가 — 게시물 문구뿐 아니라 **댓글까지** 뒤진다. 갤러리는 **월별 머리글**로 묶었다 | 새 기준선(카카오톡)으로 다시 보니, 카톡 돋보기의 본체인 **키워드 검색이 통째로 빠져 있었다.** 노션 IA만 보고 만들어 사람·날짜 필터만 있었다. 갤러리도 카톡 사진 모아보기와 달리 날짜 구분이 없었다 |
| 2026-08-20 | 찾기를 **거르기에서 결과로 이동**으로 (카톡 방식) | 피드를 걸러 보여주던 것을 그만두고, 목록은 그대로 둔 채 걸린 게시물로 **데려간다.** 하단에 [N개 중 k번째] + ∧∨ 줄이 뜨고, 그 카드에 테두리가 붙는다. 찾는 동안에는 게시물을 200개까지 읽는다 | 사용자 결정 — "결과로 이동으로 ㄱㄱ, 걸러서 보기는 필요없어". 찾는 사람이 알고 싶은 건 "그 말이 어디 있었나"이지 "그 말이 든 목록"이 아니다. 걸러버리면 앞뒤 맥락이 사라진다 |
| 2026-08-20 | 초대장 미리 보기 (노션 IA 3.2) | 초대장을 만들기 전에 [받는 분에게 어떻게 보이는지 미리 보기]로 실제 화면을 그대로 본다. 실제 초대 화면과 **같은 부품(InviteLetter)** 을 쓴다 | 첫 마디는 한 번 보내면 못 고친다(링크를 새로 만드는 수밖에 없다). 미리보기를 따로 그리면 한쪽만 고쳐졌을 때 "미리 본 것과 다르게 가는" 일이 생긴다 |
| 2026-08-20 | 추억 고치기 — 사진·목소리까지 (노션 IA 3.8) | ⋯ 메뉴에 [사진·목소리 고치기] 추가. 작성 화면을 그대로 다시 열어 사진 추가·삭제·순서와 목소리 다시 녹음까지 된다. 기존 [수정]은 [문구 고치기]로 이름만 바꿔 남겼다 | 그전에는 문구만 고칠 수 있어서 사진을 잘못 고르면 지우고 다시 올려야 했다. 그러면 **거기 달린 댓글과 좋아요가 함께 사라진다** |
| 2026-08-20 | 프로필 사진 확대 (노션 IA 6.4) | 사서함 카드와 내 정보 화면의 프로필 사진을 누르면 전체화면으로 크게 본다. 사진이 없으면(기본 하트) 누를 수 없다 | 44px 동그라미로는 얼굴을 알아보기 어렵다. 확대 화면은 사진 뷰어를 그대로 쓴다 — 확대·닫기 규칙을 두 벌로 만들지 않는다 |
| 2026-08-20 | 피드 맨 아래로 이동 버튼 (노션 IA 3.4) | 피드에 떠 있는 [맨 아래로] 버튼. 맨 아래에 닿으면 같은 자리에서 [맨 위로]로 바뀐다. 스크롤할 것이 적으면 뜨지 않는다 | IA는 최하단 이동만 요구했지만 한 방향만 두면 내려간 사람이 손으로 다 밀어 올려야 한다 |
| 2026-08-20 | 피드 찾아보기 + 달력 (노션 IA 3.4 · 6.8) | 앨범방 머리띠의 돋보기 → 작성자·날짜로 좁혀 보기. 조건은 주소(`?who=&on=&find=1`)에 실리고 **거르는 일은 서버가 한다**. 날짜는 달력 모달에서 고르고 KST 하루 기준으로 자른다 | 화면이 30개를 받아놓고 스스로 거르면 목록이 짧아지고 실제 데이터와 어긋난다. `<input type="date">`를 쓰지 않은 이유는 기기마다 생김새가 달라 시니어에게 불확실해서다 |
| 2026-08-24 | 🗑 몽실이 시안 — 검은 알림 10곳 → 2곳 | 진행 단계마다 뜨던 검은 알림을 걷어냈다. **화면에 흔적이 없을 때**와 **단추의 말과 실제 동작이 다를 때**만 남겼다(빈 몽실이 던지기 · [다시 녹음]). 규칙은 `12_ux_baseline.md`에 적었다 | 담긴 것은 몽실이 몸에, 한마디는 카드에 이미 보인다. 보이는 것을 또 말하면 잔소리가 되고, 정작 필요한 안내까지 흘려보게 된다(사용자 지적) |
| 2026-08-24 | 🗑 몽실이 시안 — 던진 뒤 문구 달기 + 첫 실행 안내 | 시안에 두 가지를 더했다. (1) **문구는 올린 뒤에 단다** — 던져서 올라간 다음 "한마디 덧붙일까요?"를 묻고, 건너뛰어도 이미 올라가 있다. (2) **첫 실행 안내** — 톡·꾹·던지기 세 줄을 한 번만 보여주고 닫으면 몽실이가 튕겨 보인다 | 올리기 **전에** 문구를 물으면 "쓸 말이 없어서" 던지기를 망설이게 된다. 안내는 카톡에 없는 동작이라 한 번은 말로 알려야 하지만, 상시 버튼으로 두면 화면이 지저분해진다 |
| 2026-08-24 | 🗑 몽실이 시안 — 판이 겹쳐 뜨던 버그 수정 | 사진을 올린 뒤 문구 판이 떠 있을 때 몽실이 **바깥을 누르고** 다시 사진을 고르면, 수정창 위에 "한마디 덧붙일까요?"가 함께 떴다. 판을 갈아 끼우는 곳을 `setPanel()` 하나로 모았다 | 갤러리→수정창 자리에서만 판을 손으로 껐다 켜고 있었고, 문구 판이 나중에 생기면서 그곳에서 끄는 것을 빠뜨렸다. 목소리 쪽은 공통 함수를 타서 멀쩡했다 |
| 2026-08-24 | 🗑 몽실이 시안 — 한마디 판 다듬기 + 시트가 흰 판 자리로 | (1) 빈 칸이면 [달기]가 잠긴다. 건너뛰기·바깥 누르기는 아무 말도 하지 않는다. (2) 고르기 시트가 줄어들 때 멈추는 자리를 **흰 판(.pouch)의 자리 그대로**로 맞췄다 — 자리·크기 1px 이내, 모서리도 14px로 같이 간다 | (1) 쓴 게 없는데 "올렸어요"를 세 번 말하면 잔소리고, 빈 채로 "달기"가 눌리면 말과 행동이 어긋난다. (2) 전에는 구름의 바깥 상자에 맞춰 세워서 시트가 멈춘 곳과 흰 판이 어긋났고, 갈아타는 순간 내용이 한 번 튀었다 |
| 2026-08-24 | 🗑 몽실이 작성 흐름 시안 (UX 개편 0단계) | `frontend/_workspace/13_mongsil_prototype.html` — 몽실이로 마음을 올리는 새 작성 흐름의 **감촉 확인용 시안**(앱에는 아직 안 붙였다). 동작을 톡(사진)·꾹(녹음)·던지기(올리기) 셋으로 가르고, 녹음 확인·자르기, 담은 것 순서 바꾸기, 바깥 톡으로 한 단계 되돌리기를 담았다. 정해진 규칙은 `12_ux_baseline.md`에 남겼다 | 보고서 0단계는 "UX 개편 범위를 먼저 못 박는다"였는데, **던지기가 실제로 기분 좋은지는 글로 판단할 수 없다.** 버릴 각오로 짧게 만들어 먼저 만져 보기로 했다(사용자 결정) |
| 2026-08-25 | 🗑 [DESIGN-01] 강조색 분홍→남보라, 로고 교체, 시작 화면 리디자인 (2026-09-06 전면 재작업 — §6⑭) | 강조색 `#d50e68` → **`#5b4be0`(남보라)**. 분홍 바탕·면·선·그림자를 중립(`#f7f8fa` `#f2f4f6` `#e5e8eb`)으로. 브랜드 마크를 직접 그린 하트 SVG → **실제 로고 이미지**(BrandMark). 몽실이를 컴포넌트로 분리(Mongsil). 시작 화면은 몽실이를 가운데로, 준비 중 버튼 3개는 [다른 방법으로 시작하기]에 접었다. 문구에서 '가족'을 뺐다 | 바탕까지 전부 분홍이라 강조색이 배경에 묻혔고 분홍 캐릭터와 UI가 서로 밀어냈다. Airbnb 원본은 코랄을 **점으로** 쓰는데 우리는 **면으로** 칠했다. '가족'을 뺀 것은 연인·나 자신에게도 쓰는 서비스라서 |
| 2026-08-25 | [SIGNUP-01] 가입 폼 규칙을 살아 있는 체크 목록으로 + GA4 계측 | 입력 규칙을 낱개로 쪼개 지킨 것에 색이 살아나고 체크가 켜지게 했다(RuleList). 상태는 **비활성(회색)/활성(강조색+체크) 둘뿐** — 못 지킨 것을 빨갛게 만들지 않는다. 함께: `@/lib/analytics` 한 곳에서 이벤트 이름을 정의, 작성·가입 퍼널 계측 | "2~10자로 적어주세요"는 읽고 나면 끝이라 지금 내가 지켰는지 **직접 세어봐야** 알 수 있었다. 두 글자 쳤을 때부터 빨간 글씨면 적는 중인 사람을 혼내는 꼴이 된다 |
| 2026-08-25 | [SIGNUP-02] 규칙을 입력칸 아래 한 줄로, 이름 옆에 [랜덤] 버튼 | 규칙 체크 목록을 입력칸 위 → **아래**로, 세로 쌓기 → **한 줄**로. "아무 이름이나 넣어주세요"(밑줄 링크) → 입력칸 오른쪽의 **[랜덤] 버튼**. 약관의 "(새 창에서 열려요)" 제거 | 치면서 바로 아래에 불이 들어와야 눈이 위아래로 안 흔들린다. 조건이 두어 개뿐이라 세로로 쌓으면 입력칸과 규칙이 한 덩어리로 안 읽혔다 |
| 2026-08-25 | [SIGNUP-03] 아이디 중복확인 + 설명 문구 정리 | **[중복확인] 버튼** 추가. 가입 화면은 로그인 전(anon)이라 users를 조회할 길이 없어, **있다/없다 하나만** 돌려주는 함수를 열었다(마이그레이션 `username_taken_lookup`). 아이디를 한 글자라도 고치면 결과를 즉시 버린다 | ⚠️ **감수한 것**: 누구나 "이 아이디가 존재하는가"를 물어볼 수 있다. 한국 가입 폼의 관례이고 아이디만으로는 로그인이 안 되므로 받아들였다(사용자 승인). **이건 중복을 막는 장치가 아니다** — 진짜 방어선은 `users.username` 의 unique 제약이다 |
| 2026-08-25 | [UI-01] 생년월일 휠에 드럼 굴림감 | 가운데에서 멀어질수록 눕고, 작아지고, 흐려진다. `animation-timeline: view()` 로 브라우저가 진행도를 직접 재서 합성 단계에서 그린다. 못 받는 브라우저에서는 평평하게 보인다 | 전에는 가운데 줄만 진하고 나머지가 전부 똑같이 흐려서, 굴러가는 드럼이 아니라 평평한 목록으로 읽혔다. 스크롤 위치를 JS로 매 프레임 재면 손끝을 못 따라온다 |
| 2026-08-25 | [DESIGN-02] 글씨체 Noto Sans KR → Pretendard, 제목 900→700 · 버튼 800→600 | `font-black(900)`·`font-extrabold(800)` → **`font-bold(700)`** 전면 교체(60곳), 버튼만 **600**. dynamic-subset 92조각으로 자체 호스팅 | 제목을 900으로 눌러 쓴 화면이 **"공지·커머스"의 목소리**로 읽혔다. 다루는 감정이 쑥스러움·미안함·후회인데 소리를 지르고 있었다. 토스 전용 폰트와 애플 SF Pro는 라이선스로 못 쓴다 |
| 2026-08-25 | [PUSH-01] 웹푸시 — 앱 밖 알림 | 구독 등록·해지, 설정의 알림 줄, 새 추억을 남기면 같은 방 사람들에게 알림. VAPID 키가 없으면 **조용히 건너뛴다**. `sw.js` 는 push·notificationclick 만 듣고 **fetch를 가로채지 않는다**. 함께: 홈 화면 아이콘이 아직 옛 분홍 하트여서 실제 로고로, manifest 색도 새 팔레트로 | 답장 미션·연속 기록이 "다시 들어오라"고 말할 통로가 없었다. 캐싱하는 서비스워커는 배포 뒤 낡은 화면을 붙잡고 놓지 않는다. ⚠️ **런타임 검증은 못 했다** — 타입체크·빌드만 통과 |
| 2026-08-25 | [PROMPT-01] 오늘의 질문을 주 단위로 고정 + 하네스 라우팅 정리 | ISO 주차(KST) 기준으로 **이번 주의 시작 질문**이 정해진다. [다른 질문 보기]는 이 화면 안에서만 한 칸 돌리고, 다시 들어오면 이번 주 질문으로 돌아온다. 질문을 접은 채 보내면 `promptUsed` 를 null 로 남긴다 | 매일 바뀌면 "어제 그 질문에 답하려던 것"을 놓친다. 넘긴 것이 영구히 남으면 주 단위로 고정한 뜻이 사라진다 |
| 2026-08-25 | [UI-02] 프로필 기본 그림: 하트 → 이름 첫 글자 | 사진이 없을 때 브랜드 하트 대신 **이름 첫 글자**. `nameInitial()` 로 빼서 부품들이 같은 규칙을 쓴다. 이름을 못 읽으면 물음표가 아니라 가운뎃점. 이모지는 코드 포인트 단위로 자른다 | 하트는 로고도 기능 아이콘도 아닌 어중간한 자리였고, 무엇보다 **사람이 여럿 있는 화면에서 모두가 같은 하트**라 누가 누군지 구분이 안 됐다. 물음표는 오류로 보인다 |
| 2026-08-25 | [UI-03] 방 빈 화면을 카드로, "문구 선택" → "문구 (선택)" | 방 안의 빈 화면을 홈과 같은 공용 EmptyState(카드)로. 라벨을 **문구 (선택)** 로 | 홈은 같은 상황을 카드에 담는데 방 안만 맨 글씨라, 같은 앱에서 같은 상황이 다르게 보였다. "문구 선택"은 **"문구를 고르세요"라는 명령**으로 읽혔는데 실제로 고를 목록도 없다 |
| 2026-08-25 | [COMPOSE-01] 사진을 선택 사항으로, 규칙을 체크 목록으로 | **사진 1장 이상 규칙을 없앴다**(사용자 결정). DB에 제약이 없고 화면도 0장을 이미 처리해서 서버 검사 두 곳만 걷어냈다(마이그레이션 없음). ✕/✓ 배지를 RuleList로 — 작성·사서함·댓글 세 화면이 함께 좋아진다 | 목소리가 중심인 앱에서 사진을 강제하면 "지금 찍을 사진이 없어서" 마음을 못 남긴다. 전에는 **아직 녹음을 시작도 안 했는데 ✕가 붙어** 실패한 것처럼 보였다. ⚠️ 실제 녹음 끝단은 확인 못 함(자동화로 마이크 사용 불가) |
| 2026-08-26 | [UI-04] 파형 재생을 부드럽게 + 문지르기, ~~시안 몽실이를 새 모양으로~~ (🗑 뒷부분만 폐기 — 파형은 살아 있다) | 재생 중에는 **매 프레임(rAF)** `currentTime` 을 다시 읽는다. 파형을 누르면 그 자리로 가고 끌면 따라온다(애플 음성 메모처럼). `role` 을 progressbar → **slider** 로, `touch-action: pan-y`. 시안 몽실이를 뭉치 4개 → **솜사탕(뭉치 16개 + 얼굴)** 으로 | `<audio>` 의 `timeupdate` 는 **1초에 네 번쯤만** 온다(브라우저가 정하는 값) — 소리는 이어지는데 그림만 250ms마다 튀었다. progressbar 는 "보기만 하는 값"이라는 뜻이라 낭독기가 조작법을 알려주지 않는다 |
| 2026-08-26 | 🗑 [MONGSIL-01] 큰 몽실이도 솜사탕으로, 파형 눌러 그 자리부터 재생 | 큰 몽실이를 **JS가 실제 픽셀 크기로 다시 그린다** — 전에는 120×84 그림틀을 344×240으로 늘려 써서 동그라미가 타원으로 찌그러졌다. 담고 있을 때도 얼굴을 보이되 담은 것을 배 쪽으로 내렸다. 문구: 다시 녹음→**재녹음**, 들어보기→**재생**. "들어보고, 필요하면 잘라내세요" 제거 | 찌그러지면 솜사탕이 아니라 눌린 덩어리로 보인다. 담을 때 얼굴을 숨기면 "얘가 걔 맞나" 싶어진다. 손잡이와 파형이 이미 자를 수 있게 생겼으므로 말로 또 설명할 필요가 없다 |
| 2026-08-26 | [FIX] 마음 보내기 — 고른 사람의 ✕ 단추 윗부분이 잘리던 것 | ✕의 **보이는 동그라미를 칸 안쪽으로** 당기고 넓은 누름판(44px)만 밖으로 넘겼다. 줄의 위 여백도 넉넉히 | **CSS 규칙상 가로가 `auto` 면 세로 `visible` 이 `auto` 로 바뀐다** — 즉 세로도 잘린다. ✕는 칸 위쪽 밖에 나가 있었고 위로 넘친 것은 스크롤로 닿을 수도 없었다(사용자 신고). 같은 함정을 몽실이 시안에서 겪고 주석까지 남겨뒀는데 실제 앱에는 반영되지 않은 채였다 |
| 2026-09-06 | [CLEAN] 남보라·답장 미션 락·몽실이 컴포넌트를 코드에서 걷어냄 | ① **강조색**: `globals.css` 토큰 네 줄을 남보라(`#5b4be0`)에서 **임시 중성 회색**(`#3a3a3c` 계열)으로. 219곳이 따라왔다 ② **답장 미션 락**: `lib/mission.ts` 삭제, `actions/mailbox.ts` 의 락 계산 제거, 사서함의 `LockedHeart` 제거, `MailboxItem` 의 `locked`/`unrepliedCount` 제거. **읽음 표시(`markHeartRead`)와 DB 함수 `locked_senders()` 는 남겼다** ③ **몽실이**: `components/brand/Mongsil.tsx` 삭제, `/start` 에서 제거(말풍선 꼬리도 함께). `_workspace/` 의 시안 파일은 남겼다. 타입체크·빌드 통과 | 셋 다 사용자 지시. 락은 원래 "앱 푸시·위젯 확인 후에 뺀다"였으나 먼저 빼기로 했다 — **되돌아오게 할 장치가 비는 구간을 알고 지나간다**(§4). 색은 어중간한 임시색을 채우면 굳어버리므로 **비어 있는 상태를 보이게** 뒀다. `/start` 는 몽실이가 있던 자리가 **비어 있다** — 새 로고가 나오면 디자인 관문을 밟아 다시 짠다 |
| 2026-08-26 | [RULE] UI 작업에서 디자인 관문을 건너뛰지 않는다 | `frontend/CLAUDE.md` 맨 위에 규칙을 못 박았다 — 화면 작업은 방향(`frontend-design-direction`) → 시안 비교(`superdesign`) → 구현(`frontend-design`)을 건너뛰지 않고, **시안 HTML을 손으로 짜지 않는다** | 시작 화면 리디자인에서 이 셋을 전부 건너뛰고 시안을 손으로 짰다. 큰 몽실이를 세 번 고쳐 **세 번 다 반려**됐고 결국 처음으로 돌아가 루틴을 다시 밟았다 — **건너뛴 것이 시간을 아낀 게 아니라 두 배로 썼다** |
| 2026-09-06 | [DB] 스키마를 저장소로 내렸다 — 원본이 클라우드 한 곳뿐이었다 | `frontend/supabase/schema/` 에 실행 순서대로 7개 파일 — 테이블 20 · 열거형 7 · 인덱스 37 · 함수 28 · 트리거 11 · public 정책 56 · storage 정책 7 · 버킷 4. 개수를 DB 실제와 하나씩 대조해 전부 일치 | **저장소에 마이그레이션 SQL이 한 줄도 없었다.** 스무 건 넘는 변경이 전부 운영 DB에 직접 적용됐고 이력 테이블조차 없어, 스키마의 유일한 원본이 라이브 DB 하나였다. 그날 그 프로젝트가 무료 플랜 미사용으로 **일시정지돼 앱의 로그인·가입이 죽어 있었다** — 그때 스키마까지 잃었다면 되살릴 방법이 없었다 |
| 2026-09-06 | [DB] 보호자 인증 행 24시간 자동 정리 (pg_cron) | `pg_cron` 설치 + 매일 03:00 KST 에 24시간 지난 행 삭제. 규칙은 `purge_guardian_verifications()` 함수로 빼고 anon·authenticated 실행 권한은 회수했다. `created_at` 기준이라 **인증을 시도만 하고 버려진 행까지** 지워진다 | §7② 의 숙제이자 개인정보 최소보관 원칙. 결정이 아니라 할 일이었다. 검증: 25시간 전 행과 1시간 전 행을 넣고 돌려 앞의 것만 지워지는 것을 확인 |
| 2026-09-06 | [FIX] 탈퇴 시 파일 영구 소실 + 조용히 실패하던 동작 4곳 | ① 탈퇴에서 **파일 삭제가 `withdraw_account` RPC 앞에서** 돌고 있었다 → 목록 수집(RPC 앞)과 실제 삭제(RPC 성공 뒤)를 갈랐다 ② `hideHeartMessages` · `toggleMemoryLike` · `toggleRoomFavorite` · `deleteNotifications` 가 `Promise<void>` 라 실패해도 성공처럼 끝났다 → `lib/action-result.ts` 를 두고 결과를 돌려주게 했다 | ①은 RPC가 실패하면 프로필 사진과 목소리가 이미 사라졌는데 화면은 "계정은 그대로 있습니다"라고 안내했다 — 되돌릴 수 없는 손실이고, 2026-08-10 에 실제로 났던 사고다 ②는 사서함 치우기가 실패해도 편집 모드를 닫고 고른 것을 비워 **치워진 것처럼** 보였다 |
| 2026-09-06 | [정리] 죽은 파일 정리 — 앱 전환 전에 바닥을 비운다 | 저장소 1.7G → 625M. 캐시(`.next` 1.1G 등)와 참조 0건인 파일(`ia.pdf` · `code_review_report.md` · `airbnb.md` · `tool.md` · next 기본 svg 5개), 폐기된 몽실이 시안(`_workspace/mock/` 전체 + 프로토타입)과 옛 하네스 스킬·에이전트 5개를 지웠다 | 큰 변경 전에 바닥을 비운다. **함정**: `.next` 를 지우면 Next.js 가 만드는 전역 타입(`PageProps`·`LayoutProps`)이 함께 사라져 타입 검사가 18곳에서 깨진다 — `verify.sh` 는 타입 검사를 빌드보다 먼저 돌리므로 캐시를 비운 뒤에는 `build` 를 한 번 돌리고 verify 해야 한다 |
| 2026-09-06 | [APP-01] 안드로이드 앱 껍데기 — Capacitor + 원격 URL | 패키지명 `app.oneuldo.android`, 앱 이름 "오늘도 사랑해". `capacitor.config.ts` 의 `server.url` 이 vercel 주소를 부른다. `src/` 는 한 줄도 안 고쳤다 | §6⑮ 확정안 그대로. **에뮬레이터(API 35)에서 실제로 확인** — 앱이 뜨고 `/start` 가 보이고 `/signup` 으로 이동하며, 아이디 [중복확인] 이 "쓸 수 있는 아이디예요"를 돌려줬다(서버 액션 → Supabase RPC 왕복). 이 방식의 가장 큰 위험이 해소됐다. **함정**: Android Studio 내장 JDK 25 는 Gradle 8.14.3 이 못 읽는다(major version 69) — Homebrew `openjdk@21` 로 빌드해야 한다 |
| 2026-09-06 | [DESIGN-01] 로고 확정 + 강조색 선명한 번트 #BF3F0D + design.md 신설 | 로고: Recraft(V4.1 Vector)에서 뽑은 "봉투에서 나오는 하트"를 원본 픽셀 기준으로 벡터화(일치율 98.6%) — `public/brand/logo.svg`·`mark.svg`, Play 512, 안드로이드 런처 mipmap 5종 + 적응형 3레이어(배경색·전경·모노크롬 벡터). 봉투 앞면은 밝기 −1.5 두 톤, 하트·플랩은 크림 면. `BrandMark.tsx` 를 PNG img → 인라인 SVG. `globals.css` 강조색 4줄(+버튼 그림자 rgb, 브랜드 토큰)과 themeColor·manifest 교체. 루트 `design.md` 신설 | 색 후보 셋 중 **토큰 4줄이 전부 AA를 통과하는 유일한 색**이 선명한 번트였다(밝은 번트는 primary-light 3.98:1 미달, 붉은 번트는 에러 빨강과 겹침). Recraft 원본 #C17753 은 3.48:1 미달이라 그대로 못 썼다. **함정 셋**: ① Recraft 로고의 하트·플랩은 흰색이 아니라 뚫린 투명 — 어두운 배경에서 하트가 사라져 면으로 채웠다 ② 모서리는 원이 아니라 스쿼클이라 실측 프로파일로 그렸다 ③ 시안 도구(superdesign)에 명사("파형")를 주면 그 명사의 관습 아이콘이 나온다 — 3안 전부 반려, 조형은 Recraft·검증과 코드화는 이쪽으로 역할을 갈랐다 |
| 2026-09-06 | [APP-01] 안드로이드 앱 껍데기 — Capacitor + 원격 URL | 패키지 `app.oneuldo.android`, 앱 이름 "오늘도 사랑해". `capacitor.config.ts` 의 `server.url` 이 vercel 주소를 부르고 `src/` 는 한 줄도 안 고쳤다 | §6⑮ 확정안 그대로. 정적 전환은 서버 액션 47개와 미들웨어를 못 쓴다. **에뮬레이터(API 35)에서 확인** — 앱이 뜨고 `/start` → `/signup` 이동, 아이디 [중복확인] 이 서버 액션 → Supabase RPC 왕복으로 "쓸 수 있는 아이디예요"를 돌려줬다. **함정**: Android Studio 내장 JDK 25 는 Gradle 8.14.3 이 못 읽는다 — Homebrew `openjdk@21` 로 빌드할 것 |
| 2026-09-06 | [WIDGET-01·APP-01] 홈 위젯 네이티브 + 딥링크 + 웹 폐기 라우팅 + DB | ① `widget_tokens` 테이블과 함수 4개(발급·회수·조회·톡톡), `notification_type` 에 `knock`, `memories` 에 손글씨 컬럼 2개 + `handwriting` 버킷 ② 위젯 네이티브(4×2, 30분 주기, 캐시 폴백, [톡톡] 단추) ③ App Links + Install Referrer 딥링크 ④ 브라우저는 `/welcome` 으로, 초대장은 [앱에서 열기] → 스토어 | 위젯은 웹뷰 밖에 살아 로그인 세션을 못 본다 → 전용 토큰. **안드로이드 위젯은 길게 누르기를 앱이 못 받는다**(시스템이 이동·크기조절로 가져간다) → PRD 의 "꾹 누르면"을 단추로 바꿨다(사용자 결정). 로그인 관문의 공개 경로에 `manifest.json`·`sw.js` 를 더했다 — **원래부터 로그아웃 상태에서 `/start` 로 튕기고 있었다** |
| 2026-09-06 | [WIDGET-01·DAILY-01] 위젯 API·토큰 동기화 + 매일 바뀌는 자리 | `GET /api/widget/latest` · `POST /api/widget/knock`(Bearer 토큰, 검사는 DB 함수). 로그인하면 토큰을 발급해 기기에 저장하고 로그아웃하면 지운다(WidgetTokenSync + signOut). 홈 맨 위에 `작년 오늘` / `이번 달 N번 남겼어요`(추억+마음, KST 기준) | **service_role 의 유일한 예외**(사용자 승인) — 위젯 사진 서명 한 곳. 비공개 버킷 서명은 Storage API 의 일이라 DB 함수가 못 만들고 위젯에는 세션이 없다. DB 가 "이 토큰이 봐도 되는 사진"이라고 답한 경로에만 도장을 찍는다. 검증: 토큰 없음 401 · 무효 401 · 유효 200 · 자기 자신 톡톡 403 |
| 2026-09-07 | [WRITE-01·02] 손글씨 + 타임랩스 재생 + 음성 필수 해제 | 획 좌표(`{v,w:800,h:600,strokes:[{t,p:[[x,y,dt]…]}],durationMs}`)를 `handwriting` 버킷 JSON 으로. 쓰는 판(검정 한 색·굵기 고정·4:3), 재생기(멈춤 300ms 로 자르고 최대 4초 압축, 화면에 들어올 때 받음, 그리는 동안 canvas·끝나면 SVG). `canSubmit` 이 "음성 3초"에서 **목소리·손글씨·사진 중 하나 이상**으로 | §6⑯. 문제 정의가 "쑥스러워서 표현을 못 하는 사람"인데 가장 쑥스러운 일(자기 목소리 녹음)을 가장 먼저 시키고 있었다. 저장을 획 좌표로 한 것은 타임랩스가 여기 걸려 있어서다(못 바꾸는 결정). **DB 구멍 발견·수정**: 손글씨 쌍 제약이 `NULL >= 0` 때문에 경로만 있는 행을 통과시켰다(schema/11). ⚠️ `memories_voice_pair` 도 같은 모양이라 같은 구멍이 있다 |

---

## 9. 문서 수정 이력 (기획 문구가 바뀐 기록)

| 수정 날짜 | 수정한 사람 | 수정한 내용 요약 |
| :--- | :--- | :--- |
| 2026-07-10 | 기획 담당자 | 최초 작성 (MVP 기능 범위 확정) |
| 2026-08-19 | 기획 담당자 | [MSG-01] 음성 메시지 최대 길이 30초 → 60초로 확정 (구현·DB가 이미 60초로 동작 중이었고 그쪽에 맞춤) |
| 2026-08-19 | 기획 담당자 | [ALBUM-01] 관계 유형(가족/연인/친구/반려동물/나 자신) 선택 폐기. 방을 만들 때 관계를 규정짓게 하지 않고 개방적으로 간다 |
| 2026-08-20 | 기획 담당자 | 타깃·UX 기준선 개정 — 부모를 "디지털이 서툰 시니어"로 규정하던 문장을 걷어내고 기준선을 카카오톡으로 바꿈 |
| 2026-08-25 | 기획 담당자 | [INVITE-02] **'준회원 → 구성원' 관문 폐기.** 가입하면 바로 구성원이 된다. 관문을 통과해야 하는 쪽이 활성화가 일어나야 할 부모라 이탈이 가장 크게 나는 자리였고, 답장은 답장 미션이 이미 유도하고 있어 이중이었다 |
| 2026-09-02 | 기획 담당자 | **§0 지금 상태를 2026-09-02로 갱신**(강조색·서체 반영, "지금 막고 있는 것" 표 신설). §3에 DESIGN-01 · DESIGN-03 · SIGNUP-01~03 추가하고 PUSH-01 · PROMPT-01의 "미커밋" 표시 해제. §6에 ⑩~⑬(남보라 전환 / Pretendard / 사진 선택화 / 디자인 관문) 추가. §7에 ③디자인 시스템 부재 ④웹푸시 미검증 ⑤GA4 미수집 추가. §8에 2026-08-25~26 패치 15건 기록 |
| 2026-09-06 | 기획 담당자 | **남아 있던 결정을 전부 닫았다.** 열 가지를 한 번에 정했다. ① **래핑 = Capacitor + 원격 URL** — 정적 전환은 서버 액션 18개 재작성 + 보안 재검토라 몇 주가 걸리고, 위젯이 1급이 되면서 네이티브 브릿지가 필요해졌다(§6⑮) ② **위젯을 처음부터 넣는다**(WIDGET-01, §6⑰) — 락을 없앤 자리를 메우는 것이고 **서비스 정의의 3분의 1**이다. 푸시는 밀어내는 것, 위젯은 거기 있는 것이라 대체가 안 된다 ③ **손글씨는 획 좌표로 저장**(WRITE-01) — 나중에 못 바꾸는 결정. 앨범방 작성 화면에만, 배경 고르기는 1차 제외 ④ **타임랩스는 손글씨 바로 다음**(WRITE-02) — 미루면 핵심 축 ③현장감이 계속 빈다 ⑤ **오늘의 질문은 주 단위 유지 + 매일 바뀌는 자리 신설**(DAILY-01, §6⑱) ⑥ **보관 정책 = 지우기 전까지 영구**(§6⑲) — 장기목표가 "가장 오래 보관하는 아카이브"라 자동 삭제와 정면 충돌한다. 원가는 시간이 아니라 용량으로 받는다 ⑦ **테마 4종 폐기** — 로고·색을 새로 뽑는 중에 그걸 갈아끼우는 기능은 서로를 깎는다 ⑧ **앨범방 개수 제한 없음을 정식 정책으로** — "구독 보류라 임시로 풀림"이 아니다 ⑨ 용량·구독은 **계속 보류** — 가입자 0명이라 방어할 원가가 없다 ⑩ 작성 경로 두 벌은 **유저 테스트 후 결정**(§7⑧). → §7 의 열린 질문 ①⑦ 이 닫혔고, ② 는 "결정이 아니라 할 일"로 성격이 분명해졌다 |
| 2026-09-06 | 기획 담당자 | **웹 폐기 확정, 신뢰 설계 철회, 락 실제 제거.** ① **웹을 보조로도 남기지 않는다** — "카카오톡이 웹 서비스를 하나? 아니지"(사용자). 앱 하나로 간다(§6⑮) ② **§2의 "부모가 믿을 수 있게 보일 것"을 철회했다.** 부모에게 앱이 닿는 길이 둘 다 자녀를 거치므로(초대 / 자녀가 쓰는 걸 보고 먼저 하자고 함) **신뢰는 자녀가 담보한다.** 스팸·사기 걱정을 설계 과제로 두지 않는다 — 이 판단은 내가 과했던 것을 사용자가 바로잡은 것이다 ③ 락 폐기를 **결정에서 완료로** 바꿨다(§4). 원래 계획한 순서(앱 푸시·위젯 먼저)를 건너뛰었으므로 **리텐션 공백을 명시** ④ §7⑥ 아이폰 — 웹까지 없앴으므로 **아예 못 쓴다**로 강해졌다 |
| 2026-09-06 | 기획 담당자 | **앱 전환 · 손글씨 · 락 제거 · 세 기획서를 번호로 통일.** 피그마 보드 PRD 섹션과 노션 '오늘도사랑해'를 이 문서와 대조한 결과를 반영했다. ① **§1 신설** — "왜 만드는가"가 통째로 없었다. 스프린트가 정한 **북극성("숙제가 아닌 즐거운 일과")·핵심 축 3개·장기목표**를 원문으로 가져왔다(HMW 35개와 Can We? 17개는 피그마에 두고 옮기지 않았다 — 사본을 늘리면 또 갈라진다) ② **§2 전제 교체** — "설치 장벽을 부모에게 지우지 않는다"를 버리고 **"장벽은 설치가 아니라 신뢰"**로. 사용자 판단. 이에 따라 **로고가 전환율 문제**가 됐다 ③ **§6⑧ 뒤집힘** — 웹 우선 → **앱 우선, 웹은 무신사식 보조**(§6⑮ 신설). iOS 제외는 기술이 아니라 **테스트 비용** 판단 ④ **§6⑯ 신설** — 손글씨 도입, 음성 필수 해제 ⑤ **§4 MISSION-01 개정** — 열람 락 폐기 결정과 **"앱 푸시·위젯 확인 후에 뺀다"는 순서** ⑥ **§4 HEART-01 신설** — 구현돼 있는데 문서에만 없던 것. **일방 소통의 답**이 여기 있다 ⑦ **§3에 노션 IA 번호 열 신설**, §4·§5 제목에도 번호를 붙여 **세 문서를 번호로 연결** ⑧ §3에 누락 3건(HEART-01·ACCOUNT-01·BLOCK-01)과 신규 2건(WRITE-01·APP-01) ⑨ §6⑥ 소셜 로그인 보류 → **예정**(휴대폰·카카오·구글) ⑩ §7에 ⑥아이폰 ⑦질문 주기 ⑧작성 경로 두 벌 추가 |
| 2026-09-06 | 기획 담당자 | **몽실이·남보라 폐기, 브랜드를 로고부터 재정의.** 팀 회의 결정. ① §3 COMPOSE-01 🗑폐기, DESIGN-01 ❗재작업, DESIGN-03 ❗대기 ② §6② 를 캐릭터·작성 흐름 동시 폐기로 다시 씀 ③ §6⑩ 의 원칙 문장에서 "성격은 몽실이가 낸다"를 걷어내고 남보라 `#5B4BE0` 폐기 — 이 색을 고른 두 근거(기존 로고와 잇기 / 몽실이 분홍과 안 부딪히기)가 모두 사라져서다 ④ §6⑭ 신설 — 로고→색→토큰→아이콘 작업 순서 ⑤ §0 강조색 "미정", **문서와 코드가 일부러 어긋난 상태**임을 명시(코드는 새 색이 정해질 때까지 남보라 그대로) ⑥ §7③ 시안 3안 무효 처리 ⑦ §8 의 몽실이·남보라 커밋 줄에 🗑 표시(줄은 지우지 않음). **코드를 걷어낸 기록은 실제 커밋 때 §8 에 적는다** |
| 2026-08-25 | 기획 담당자 | **문서 전면 정리.** 제목 구조 복원, 뭉개진 표 복구, 비어 있던 §3 MVP 기능 목록을 코드와 대조해 채움, 모든 명세에 구현 상태 표시(`✅⏸❗🗑`), §6 최근 방향 조정과 §7 열린 질문 신설, 강의 실습 잔해를 `_workspace/16_prd_qa_legacy.md` 로 분리 |
