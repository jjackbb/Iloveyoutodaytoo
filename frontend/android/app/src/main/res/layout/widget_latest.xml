<?xml version="1.0" encoding="utf-8"?>
<!--
  홈 화면 위젯 4×2. RemoteViews 가 다룰 수 있는 뷰(LinearLayout·TextView·ImageView)만 쓴다.
  ConstraintLayout 등은 위젯에서 못 쓰니 추가하지 말 것.

  본문(widget_root) 전체가 앱 열기 버튼이고, 톡톡(widget_knock)만 따로 자기 클릭을 갖는다.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_surface_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="@dimen/widget_padding">

    <ImageView
        android:id="@+id/widget_photo"
        android:layout_width="@dimen/widget_photo_size"
        android:layout_height="@dimen/widget_photo_size"
        android:background="@drawable/widget_photo_bg"
        android:contentDescription="@string/widget_label"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/widget_gap"
        android:layout_marginEnd="@dimen/widget_gap"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/widget_text_primary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/widget_text_secondary"
            android:textSize="13sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_knock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/widget_button_bg"
        android:minWidth="64dp"
        android:paddingStart="14dp"
        android:paddingTop="9dp"
        android:paddingEnd="14dp"
        android:paddingBottom="9dp"
        android:text="@string/widget_knock"
        android:textAlignment="center"
        android:textColor="@color/widget_button_text"
        android:textSize="14sp" />

</LinearLayout>
