# home 검증 결과 — 기획 준수

> 검증자: prd-guard · 2026-08-09
> 대상: `src/app/page.tsx`, `src/components/room/RoomCard.tsx` (+ 홈이 쓰는 `MemberStack`, `StreakBadge`, `Button`, `BottomNav`)
> 기준: `PRD/05_REDESIGN_PLAN.md` §1·§2·§5 → `PRD/04_PROJECT_SPEC.md` → `PRD/README.md` → `PRD/01_PRD.md` · `PRD/03_PHASES.md`
> 이식가의 보고(`01_home_port.md`)를 근거로 삼지 않고 코드·git diff·PRD 원문을 직접 대조했다.

## 판정: 조건부 통과

**들어오면 안 되는 것은 하나도 안 들어왔다.** 이 화면의 가장 큰 위험(프로토타입에 딸려 오는
즐겨찾기 하트·알림함·정렬 로직)은 전부 막혔다. 다만 **시니어 기준 글자 크기 위반 3곳**과
**PRD 확정 결정 1건 미구현**이 남아 있어 조건부로 둔다.

- 들어오면 안 되는 것: **0건**
- 지켜야 할 것 위반: **2건** (글자 크기 3곳 / 용어 1곳)
- 빠진 것: **1건** (근거 없는 누락). 근거 있는 누락 3건은 평가 결과 전부 타당.

---

## 들어오면 안 되는 것이 들어왔다

**없다.** 항목별 확인 결과는 아래와 같다.

| 금지 항목 | 결과 | 확인한 자리 |
|---|---|---|
| 좋아요·댓글·이모지 반응·공지 | 없음 | `page.tsx` 전체, `RoomCard.tsx` 전체 |
| 즐겨찾기 하트(`.fav`/`toggleFav`/`liked` 정렬) | 없음 — 그 자리를 연속일수 배지가 쓴다 | `RoomCard.tsx:111`. 정렬은 `joined_at` 오름차순 하나뿐(`page.tsx:37`) |
| 친구 목록 | 없음 — 관계방 카드 목록이 그 역할 | `page.tsx:17` 주석이 `01_PRD.md:121`을 정확히 인용 |
| 저장용량 그래프 | 없음 | — |
| 시니어 폰트 토글 | 없음 | — |
| 로그인 화면 | 없음 — `requireUser()`로 넘길 뿐 UI 없음 | `page.tsx:26` |
| 사진·영상 업로드(4단계) | 없음 — 홈은 방 커버를 **읽기만** 한다 | `page.tsx:89-104` (서명 URL 생성) |
| 답장 강제·열람 잠금 | 없음 | — |
| 랜덤 매칭 | 없음 | — |

프로토타입의 상태 기계(`state.rooms`, `innerHTML`, `bindHomeLongPress`, `.center` 토글)도
한 줄도 안 들어왔다. `'use client'`는 `BottomNav` 하나뿐이다.

### 참고 — 판정 없음

- **방 커버 사진**(`rooms.cover_path` + `covers` 버킷). `05_REDESIGN_PLAN.md` §2 판정표에
  방 커버에 대한 판정이 없다. 4단계로 분리된 것은 `album`·`upload`·`galleryView`·`editPost`
  즉 **게시물** 사진/영상이므로 방 커버와는 별개다. 커밋 `c79f27b`에 이미 있던 기존 기능이고
  홈은 읽기만 한다. **임의로 통과시키지도 차단하지도 않고 판정 없음으로 보고한다.**

---

## 지켜야 할 것을 안 지켰다

### 1. 글자 크기 17px 기준 위반 — 3곳

- 파일: `src/components/room/RoomCard.tsx:99`, `:111`, `:135`
- 기준: **본문 기본 17px** (`05_REDESIGN_PLAN.md` §5 "손대지 않는 것", `04_PROJECT_SPEC.md`
  "절대 하지 마" — *"시니어 사용자가 쓸 화면에 작은 폰트나 저대비 색상을 쓰지 마"*)
- 현재: `text-sm` = **14.9px** (`globals.css:81`의 자체 주석이 이 환산값을 명시하고 있다)

| 줄 | 무엇 | 화면에 보이는 글 |
|---|---|---|
| 99 | '초대' 칩 | `초대` |
| 111 | 연속일수 배지 | `연속 3일` / `오늘부터 시작해요` |
| 135 | 카드 밑 정보 줄 | `가족 · 멤버 3명` |

이 프로젝트는 같은 규칙을 코드 안에 **세 번** 못 박아 놨는데 홈 카드만 예외다:

- `Button.tsx:22` — *"어떤 경우에도 이보다 작게 만들지 마라"*
- `BottomNav.tsx:11` — *"탭 글자라고 작게 줄이지 않는다"*
- `StreakBadge.tsx:21` — *"글자 크기는 두 크기 모두 17px(text-base)로 같다. 목록 안에서
  자리를 덜 차지해야 할 때는 **글자가 아니라 안쪽 여백만** 줄인다"*

**특히 111번 줄이 문제다.** 배지를 `StreakBadge` 컴포넌트로 그리지 않고 `streakLabel()`
문자열만 꺼내 `<span>`에 직접 넣는다. 그 결과 **`StreakBadge`가 지키려고 만들어진 17px 규칙을
그대로 우회한다.** 부품은 있는데 그 부품이 담고 있는 규칙만 빠져나갔다.

> **유래**: 이 `text-sm` 3곳은 이번 이식이 만든 게 아니라 커밋 `c79f27b`에 이미 있었다
> (`git diff HEAD` 확인). 다만 이번 작업이 99·111번 줄을 직접 고치면서
> (`shadow-pill`→`shadow-chip`, `min-h-[44px]` 추가) **글자 크기만 그대로 두고 지나갔다.**
> 이식 기록의 "명세와 대조해 어긋난 4곳을 맞췄다"에도 이 항목은 없다.
> 프로토타입 원본이 11.5~12.5px이라 상대적으로는 이미 키운 값이지만,
> 우리 기준선은 프로토타입이 아니라 17px이다.

### 2. 화면 용어가 앱 전체와 어긋난다 — "멤버"

- 파일: `src/components/room/RoomCard.tsx:138`
- 기준: 앱 전체 용어는 **"구성원"** (`legal/terms.md`, 방 설정, 초대 화면, 탈퇴 화면 모두)
- 현재: `멤버 {n}명`. **`grep` 결과 `src/` 전체에서 "멤버"는 이 한 줄뿐이다.**

같은 카드 안에서 말이 갈린다 — 바로 옆 아바타 더미의 낭독기 라벨은
`구성원 3명`인데(`MemberStack.tsx:20`) 눈에 보이는 글자는 `멤버 3명`이다.
**낭독기 사용자와 눈으로 보는 사용자가 서로 다른 단어를 듣고 본다.**

이식가는 "앨범방→관계방"을 *"같은 걸 가리키는 이름이 두 개가 되면 안 된다"*는 이유로
바꿨다. 그 판단은 옳다. 그런데 **같은 논리를 바로 아래 줄의 "멤버"에는 적용하지 않았다.**

---

## 빠졌다

### 1. 카드에 생성일이 없다 — **근거 없는 누락 (실질 지적)**

- 파일: `src/app/page.tsx:33`, `src/components/room/RoomCard.tsx:135-139`
- PRD 근거: **확정된 결정이다.**
  - `PRD/README.md:34` — *"관계방 이름 중복: **허용**, 카드에 생성일 함께 표시해 구분"*
  - `PRD/01_PRD.md:208` — 가정 원장 1번, *"관계방 이름은 중복 허용하고, **카드에 생성일을
    함께 표시해 구분한다**"* / 상태 **확인됨**
  - `PRD/01_PRD.md:135` — 확인 완료 항목에 *"관계방 이름 중복(허용+생성일 표시)"*
- 현재: `page.tsx:33`이 `rooms(… created_at …)`를 **가져오기는 하는데 아무도 안 쓴다.**
  `RoomCard`에는 생성일 prop 자체가 없고, 화면에 나가는 정보 줄은 `관계유형 · 멤버 N명`뿐이다.
  `formatKstFullDate()`는 *"(방 시작일 등)"*이라는 주석을 달고 만들어져 있지만
  (`lib/format.ts:116`) 실제로는 가입일·차단일·초대만료일에만 쓰이고 방에는 안 쓰인다.

**"뺀 것" 기록에 이 항목이 없다.** `01_home_port.md` §2는 프로토타입의 `게시물 N개`와
`r.updated`를 뺀 이유는 적었지만, **PRD가 요구하는 생성일을 안 넣은 것은 언급조차 없다.**
정보 줄을 손대면서 프로토타입 필드 2개를 빼고 관계유형을 새로 넣는 동안, 정작 PRD가
그 자리에 넣으라고 한 값만 지나쳤다.

이름 중복을 허용하기로 한 결정과 짝이 되는 장치다. `엄마` 방이 두 개면 지금 화면에서는
`가족 · 멤버 3명`과 `가족 · 멤버 3명`으로 **구분할 방법이 전혀 없다.**

> 데이터는 이미 손에 있다(`created_at`을 select에 넣어 뒀다). `RoomCard`에 prop 하나와
> 정보 줄에 `· 2026년 8월 7일부터` 한 조각을 더하면 끝난다.

### 2. 알림 종 (`.iconbtn` + `#i-bell`) — **근거 있음, 평가 결과 타당**

- 명세 위치: `01_home_spec.md` §1 마크업, appbar 오른쪽 `openNotifications()`
- 기록된 근거: `01_home_port.md` §2 — "알림은 PRD에서 P2다"
- **독립 확인 결과 근거가 맞다:**
  - `03_PHASES.md:14-33` **Phase 1 기능 목록 전체에 알림 항목이 하나도 없다.**
  - `03_PHASES.md:83` — 알림/리마인더 커스터마이징이 **Phase 2** 목록에 있다.
  - `01_PRD.md:68` — 알림 커스터마이징 **P2**.
- 껍데기 버튼을 두지 않은 판단도 지지한다. `04_PROJECT_SPEC.md` "항상 해"의
  *"시니어 사용자를 고려해 네비게이션을 단순하게 유지"*와 맞고, 눌러도 아무 일이 없는
  버튼은 시니어에게 "내가 잘못 눌렀나"를 만든다. **타당.**

### 3. `게시물 N개` · `r.updated` — **근거 있음, 평가 결과 타당**

- 명세 위치: `01_home_spec.md` §2 `renderHome`의 `.album-meta p`
- 기록된 근거: "지금 조회하지 않는 값이라 안 넣었다 (없는 데이터를 0으로 채워 그리지 않는다)"
- **평가: 타당.** `04_PROJECT_SPEC.md` "절대 하지 마" — *"목업/하드코딩 데이터로 완성이라고
  하지 마"*와 정확히 같은 이야기다. 또 `05_REDESIGN_PLAN.md` §1은 구조 이식이 원칙이면서도
  *"단, **기능 범위(뭘 넣고 뺄지)는 PRD가 최종 승인**"*이라고 못 박았고, 게시물 수·최근 활동은
  PRD 어디에도 요구가 없다. **다만 이 줄이 곧 위 1번(생성일)이 들어갔어야 할 자리다.**

### 4. 인사말·섹션 제목 / `aria-label` 처리 — **판단 타당**

- 이식가 주장: "안녕하세요 ○○님"과 보이는 "나의 관계방"은 프로토타입 `#homeBody`에 없다.
- **명세를 직접 확인한 결과 맞다** — `01_home_spec.md`의 `renderHome`에는 둘 다 없다.
  즉 이건 "이식 누락"이 아니라 **원래 없던 것을 안 만든 것**이다. 지적 대상이 아니다.
- 보이는 제목 대신 `<ul aria-label="나의 관계방">`을 붙인 처리(`page.tsx:161`)도 타당하다.
  `<ul>`은 list role이라 이름을 받을 수 있고, 화면에 없는 글자를 만들지 않으면서
  낭독기에는 목록 이름이 전해진다. `sr-only` 제목을 만드는 것보다 낫다.

### 5. 즐겨찾기 하트 → 연속일수 배지 — **확정 결정대로**

`05_REDESIGN_PLAN.md` §1의 "댓글바·이모지 피커 넣지 않는다" 및 `01_PRD.md:120`
"좋아요/댓글/공지 제외"와 일치. `RoomCard.tsx:14-17` 주석이 *"하트 모양은 PRD §6에서 뺀
'좋아요'와 헷갈린다"*고 적은 것은 옳은 읽기다. **통과.**

---

## 화면 문구 검토

화면에 실제로 나가는 한국어 문장만 골라 시니어 독자 기준으로 읽었다.

| 문구 | 문제 | 제안 |
|---|---|---|
| `아직 연결된 소중한 공간이 없어요` (`page.tsx:242`) | **"소중한 공간"이 관계방을 가리키는 세 번째 이름이다.** 이식가는 "앨범방→관계방"을 *이름이 두 개가 되면 안 된다*는 이유로 바꿔놓고 바로 위 제목에는 그 원칙을 적용하지 않았다. 또 `연결된`은 번역투이고 기계적이다 — 시니어에게 "연결"은 통신 용어로 읽힌다 | `아직 만든 관계방이 없어요` |
| `멤버 3명` (`RoomCard.tsx:138`) | 앱 전체가 쓰는 `구성원`과 어긋나고, `src/` 통틀어 여기 한 곳만 "멤버"다. 같은 카드의 낭독기 라벨은 `구성원 3명`이라 **보는 말과 듣는 말이 다르다.** 외래어라 시니어에게도 불리하다 | `구성원 3명` |
| `방 목록을 불러오지 못했어요. 잠시 후 다시 열어봐 주세요.` (`page.tsx:151`) | 톤은 좋다(원인을 사용자 탓으로 돌리지 않는다). 다만 여기서만 `방 목록`이라 부른다 | `관계방을 불러오지 못했어요. 잠시 후 다시 열어봐 주세요.` |
| `우리만의 첫 번째 관계방을 만들고 소중한 사람들을 초대해 보세요.` (`page.tsx:246`) | 문제 없음. 따뜻하고 다음 할 일이 분명하다 | 그대로 |
| `새로운 관계방 만들기` (`page.tsx:202`) | 문제 없음. "앨범방→관계방" 교체는 `04_PROJECT_SPEC.md:90`·`README.md:34`의 PRD 용어와 일치한다 | 그대로 |
| `연속 3일` / `오늘 다시 시작해요` / `오늘부터 시작해요` (`StreakBadge.tsx:32-36`) | 문제 없음. **0일일 때 죄책감이 전혀 없고** "끊겼어요/실패/놓쳤어요"를 피했다. `04_PROJECT_SPEC.md:84`와 `README.md:35`를 정확히 지켰다 | 그대로 |
| `초대` (`RoomCard.tsx:102`) | 글자 자체는 문제 없다. 다만 방이 여러 개면 낭독기에서 `초대` 링크가 같은 이름으로 반복돼 어느 방인지 알 수 없다 (WCAG 2.4.4). 크지 않은 사안이라 다음 화면들과 함께 정리해도 된다 | `aria-label={`${name} 방에 초대하기`}` |

---

## 그 밖에 확인한 것 (전부 통과)

| 기준 | 결과 |
|---|---|
| 로고 원본색(`#fb2e39`/`#ed1074`/`#9a11c5`)을 버튼 배경에 안 썼는가 | **통과.** `.btn-primary-surface`는 `--color-primary`(#d50e68)→`--color-primary-light`(#df1670)만 쓴다(`globals.css:114-130`). 로고 3색은 `.brand-gradient`에 격리돼 있고 홈에서 부르지 않는다 |
| 색 토큰만 썼는가 | **통과.** 홈이 쓰는 색은 전부 토큰이다. 프로토타입 생색(`#FBEAEF`, `#FFE1E9→#FFC9D8`)을 `border-hairline`·`bg-primary-soft`로 바꾼 판단은 옳다 |
| 터치 목표 44px | **통과.** '초대' 칩 `min-h-[44px]`, 탭 `min-h-[68px]`, 주 버튼 `min-h-[52px]`, 전역 `button{min-height:44px}` |
| 탭 3개 유지 | **통과.** 홈·사서함·마이 (`BottomNav.tsx:38-70`) |
| `text-xs` 남용 | **통과. `src/` 전체에 `text-xs`가 한 번도 없다.** `text-sm`도 9곳뿐이고 그중 홈이 5곳이다(위 지적 3 + 아바타 이니셜 2) |
| 저대비 회색 | **통과.** `muted`(#6a6a6a)는 흰 카드 위 5.3:1, 빈 화면 분홍 그라데이션 위 약 4.8:1로 AA를 넘는다 |
| 포커스 링 제거 | **통과.** 홈 경로에 `outline-none`이 없다. 전역 `:focus-visible{outline:3px solid var(--color-primary)}`가 살아 있다 |
| 아이콘 전용 요소의 낭독기 처리 | **통과.** `BrandHeart`·`PhotoIcon`·`PeopleIcon`·`Dot` 모두 `aria-hidden`, `MemberStack`은 `role="img"` + `aria-label` |
| 아바타 이니셜의 `text-sm` | **지적 안 함.** `aria-hidden` 장식이고 32px 원 안의 한 글자다. 읽어야 할 문장이 아니다 |

---

## 고칠 것 (우선순위 순)

1. **`RoomCard`에 생성일 표시** — PRD 확정 결정 미구현. 데이터(`created_at`)는 이미 select에 있다.
2. **`RoomCard`의 `text-sm` 3곳을 `text-base`로** (`:99`, `:111`, `:135`).
   특히 `:111`은 `StreakBadge size="sm"`을 그대로 쓰는 쪽이 낫다 — 규칙이 부품 안에 이미 있다.
3. **`멤버` → `구성원`** (`RoomCard.tsx:138`).
4. **`아직 연결된 소중한 공간이 없어요` → `아직 만든 관계방이 없어요`** (`page.tsx:242`).
5. (선택) `초대` 링크에 방 이름을 담은 `aria-label`.

---

## 검증 범위 밖이지만 넘길 것

- **`.screenshots/home-ported.png`(17:52)에서 빈 화면 hero가 세로 가운데가 아니라 위에 붙어 있다.**
  이식 기록(18:14)의 *"hero가 body 세로 가운데에 놓이고"*와 어긋난다. 스크린샷이 수정 전
  시점일 수도 있다. **렌더링 문제라 residue-qa 몫이지만 증거가 서로 안 맞으니 확인이 필요하다.**
- 이식 기록 §4에 적힌 **확인용 방 3개가 DB에 남아 있다.** `05_REDESIGN_PLAN.md` §4는
  *"테스트 계정을 만들었으면 그 세션 안에서 지운다"*고 정했다. 다음 화면 검증에 필요하다는
  판단은 이해하지만, 규칙과는 어긋나므로 정리 시점을 정해 둘 것.
