@AGENTS.md

## UX 기준선 (2026-08-20 개정)

**"시니어라서"를 근거로 판단하지 마라.** 주 사용자는 2030이고, 동반 사용자인 부모(5060)는
카카오톡을 무리 없이 쓴다. 조심할 대상이던 어르신(70+)은 주 대상이 아니다.

새 기준은 **카카오톡**이다 — 카톡이 이미 가르친 동작(롱프레스·스와이프·아이콘 버튼)은
"배워야 할 것"으로 치지 않는다. 규칙 전문은 `_workspace/12_ux_baseline.md`.

⚠️ 코드 주석 곳곳에 아직 "시니어 사용자라서…"라는 옛 근거가 남아 있다(약 55곳).
**그 문장을 새 판단의 근거로 인용하지 마라.** 손대는 김에 보이면 고치되,
멀쩡한 파일을 그 이유만으로 열지는 않는다.

## 하네스: 프로토타입 구조 이식

**목표:** `민규ver.html` 프로토타입의 모양과 구조를 실제 앱으로 옮기되, 그 프로토타입을
폐기하게 만든 **잔여데이터 구조는 한 줄도 가져오지 않는다.**

**트리거:** 화면 이식·부품 교체·디자인 적용 관련 작업 요청 시 `oneuldo-port` 스킬을 사용하라.
파일 위치나 개념을 묻는 단순 질문은 직접 답해도 된다.

**관문:** 화면 하나가 끝날 때마다 `bash .claude/skills/residue-scan/scripts/verify.sh` 를
통과해야 다음 화면으로 넘어간다.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-08 | 초기 구성 — 에이전트 4개, 스킬 4개(오케스트레이터 포함) | 전체 | 이식 작업을 오류·잔여데이터 없이 진행하기 위해 |
