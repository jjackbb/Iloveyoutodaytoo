# 인수인계 — 기능 구현 세션

> 이 파일 하나만 읽고 시작할 수 있게 썼다. 대화 요약이 아니라 **결정된 것**만 담았다.
> 마지막 갱신 2026-09-07. 원본 기획은 `PRD.md` — 충돌하면 PRD가 맞다.

---

## ✅ 2026-09-07 — 아래 여섯 개는 전부 붙였다

**이 문서의 "할 일" 1~6번은 끝났다.** 남은 것은 맨 아래 "지금 남은 것"뿐이다.
결정의 이유를 쉬운 말로 푼 것은 `DECISIONS_2026-09-06.md`, 코드 변경은 `PRD.md` §8,
함정은 `WORKLOG.md` 2026-09-07 항목에 있다.

| # | 무엇 | 어디에 |
| :--- | :--- | :--- |
| 1 | 위젯 + 톡톡 | `android/…/widget/` · `src/app/api/widget/` · `src/components/native/WidgetTokenSync.tsx` |
| 2 | 앱 전환 (Capacitor) | `capacitor.config.ts` · `android/` · `src/lib/native.ts` |
| 3 | 손글씨 | `src/lib/handwriting.ts` · `src/components/handwriting/` |
| 4 | 타임랩스 재생 | `src/components/handwriting/HandwritingPlayer.tsx` |
| 5 | 매일 바뀌는 자리 | `src/lib/daily.ts` · `src/components/home/DailySlot.tsx` |
| 6 | 보호자 인증 행 정리 | `supabase/schema/07_cron.sql` (2026-09-06 완료) |

**바뀐 것 하나** — PRD 의 *"위젯을 꾹 누르면"* 은 **불가능하다**. 안드로이드가 길게 누르기를
시스템(이동·크기조절)에 쓴다. 위젯 안 **[톡톡] 단추**로 바꿨다(사용자 확인).

## ✅ 2026-09-08 — 환경변수 2개 완료·실제 검증까지 끝남

| 변수 | 상태 | 검증 방법 |
| :--- | :--- | :--- |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ Vercel 에 설정·재배포 완료 | 배포된 `/api/widget/latest` 를 시험 토큰으로 호출 → 받은 서명 URL 로 실제 사진(JPEG 333KB) 수신 확인 |
| `ANDROID_CERT_SHA256` | ✅ Vercel 에 설정·재배포 완료 | `assetlinks.json` 이 지문을 정확히 돌려준다. 앱을 지웠다 새로 설치 → `pm get-app-links` 가 `verified` → 패키지 지정 없이 순수 링크(`am start -a VIEW -d ...`)를 던졌더니 **브라우저 대신 앱이 자동으로 열리고** `/invite/[token]` 까지 정확히 이동(스크린샷으로 확인) |

**플레이스토어 등록은 포폴 목적상 하지 않기로 함(사용자 결정, 2026-09-08).**
그래서 아래는 **원천적으로 시연 불가능**하다 — 코드는 그대로 두되(나중에 스토어에
올리면 살아난다), 시연/실기기 검증 대상에서 뺄 것:
- 스토어 링크(`PLAY_STORE_URL`)로 앱 받기
- 설치 뒤 초대 자동 복원(Install Referrer) — Play 스토어가 설치를 기록해야만 동작한다.
  사이드로드 APK는 이 정보 자체가 없다(에뮬레이터에서도 "설치 리퍼러 없음" 코드로 확인됨)

**딥링크의 "이미 설치된 상태에서 링크 클릭 → 앱이 열림"은 스토어와 무관하게 100% 동작한다** —
위 검증이 그것이다. 포폴 시연은 "APK 설치 → 링크 클릭 → 앱이 바로 그 화면으로" 로 하면 된다.

## 지금 남은 것

**사람이 해줘야 진행됨**
- Firebase(`google-services.json`) — 앱 푸시(FCM). 톡톡이 앱 밖 알림으로 가려면. 아직 코드 없음

**다음 세션에서 이어갈 순서 (사용자 결정, 2026-09-08)**
1. **디자인 먼저.** 브랜드 확정(`#BF3F0D`)됐으니 `/welcome`·손글씨 판(`HandwritingPad`)·
   위젯 색(`android/…/res/values/widget.xml`)을 디자인 관문(방향→시안→구현)으로 다시 그린다.
   지금 이 셋은 전부 "겉모습은 임시" 주석이 달린 중성 회색 상태다.
2. **그다음 실기기 검증.** 디자인 적용 전에 하면 임시 화면을 검증하고 나중에 또 해야 한다 —
   순서를 바꾸지 말 것(사용자와 합의된 이유). 검증할 것:
   - 위젯이 홈 화면에 실제로 그려지는 것, 톡톡 왕복
   - 손글씨를 실제로 쓰고 피드·상세에서 재생되는 것
   - (Install Referrer 는 위에서 설명한 대로 스토어 없이는 검증 불가 — 건너뛴다)
3. FCM 코드 작성 + `google-services.json`.

**코드로 남은 것**
- `memories_voice_pair` 제약의 NULL 구멍(손글씨 쪽만 고쳤다 — `supabase/schema/11`).
  `handwriting` 과 같은 방식으로 고치면 된다(`is not null` 추가).
- 사서함이 "치운 id 전부"를 URL 에 싣는 문제 — 데이터가 없어 검증을 못 해 안 고쳤다

---

## 지금 앱이 어떤 상태인지 (2026-09-06 기준)

세 가지를 방금 코드에서 걷어냈다. **고장이 아니라 의도된 상태다.**

| 없앤 것 | 지금 |
| :--- | :--- |
| 답장 미션 **열람 락** | `lib/mission.ts` 삭제, 사서함 `LockedHeart` 삭제. **읽음 표시(`markHeartRead`)와 DB 함수 `locked_senders()` 는 남겼다** |
| 강조색 **남보라** | `globals.css` 토큰 4줄이 **임시 중성 회색**이다. 앱이 무채색으로 보이는 게 정상 |
| 캐릭터 **몽실이** | `Mongsil.tsx` 삭제. **`/start` 화면 가운데가 비어 있다** |

**⚠️ 되돌아오게 하는 장치가 지금 비어 있다.** 락을 먼저 뺐기 때문이다.
그래서 아래 1번(위젯)이 가장 급하다.

**로고·색·`/start` 화면은 다른 세션이 맡는다**(`HANDOFF_LOGO.md`). 건드리지 마라.

---

## 할 일 — 이 순서로

### 1. 홈 화면 위젯 + 톡톡 `WIDGET-01` 🔴 가장 급함

PRD §4 `WIDGET-01` · §6⑰. **락을 없앤 자리를 메우는 기능이고, 서비스 정의의 3분의 1이다.**

| 만들 것 | 무엇 |
| :--- | :--- |
| 보여주기 | 홈 화면 위젯에 **최근 표현**을 띄운다 — "앱 안 켜도 마음이 보임" |
| 톡톡 | 위젯을 **꾹 누르면** 상대 폰이 진동 — "○○님이 톡톡했어요" |

**🚨 위젯은 웹뷰 밖에서 산다.** 네이티브 안드로이드 코드(AppWidgetProvider)이고,
웹뷰의 로그인 세션·데이터를 **자동으로 보지 못한다.** 그래서 둘이 필요하다:

1. **위젯 전용 API** — 최근 표현 한 건을 돌려주는 엔드포인트. 인증 토큰은 네이티브 저장소에
2. **톡톡 → 푸시 경로** — 위젯의 누름이 서버를 거쳐 상대에게 알림으로

**"내 위젯 디자인은 나만 바꾼다"**(상대는 못 바꿈)를 지킨다 —
앨범방 이름·커버를 각자 화면에서만 바꾸는 규칙과 같은 사상이다.

### 2. 앱 전환 `APP-01`

**Capacitor + 원격 URL로 확정**(PRD §6⑮). Capacitor가 만든 안드로이드 프로젝트의
WebView가 배포된 Next.js 서버를 부른다.

- **서버 액션 18개와 미들웨어를 건드리지 마라.** 이 방식을 고른 이유가 그것이다.
  정적 전환(`output: 'export'`)은 그것들과 양립하지 않는다
- **딥링크 필수** — 설치 후 원래 초대를 복원한다. 없으면 부모가 앱을 열고
  "그래서 뭘 하라는 거지"가 된다. Firebase Dynamic Links는 종료됐으니 다른 수단을 고를 것
- 오프라인은 포기했다. 이 앱은 전부 서버 데이터라 실질 손실이 작다
- `manifest.json` 의 `theme_color` 가 아직 남보라(`#5b4be0`)다 — 로고 세션이 색을 정하면 같이 고친다

### 3. 손글씨 `WRITE-01`

PRD §4 `WRITE-01`. **정할 것은 다 정해졌다.**

| | 결정 |
| :--- | :--- |
| **저장** | **획 좌표** (획마다 점 배열 + 시간). ⚠️ 나중에 못 바꾼다 — 4번이 여기 걸려 있다 |
| **화면** | **앨범방 작성(`3.7`)만.** 사서함·댓글에는 넣지 않는다 |
| **배경** | 편지지·포스트잇 고르기는 **1차 제외** |
| **음성 필수** | **해제한다** — 음성 · 손글씨 · 사진/영상 중 **하나 이상** |

> 지금 `compose-form.tsx` 의 `canSubmit` 은 **음성 3초가 유일한 조건**이다. 여기를 고친다.

### 4. 타임랩스 재생 `WRITE-02`

**3번 바로 다음.** 손글씨만 있고 재생이 없으면 그냥 그림이고,
핵심 축 ③현장감은 여전히 빈 채로 남는다. 획 좌표가 쌓여 있으니 비교적 작은 작업이다.
열람할 때 **빠른 타임랩스가 지나간 뒤 결과물**을 보여준다.

### 5. 매일 바뀌는 자리 `DAILY-01`

오늘의 질문은 **주 단위 고정 그대로 둔다**(PROMPT-01). 대신 매일 바뀌는 자리를 하나 만든다 —
`작년 오늘` · `이번 달 12번 남겼어요`.

> **"12번 주고받았어요"가 아니라 "12번 남겼어요"로 쓴다.**
> 앞의 문장은 상대가 없으면 실패로 읽히고, 뒤의 문장은 혼자여도 성립한다.
> 혼자 쓰는 사람을 버리지 않는 것이 이 자리의 숨은 역할이다.

### 6. 보호자 인증 행 정리

가입을 끝내지 않고 버려진 행에 보호자 번호가 남는다. **24시간 후 삭제.**
개인정보 최소보관 원칙이라 **결정이 아니라 할 일**이다. 출시 전 필수.

---

## 🚨 마음대로 바꾸지 마라

`frontend/CLAUDE.md` 에 전문이 있다. 아래는 이번 작업에서 특히 걸릴 것들.

| 절대 임의로 | 왜 |
| :--- | :--- |
| **`users.birth_date` 의 NOT NULL** | 나이 제한용이 아니라 **만 14세 미만 보호자 동의 분기를 태우기 위한 것**이다. 2026-08-24에 카카오 로그인을 붙이다 이걸 풀었다가 **전부 되돌린 일**이 있다 |
| **운영 DB 스키마·제약·트리거·RLS** | 되돌리기 어렵고 모든 사용자에게 즉시 영향을 준다 |
| **PRD에 적힌 기능을 없애거나 뜻을 바꾸는 것** | 기획 결정이지 구현 판단이 아니다 |

**"이걸 안 바꾸면 작업을 못 끝낸다"는 우회할 이유가 아니라 보고할 이유다.**
절반만 끝내고 "여기서 막혔고 뚫으려면 X를 바꿔야 하는데 그 판단은 제 몫이 아니다"라고
말하는 편이, 다 끝내놓고 몰래 바꾼 것보다 낫다.

---

## 손대면 안 되는 것

- **`/start` 화면 · 로고 · 색 토큰** — 로고 세션 담당(`HANDOFF_LOGO.md`)
- **DB 함수 `locked_senders()`** — 안 쓰이지만 남겨뒀다. 정리하려면 따로 결정한다
- **`frontend/_workspace/`** — 몽실이 시안 등. 왜 그만뒀는지 기록으로만 둔다

---

## 이미 정해져서 다시 묻지 않아도 되는 것

| | |
| :--- | :--- |
| 보관 정책 | 지우기 전까지 **영구**. 자동 삭제 없음 |
| 앨범방 개수 | **제한 없음**이 정식 정책 |
| 테마 4종 | 🗑 폐기 (다크모드도 지금은 안 만든다) |
| 저장 용량·구독 | ⏸ 계속 보류 |
| 로그인 최종안 | 휴대폰·카카오·구글 (지금 아이디/비번은 임시) |
| 작성 경로 두 벌 | 유저 테스트 후 결정. **지금 합치지 마라** |
| iOS | 안 만든다 (테스트 비용) |

---

## 먼저 읽을 것

| 파일 | 무엇 |
| :--- | :--- |
| `PRD.md` **§3** | 기능 목록 + 노션 IA 번호 |
| `PRD.md` **§4** | WIDGET-01 · WRITE-01 · WRITE-02 · MISSION-01 · HEART-01 명세 |
| `PRD.md` **§6⑮⑯⑰⑱⑲** | 앱 전환 · 손글씨 · 위젯 · 매일 바뀌는 자리 · 보관 정책 |
| `frontend/CLAUDE.md` | 🚨 규칙 |

---

## 끝나면

`PRD.md` **§8 패치 기록**에 한 줄(커밋 하나에 한 줄, 해시 말고 커밋 제목),
`app_Iloveyoutodaytoo/PRD.md` 사본도 함께 갱신. 그리고 커밋·푸시.
`WORKLOG.md` 에 5~10줄 — 한 것 / 결정과 이유 / 다음 할 일 / 함정(실패한 시도 포함).
