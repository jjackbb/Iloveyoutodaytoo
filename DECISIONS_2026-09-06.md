# 2026-09-06 결정 총정리 — "이래서 이렇게 했습니다"

> 오늘 질문지로 고른 결정들을 **왜 그랬는지부터** 쉽게 풀어 쓴 것.
> 팀원에게 그대로 말할 수 있게 썼다. 기술 용어는 괄호 안에만.
> 원본 기획은 `PRD.md`, 코드 변경 기록은 `PRD.md` §8.

---

## 한 줄 요약

**앱으로 가기로 했으니, 앱에서만 되는 것(위젯·손글씨)을 먼저 붙였고,
그러려고 필요한 밑바탕(DB 백업·토큰·딥링크·웹 정리)을 깔았다.**

---

## Ⅰ. 앱 전환 — 어떻게 감싸나

### 1. 앱은 "웹뷰가 서버를 부르는" 방식으로 만든다
- **상황**: 지금 앱은 화면 26개가 전부 서버에서 그려진다(서버 액션 47개). 앱 안에 화면을 통째로 넣는 방식(정적 전환)은 이걸 다 다시 짜야 한다.
- **고른 것**: **Capacitor + 원격 URL** — 앱 껍데기 안의 웹뷰가 배포된 서버(vercel 주소)를 그대로 부른다.
- **왜**: 서버 코드를 한 줄도 안 건드린다. 실제로 에뮬레이터에서 아이디 중복확인(서버 왕복)이 되는 걸 확인했다.
- **감수한 것**: 오프라인이 안 된다. 어차피 이 앱은 전부 서버 데이터라 손해가 작다.
- **말할 때**: "앱은 껍데기고 알맹이는 서버예요. 그래서 빨리 만들 수 있었고, 위젯 같은 네이티브 기능은 껍데기 쪽에 붙였어요."

### 2. 패키지 이름 `app.oneuldo.android`, 앱 이름 "오늘도 사랑해"
- **왜**: 패키지 이름은 스토어에 올리면 **영영 못 바꾼다**. 코드가 이미 `oneuldo`를 쓰고 있어서 거기 맞췄고, `app.` 접두는 도메인이 없어도 쓸 수 있다.

### 3. 웹은 접는다 — 브라우저로 오면 "앱 받으세요" 화면만
- **상황**: 웹을 같이 끌면 화면을 두 벌 맞춰야 하고, 위젯·손글씨는 웹에서 안 된다.
- **고른 것**: 브라우저는 전부 **소개 + 스토어 버튼** 랜딩으로 보낸다(LINE 홈페이지처럼). 예외는 초대장·약관·기계가 읽는 파일뿐.
- **왜**: "카카오톡이 웹 서비스를 하나? 아니지." 매일 쓰는 메신저는 앱 하나로 간다.
- **지금 상태**: 랜딩 **디자인은 임시**다(로고·색이 미정). 로고가 나오면 디자인 관문을 밟아 다시 그린다.

### 4. 앱이 없는 사람이 초대 링크를 누르면 → 스토어로
- **고른 것**: 초대장 미리보기(누가 불렀는지)는 보여주고, 가입 버튼 대신 **[앱에서 열기] → Play 스토어**.
- **왜**: 누가 불렀는지 보여야 설치를 결심한다. 웹에서 가입시키면 계정만 만들고 다음 화면부터 막힌다.
- **함께 만든 것 — 딥링크**: 설치 뒤 앱을 열면 **원래 초대로 돌아온다**(Play Install Referrer). 이게 없으면 부모가 앱을 열고 "그래서 뭘 하라는 거지"가 된다.
- **⚠️ 아직**: 스토어에 등록 전이라 스토어 링크는 404다. 등록되면 그대로 살아난다.

---

## Ⅱ. 홈 화면 위젯 — 앱 안 켜도 마음이 보임

### 5. 위젯은 자기만의 열쇠(토큰)로 서버에 묻는다
- **상황**: 위젯은 앱 화면 밖(안드로이드 홈)에서 살아서 **로그인 상태를 못 본다**. 뭔가로 자기를 증명해야 한다.
- **고른 것**: 로그인하면 서버가 **위젯 전용 토큰**(긴 난수)을 만들어 폰에 저장. 위젯은 그걸로만 서버에 묻는다.
- **왜**: 이 토큰이 할 수 있는 건 딱 둘 — 최근 표현 1건 읽기, 톡톡 보내기. 새어도 그 둘뿐이고, 행 하나 지우면 폐기된다. 이 프로젝트가 이미 쓰는 방식(권한을 좁게 자른 DB 함수)과 같다.
- **버린 안**: 로그인 쿠키를 그대로 쓰기 → 새면 계정 전체가 노출되고, 앱을 오래 안 열면 위젯이 죽는다.

### 6. 토큰은 로그아웃·탈퇴할 때까지 산다
- **왜**: "앱 안 켜도 마음이 보임"이 전제다. 90일마다 죽으면 오래 안 연 사람의 위젯이 빈 화면이 된다. 토큰이 할 수 있는 게 둘뿐이라 오래 살아도 위험이 작다.

### 7. 30분마다 + 앱 열 때 즉시 갱신
- **왜**: 안드로이드가 허용하는 최소가 30분이다. 앱을 열면 그 자리에서 바로 갱신한다. 나중에 FCM(앱 푸시)이 붙으면 즉시로 바뀐다.

### 8. 위젯에 띄우는 건 "내 방에서 남이 남긴 최신 추억"
- **왜**: 위젯의 뜻은 "상대가 나한테 남긴 게 보인다"다. 사진이 있으면 사진, 없으면 이름과 "목소리 12초".
- **혼자 쓰는 사람**: 남이 남긴 게 없으면 내 최신 것을 보여준다. 북극성 "혼자여도 성립"을 지키기 위해서.

### 9. 톡톡은 "꾹 누르기"가 아니라 [톡톡] 단추
- **상황**: 안드로이드 위젯은 길게 누르면 시스템이 가로챈다(이동·크기조절). 앱이 못 받는다.
- **고른 것**: 위젯 안에 작은 **[톡톡] 단추**. 나머지 영역을 누르면 앱이 그 추억으로 열린다.
- **말할 때**: "원래 꾹 누르기였는데 안드로이드가 그걸 못 주게 돼 있어서 단추로 바꿨어요."

### 10. 톡톡 횟수 제한 없음
- **왜**: 초대한 사람끼리만 앨범방을 쓰고 사서함은 랜덤이라 **스팸이 성립하지 않는다**. 톡톡은 톡톡만의 애정 표현이 된다.
- **한 가지만**: 받는 쪽 알림함이 같은 사람 톡톡으로 도배되지 않게, 안 읽은 톡톡이 있으면 새 줄 대신 시각만 당긴다. 보내는 쪽은 자유다.

### 11. 위젯 사진을 위해 "관리자 키"를 딱 한 곳에서만 쓴다
- **상황**: 이 프로젝트는 원래 관리자 키(service_role)를 안 쓰는 게 규칙이다. 그런데 위젯은 로그인이 없어서 비공개 사진의 주소(서명 URL)를 만들 수 없다.
- **고른 것**: 위젯 API **한 곳**에서만, 그것도 DB가 "이 토큰은 이 사진을 봐도 된다"고 답한 사진 하나에만 서명 도장을 찍는 데 쓴다. 검사는 여전히 DB가 한다.
- **⚠️ 해야 할 일**: Vercel 환경변수에 `SUPABASE_SERVICE_ROLE_KEY`를 넣어야 위젯에 사진이 뜬다(없으면 글만 뜬다).
- **말할 때**: "규칙의 유일한 예외예요. 어디서 왜 썼는지 코드에 적어뒀고, 다른 데서 쓰면 검사기가 막아요."

---

## Ⅲ. 손글씨 — 목소리가 쑥스러운 사람의 길

### 12. 획 좌표를 파일로 저장한다 (그림이 아니라)
- **상황**: 그림(PNG)으로 저장하면 나중에 "쓰는 과정"을 못 돌린다. **한 번 정하면 못 바꾸는** 결정이었다.
- **고른 것**: 획마다 점의 위치와 **시각**을 JSON 파일로. 파일은 목소리와 같은 방식(비공개 버킷 + 경로만 DB)으로 둔다.
- **왜**: 타임랩스(쓰는 과정 재생)가 여기 걸려 있다. 목소리와 같은 구조라 권한·주소·삭제 흐름을 새로 안 만든다.

### 13. 검정 펜 한 색, 굵기 고정
- **왜**: 고를 게 없으면 바로 쓰기 시작한다("부담 없이 첫 마음"). 색은 새 팔레트가 정해지면 바꿀 수 있게 해뒀다. 배경(편지지) 고르기는 1차 제외.

### 14. 캔버스는 가로로 긴 4:3
- **왜**: 편지지 한 장 느낌. 피드에서 사진과 같은 틀에 들어가 레이아웃을 새로 안 짠다.

### 15. 타임랩스는 최대 4초로 압축
- **왜**: 1분 넘게 쓴 것도 4초 안에 다 지나가야 피드에서 스크롤하다 멈춰 보기 좋다. 짧게 쓴 건 실제 속도 그대로, 생각하던 시간(획 사이 멈춤)은 줄여서 재생.

### 16. 음성 필수 해제 — 목소리·손글씨·사진 중 하나면 된다
- **왜**: 문제 정의가 "쑥스러워서 표현을 못 하는 사람"인데, 정작 가장 쑥스러운 일(자기 목소리 녹음)을 가장 먼저 시키고 있었다.

---

## Ⅳ. 매일 열 이유

### 17. 홈 맨 위에 "작년 오늘" 또는 "이번 달 N번 남겼어요"
- **왜**: 오늘의 질문은 주 단위라 매일 열 이유가 못 된다. 매일 바뀌는 자리를 하나 둔다. 앱을 열면 가장 먼저 보이는 자리.
- **"주고받았어요"가 아니라 "남겼어요"**: 앞의 말은 상대가 없으면 실패로 읽힌다. 혼자여도 성립하게.

### 18. N은 추억 + 마음만 센다 (댓글은 안 센다)
- **왜**: 이 숫자의 뜻은 "내가 먼저 건넨 것"이다. 댓글은 응답이라 섞이면 숫자는 빨리 오르지만 뜻이 흐려진다. 한 줄 바꾸면 되는 자리라 테스트 뒤 뒤집기 쉽다.

---

## Ⅴ. 밑바탕 — 오늘 아침에 한 것

### 19. Supabase가 멈춰 있었다 → 되살렸다
- **상황**: 무료 플랜 미사용으로 프로젝트가 잠들어 있었고, 배포된 앱의 로그인·가입이 전부 죽어 있었다.
- **교훈**: 7일 안 쓰면 잠든다. 유저 테스트 전에 한 번 열어보거나 유료로 올릴 것.

### 20. DB 설계도를 저장소에 처음으로 내렸다
- **상황**: 테이블·권한 규칙(RLS)·함수가 **클라우드 한 곳에만** 있었고, 저장소에는 SQL이 한 줄도 없었다. 그날 프로젝트가 잠들었을 때 같이 잃었으면 되살릴 방법이 없었다.
- **고른 것**: 라이브 DB에서 전부 뽑아 `frontend/supabase/schema/` 에 파일로. **앞으로 DB 변경은 파일에 먼저 쓰고 적용한다.**

### 21. 보호자 인증 행 24시간 자동 삭제 (pg_cron)
- **왜**: 가입을 끝내지 않고 버려진 행에도 보호자 전화번호가 남는다. 개인정보 최소보관 원칙 — 결정이 아니라 할 일이었다. 매일 새벽 3시(KST)에 돈다.

### 22. 서버 버그 2건 고침
- 탈퇴가 실패해도 프로필 사진·목소리 파일이 **이미 지워져 있던** 순서 문제(2026-08-10에 실제로 났던 사고).
- ♡·즐겨찾기·알림 삭제·사서함 치우기가 **실패해도 성공처럼** 끝나던 것.

### 23. 죽은 파일 정리 (1.7GB → 0.6GB)
- 몽실이 시안·옛 하네스·참조 0건 문서·빌드 캐시. 큰 변경 전에 바닥을 비웠다.

---

## 사람이 해줘야 진행되는 것 (지금 막고 있는 것)

| 무엇 | 왜 필요 |
| :--- | :--- |
| Vercel 환경변수 `SUPABASE_SERVICE_ROLE_KEY` | 위젯에 사진이 뜨려면 (없으면 글만) |
| Vercel 환경변수 `ANDROID_CERT_SHA256` = `71:28:58:AD:A8:CF:61:CC:8A:D2:28:EA:D6:64:79:CB:43:AC:31:F6:C4:6B:E1:DC:16:C5:D7:50:18:91:23:45` | 초대 링크가 앱으로 열리려면(App Links). 지금은 디버그 서명 지문 — 릴리스 키를 만들면 그 지문을 **더한다** |
| Firebase 프로젝트 (google-services.json) | 앱 푸시(FCM). 톡톡이 앱 밖 알림으로 가려면 |
| 플레이스토어 등록 | 스토어 링크·Install Referrer가 살아나려면 |
| GA4 측정 ID | 나중에 하기로(사용자 결정) |

## 확인 못 한 것 (실기기·배포 뒤)

- 위젯이 홈 화면에 실제로 그려지는 것, 톡톡 왕복
- 설치 뒤 초대 복원(Install Referrer) — 에뮬레이터는 미지원
- 손글씨를 실제로 써서 올리고 피드·상세에서 재생되는 것
- 딥링크로 열었을 때 웹이 그 화면으로 이동하는 것 — 이 코드가 배포된 뒤
