# 오늘도 사랑해 — 디자인 기준 (design.md)

> 2026-09-06 신설. **UI 작업 전에 이 파일을 먼저 읽는다.** 여기 있는 값만 쓴다 —
> 즉흥으로 색·크기를 정하지 않는다. 실제 토큰은 `frontend/src/app/globals.css` 가 원본이고,
> 이 문서는 그 값들의 **근거와 쓰는 법**이다. 둘이 어긋나면 globals.css 가 맞고 이 문서를 고친다.

## 1. 브랜드

| | 값 | 근거 |
| :--- | :--- | :--- |
| 로고 | `frontend/public/brand/logo.svg` (마크만: `mark.svg`) | 봉투에서 나오는 하트. **봉투 플랩의 V 꼭짓점 = 하트의 아래 꼭짓점**, 하트의 45° 아래 변이 그대로 플랩 선이 된다 |
| 뜻 | 편지(마음을 남김) + 하트(사랑) + 봉투(보관·사서함) | 한 형태에 셋. 캐릭터 없음(몽실이 폐기, PRD §6⑭) |
| 브랜드색 | `--color-brand` **#BF3F0D** 선명한 번트 | 로고에서 뽑았다. 번트 오렌지에서 채도 +15 |
| 봉투 앞면 | `--color-brand-front` #B83D0D | 밝기 −1.5. **자세히 봐야 보이는** 차이 — 더 벌리지 않는다(사용자 결정) |
| 하트·플랩 면 | `--color-brand-cream` #F8F2EC | 뚫린 컷아웃이 아니라 **면**. 어두운 배경에서도 하트가 하트로 남아야 한다 |
| 앱 아이콘 | `frontend/android/.../mipmap-*` · Play 512: `public/brand/playstore-512.png` | 적응형 3레이어(배경색 / 전경 PNG / 모노크롬 벡터) |
| 앱 안 마크 | `src/components/brand/BrandMark.tsx` | 인라인 SVG, `fill-primary`. 앱바 22 · 홈 24 · 시작 44 |

**로고는 하트를 다른 용도로 안 쓴다.** 좋아요·즐겨찾기의 하트는 기능 아이콘이라 별개.

## 2. 색 — 면이 아니라 점으로

| 토큰 | 값 | 흰 글자 대비 | 쓰는 곳 |
| :--- | :--- | :--- | :--- |
| `--color-primary` | #BF3F0D | 5.34:1 AA | 주 버튼, 링크, 강조 글자, 선택 상태 |
| `--color-primary-active` | #99320A | 7.46:1 | 눌림 |
| `--color-primary-light` | #D2450E | 4.57:1 — **AA 상한** | 버튼 그라데이션의 밝은 끝. 더 밝히면 미달 |
| `--color-primary-soft` | #FBF3F0 | (위에 primary 글자 4.88:1) | 연한 강조 면, 선택된 칩 배경 |
| `--color-canvas` | #F7F8FA | | 페이지 바탕 — **중립. 색 안 칠한다** |
| `--color-card` | #FFFFFF | | 바탕 위에 뜨는 흰 면 |
| `--color-surface-soft` | #F2F4F6 | | 카드 안 한 번 더 구분 |
| `--color-ink` / `--color-muted` | #222222 / #6A6A6A | 14.97 / 5.09 | 글자 |
| `--color-hairline` / `-strong` | #E5E8EB / #DCE0E5 | | 선 |

원칙 셋(PRD §6⑩에서 살아남은 교훈):
1. **면이 아니라 점으로.** 바탕·카드·그림자는 중립. 색은 버튼·링크·선택 상태에만.
2. **바탕은 중립.** 분홍 시절에 바탕까지 칠해서 강조색이 묻혔다.
3. **흰 글자 4.5:1 이상.** 새 색을 넣을 땐 계산부터. `primary-light` 가 상한선이다.

색을 바꿀 때 같이 봐야 하는 곳: `--shadow-button`(rgb로 박혀 있음), `layout.tsx` themeColor,
`manifest.json` theme_color, 안드로이드 `values/ic_launcher_background.xml`.

## 3. 글자

- **Pretendard**, 기본 16px (`html`). 큰 글자 모드 20px.
- 제목 **700**. 900은 "공지·커머스 목소리"라 쓰지 않는다(§6⑪). 버튼만 600.
- 다루는 감정이 쑥스러움·미안함·후회다 — **소리 지르지 않는다.** 크기·굵기로 밀어붙이지 않는다.

## 4. 모서리·여백·그림자 (프로토타입 실측값, globals.css)

| | 값 |
| :--- | :--- |
| 카드 / 버튼 / 안쪽 / 더 안쪽 / 칩 | 22 / 17 / 14 / 11 / 100px |
| 화면 좌우 / 아래 여백 | 20 / 22px |
| 카드 사이 | 14px |
| 그림자 | card·pill·chip은 중립 잉크, button만 강조색 번짐 |

`rounded-[14px]` 처럼 하드코딩하지 않고 토큰(`rounded-inner`)을 쓴다 — 하드코딩 6종이 남아 있어서
DESIGN-03(아이콘 한 벌·규격 통일)에서 정리한다.

## 5. 아이콘

DESIGN-03에서 한 벌로 묶는다(손으로 그린 SVG 63개, 획 굵기 9종 → 하나로). 그 전까지 새 아이콘을 추가할 땐
**획 2px · 24px 격자 · 둥근 끝**을 기본으로 하고, 나중에 교체될 것을 전제로 `ui/` 안에 둔다.

## 6. 절차

화면·컴포넌트 작업은 `frontend/CLAUDE.md` 의 디자인 관문을 따른다 — 기준 확인 → 방향 → 시안 비교 →
구현 → 마감 → 스크린샷 검증. 시안 HTML을 손으로 짜지 않는다.
