# 인수인계 — 로고·디자인 세션

> 이 파일 하나만 읽고 시작할 수 있게 썼다. 대화 요약이 아니라 **결정된 것**만 담았다.
> 마지막 갱신 2026-09-06. 원본 기획은 `PRD.md`.

---

## 이 세션이 할 일

**앱 로고를 새로 뽑고, 거기서 강조색을 뽑아 앱 전체에 넣는다.**

지금 앱은 **무채색**이다. 고장이 아니라 **색이 비어 있는 상태**다 —
남보라 `#5b4be0` 을 걷어내고 임시 중성 회색을 넣어뒀다(2026-09-06).

완료 기준 넷:

1. 로고 확정 (벡터)
2. 강조색 확정 — `globals.css` 토큰 **네 줄**을 바꾸면 219곳이 따라온다
3. `/start` 화면 다시 짜기 — **지금 가운데가 비어 있다**(캐릭터를 걷어낸 자리)
4. `design.md` 를 만든다 — 이 프로젝트엔 아직 없고 토큰만 `globals.css` 에 있다
5. **앱 아이콘 + 홈 화면 위젯 디자인** — 아래 참고

---

## 왜 새로 뽑나

**몽실이(구름 캐릭터)와 남보라를 함께 폐기했다.** 셋이 서로를 근거로 물려 있었다 —
색은 캐릭터에 맞춰 골랐고, 기존 시안 3안은 캐릭터를 전제로 뽑았고,
"UI는 조용히 있는다"는 원칙은 **캐릭터가 성격을 낸다**는 조건 위에 서 있었다.

> **그래서 지금 비어 있는 것은 로고가 아니라 "성격을 내는 주체"다.**

---

## 로고에 담을 재료 (전부 팀 문서 원문)

| 말 | 출처와 뜻 |
| :--- | :--- |
| **동결건조** | 피그마 보드의 임시 명칭. *얼려 보관했다가 나중에 되살린다* |
| **디지털 감정 아카이브** | 장기목표. "사랑과 추억을 가장 오래 보관하는… 대표 '관계' 기록 서비스" |
| **손길** | "사진 앨범이 아니라, **손길이 담긴 표현 앨범**" — 손글씨를 넣기로 했다 |
| **주고받음 · 쌓임** | 차별점을 "표현 수단이 아니라 주고받음·현장감·쌓임의 경험"으로 못 박았다 |
| **목소리 · 파형** | 앱의 핵심 행동. 이미 화면에 있다 |

**두 갈래를 다 시안으로 올려 눈으로 비교할 것.**
① **목소리·파형**(수단) ② **보관과 되살아남**(동결건조·아카이브).
PRD에 "성격은 목소리·파형"이라고 적어뒀지만 **그건 절반만 맞다** —
팀이 반복해서 적은 건 ②쪽이다.

---

## 지켜야 할 것

| | |
| :--- | :--- |
| 서체 | **Pretendard**, 제목 **700**. 900은 "공지·커머스 목소리"라 내렸다. 버튼만 600 |
| 대비 | 흰 글자 위 **4.5:1 이상**(WCAG AA). 지금 임시색은 10.6:1 |
| 색 쓰는 법 | **면이 아니라 점으로.** 바탕·카드·그림자는 중립으로 둔다 |
| 톤 | 다루는 감정이 **쑥스러움·미안함·후회**다. 소리 지르지 않는다 |
| 플랫폼 | **안드로이드 앱**(Capacitor). 웹은 폐기 — 스토어 아이콘까지 나와야 한다 |
| 테마 | **한 벌만.** 테마 4종(다크 포함)은 폐기했다 — 색을 네 벌로 유지할 필요 없다 |

## 위젯도 같이 봐야 한다 (2026-09-06 추가)

**홈 화면 위젯이 1급 기능으로 확정됐다**(PRD §6⑰ · §4 WIDGET-01).
로고·색이 **앱 밖 홈 화면에서도** 읽혀야 한다는 뜻이다.

- 위젯은 **최근 표현**을 띄우고, **꾹 누르면 상대에게 톡톡**이 간다
- 회의록 원안 중 살린 것 — **"내 위젯 디자인은 나만 바꾼다"**(상대는 못 바꿈).
  즉 **위젯에 사용자가 고를 여지**가 있어야 한다. 그 폭을 이번에 정해야 한다
- 위젯은 작다. **로고가 아주 작은 크기에서도 버티는지**가 이번 로고의 실제 시험대다

## 하지 말 것

- **캐릭터 만들지 마라.** 몽실이는 폐기했다. 다시 제안하지 마라
- **남보라·분홍으로 돌아가지 마라.** 둘 다 폐기 이력이 있다
- **"부모가 믿을 수 있게 보이는 로고"** 논리를 쓰지 마라 —
  부모에게 앱이 닿는 길이 둘 다 자녀를 거치므로 **신뢰는 자녀가 담보한다.**
  한 번 제안했다가 철회된 논리다
- **시안 HTML을 손으로 짜지 마라.** 아래 절차 참고

---

## 절차 — 건너뛰지 마라

`frontend/CLAUDE.md` 에 규칙이 못 박혀 있다. 2026-08-26에 이 절차를 건너뛰고
시작 화면 시안을 손으로 짰다가 **세 번 고쳐 세 번 다 반려**됐고, 결국 처음으로
돌아가 루틴을 다시 밟았다 — **건너뛴 것이 시간을 두 배로 썼다.**

1. `frontend-design-direction` — 톤앤매너·구조 방향
2. **`design`** — Claude Design 캔버스에 시안 **2~3개**를 아트보드로 올려 **눈으로 비교하고 고른다**
   (⛔ `superdesign` 은 쓰지 않는다 — 크레딧이 들어 작업 도중 막힌다)
3. `frontend-design` — 고른 시안을 코드로
4. `make-interfaces-feel-better` — 여백·타이포·상태 마감
5. chrome-devtools로 **스크린샷 검증** — "됐다" 선언 대신 스크린샷으로 증명

---

## 먼저 읽을 것

| 파일 | 무엇 |
| :--- | :--- |
| `PRD.md` **§0 §1 §2** | 지금 상태 · 왜 만드나(북극성·장기목표) · 타깃 |
| `PRD.md` **§6⑩ ⑯** | 색 폐기 이력 · 손글씨 도입 |
| `frontend/src/app/globals.css` | 토큰. **강조색 네 줄이 임시**라고 주석에 적혀 있다 |
| `frontend/src/app/start/page.tsx` | 지금 비어 있는 화면 |

**읽지 않아도 되는 것** — `frontend/_workspace/` 의 몽실이 시안들.
왜 그만뒀는지 기록으로만 남겨뒀다.

---

## 바꿀 곳

```
frontend/src/app/globals.css        ← 강조색 토큰 4줄 (이것만 바꾸면 219곳이 따라온다)
  --color-primary          #3a3a3c   (임시)
  --color-primary-active   #2a2a2c   (임시)
  --color-primary-light    #525255   (임시)
  --color-primary-soft     #eeeef0   (임시)

frontend/src/app/start/page.tsx     ← 가운데가 비어 있다
frontend/src/components/brand/BrandMark.tsx  ← 로고 부품
frontend/public/                    ← 앱 아이콘 · manifest
design.md                           ← 아직 없다. 만들 것
```

---

## 끝나면

`PRD.md` **§8 패치 기록**에 한 줄 추가하고(커밋 하나에 한 줄, 해시 말고 커밋 제목),
`app_Iloveyoutodaytoo/PRD.md` 사본도 함께 갱신한다. 그리고 커밋·푸시.

`WORKLOG.md` 에 5~10줄 인수인계도 남긴다 — 한 것 / 결정과 이유 / 다음 할 일 / 함정.
