## 2026-09-08 — 환경변수 검증 + 세션 인수인계 (짧은 세션)

**한 것**
- Vercel 에 `SUPABASE_SERVICE_ROLE_KEY` · `ANDROID_CERT_SHA256` 설정·재배포를 사용자가 완료.
- 둘 다 **실제로 되는지 라이브로 검증**했다(추측 아님) — 위젯 사진 API 가 실제 JPEG 을
  돌려주는 것, 앱을 지웠다 새로 깔아 App Links 도메인 검증이 `verified` 로 뜨고
  패키지 지정 없는 순수 링크 클릭으로 브라우저 대신 앱이 자동으로 열리는 것까지 확인.
- **플레이스토어 등록은 포폴 목적상 하지 않기로 결정.** 그래서 Install Referrer(설치 후
  초대 자동 복원)는 원천적으로 시연 불가 — 코드는 남기되 검증 대상에서 뺀다.
  App Links(설치된 상태에서 링크 클릭 → 앱 열림)는 스토어와 무관하게 이미 동작 확인됨.

**결정과 이유**
- 다음 순서는 **디자인 먼저 → 실기기 검증**(사용자 결정). 순서를 바꾸면 지금 임시 회색인
  `/welcome`·손글씨 판·위젯을 두 번 검증하게 된다.
- 이 대화 세션은 **compact/clear 하지 않고 그대로 둔 채** 새 세션에서 이어가기로 함
  (사용자 요청 — 나중에 이 세션을 다시 탐색할 수 있게). 그래서 새 세션이 이 세션 전체를
  다시 읽지 않아도 되게 `HANDOFF_BUILD.md` 를 최신 상태로 갱신해뒀다.

**다음 세션이 읽을 것 (이 순서로)**
1. `HANDOFF_BUILD.md` 의 "✅ 2026-09-08" 섹션 — 방금 끝난 것과 정확한 다음 순서
2. `DECISIONS_2026-09-06.md` — 어제 결정 23건의 이유
3. `PRD.md` §0 — 지금 상태

**다음 할 일**: `/welcome`·`HandwritingPad`·`android/…/res/values/widget.xml` 을 디자인
관문(방향→시안→구현)으로. 그다음 실기기에서 위젯 표시·톡톡·손글씨 쓰기/재생 검증.
FCM 은 그 뒤 — 아직 코드가 없다.

---

## 2026-09-07 — 위젯·손글씨·앱 전환 (구현 세션 이어서)

**한 것** — HANDOFF_BUILD 의 1~5번을 전부 붙였다(6번 보호자 정리는 어제 끝났다).
- **APP-01**: Capacitor 안드로이드 껍데기. 에뮬레이터에서 서버 액션 왕복까지 확인.
- **WIDGET-01**: 위젯 네이티브(4×2·30분·캐시 폴백·[톡톡] 단추) + 서버 API 둘 + 토큰 발급/회수.
- **딥링크**: App Links + Install Referrer(설치 뒤 초대 복원).
- **웹 폐기 라우팅**: 브라우저는 `/welcome`(임시 랜딩)으로, 초대장은 스토어로.
- **WRITE-01·02**: 손글씨(획 좌표) + 타임랩스 재생 + **음성 필수 해제**.
- **DAILY-01**: 홈 맨 위 `작년 오늘` / `이번 달 N번 남겼어요`.
- 결정 12건을 쉬운 말로 푼 `DECISIONS_2026-09-06.md` 를 남겼다(팀원에게 그대로 말할 수 있게).

**결정과 이유** (전문은 DECISIONS_2026-09-06.md)
- 위젯 인증은 **전용 토큰**. 웹뷰 세션을 못 보기 때문이고, 토큰이 할 수 있는 건 둘뿐이라
  새어도 피해가 작다. 수명은 로그아웃·탈퇴까지 — 그래야 "앱 안 켜도 보임"이 성립한다.
- 손글씨 저장은 **획 좌표**(못 바꾸는 결정). 타임랩스가 여기 걸려 있다.
- 톡톡 횟수 **제한 없음**(사용자 판단 — 초대한 사람끼리라 스팸이 성립하지 않는다).
- `service_role` 은 **위젯 사진 서명 한 곳만** 예외(사용자 승인). residue-scan 면제 주석을 달았다.

**함정**
- **안드로이드 위젯은 길게 누르기를 앱이 못 받는다** — 시스템이 이동·크기조절로 가져간다.
  PRD 의 "꾹 누르면 진동"은 그대로는 불가능해서 위젯 안 [톡톡] 단추로 바꿨다(사용자 확인).
- `BridgeActivity.onResume` 은 **public** — protected 로 덮으면 컴파일이 안 된다.
- Postgres 는 enum 에 값을 더한 **같은 트랜잭션에서 그 값을 못 쓴다**. `knock` 추가를
  별도 마이그레이션(08)으로 먼저 커밋해야 09 의 CHECK 가 통과한다.
- **`NULL >= 0` 은 NULL 이라 CHECK 를 통과한다.** 손글씨 쌍 제약이 경로만 있는 행을
  들여보내고 있었다(schema/11 로 수정). ⚠️ `memories_voice_pair` 도 같은 모양이다 — 미처리.
- `manifest.json`·`sw.js` 가 **원래부터** 로그아웃 상태에서 `/start` 로 튕기고 있었다.
  matcher 가 `.txt`·`.xml` 만 걸러서 `.json`·`.js` 는 로그인 관문을 탔다.
- `types/database.ts` 를 생성기 출력으로 **통째로 갈아끼우면 안 된다** — 손으로 좁힌
  `send_mode` 타입과 주석이 날아간다. 조각만 이식할 것.
- 서브에이전트 둘이 Opus 세션 한도(429)로 중간에 죽었다. 남긴 파일을 이어받아 마무리했다.

**다음**
- **사람이 해줘야 함**: Vercel 환경변수 `SUPABASE_SERVICE_ROLE_KEY`(위젯 사진),
  `ANDROID_CERT_SHA256`(App Links 검증) · Firebase(FCM) · 플레이스토어 등록.
- **실기기 검증**: 위젯 실제 표시·톡톡 왕복 · Install Referrer · 손글씨 쓰고 재생.
- 브랜드가 정해졌으니(#BF3F0D) `/welcome`·손글씨 판·위젯 색을 디자인 관문으로 다시.
- `memories_voice_pair` 의 NULL 구멍.

---

## 2026-09-06 — 인프라·버그·앱 전환 (구현 세션)

**한 것**
- **Supabase 프로젝트가 멈춰 있던 것을 되살렸다.** 무료 플랜 미사용으로 INACTIVE 였고
  도메인이 NXDOMAIN 이라 **배포된 앱의 로그인·가입이 전부 죽어 있었다.** 90초 만에 복구.
- 스키마를 저장소로 내렸다(`frontend/supabase/schema/`, 7개 파일). 개수를 DB 실제와 대조해 일치.
- 보호자 인증 행 24시간 자동 정리(pg_cron). HANDOFF 6번은 **이걸로 끝났다.**
- 서버 버그 2건: 탈퇴 시 파일이 RPC 앞에서 지워지던 것, 조용히 실패하던 액션 4곳.
- 죽은 파일 정리(1.7G → 625M).
- **APP-01: Capacitor 안드로이드 껍데기.** 에뮬레이터에서 서버 액션 왕복까지 확인.

**결정과 이유**
- 패키지명 **`app.oneuldo.android`**, 앱 이름 **"오늘도 사랑해"**(사용자 확정).
  패키지명은 스토어 등록 후 못 바꾼다.
- 마이그레이션 파일이 저장소에 **하나도 없었다** — 스키마 원본이 라이브 DB 한 곳뿐이었다.
  앞으로 DB 변경은 `supabase/schema/` 에 파일로 먼저 남기고 적용한다(`07_cron.sql` 이 첫 사례).
- 사서함의 "치운 id 를 전부 URL 에 싣는" 문제는 **안 고쳤다.** 안티조인으로 바꾸면 되고
  문법도 200 으로 통과하지만 `heart_messages` 가 0행이라 실제로 걸러지는지 검증할 수 없다.
  사서함에 무엇이 보일지 정하는 쿼리라 검증 없이 바꾸지 않았다.

**함정**
- **Android Studio 내장 JDK 25 로는 빌드가 안 된다** — Gradle 8.14.3 이 "Unsupported class
  file major version 69" 로 죽는다. `JAVA_HOME=/opt/homebrew/opt/openjdk@21` 로 빌드할 것.
  `ANDROID_HOME=~/Library/Android/sdk`, `android/local.properties` 에 `sdk.dir` 필요(gitignore 됨).
- `.next` 를 지우면 Next.js 전역 타입(`PageProps`·`LayoutProps`)이 사라져 타입 검사가 18곳에서
  깨진다. `verify.sh` 는 타입 검사를 빌드보다 **먼저** 돌리므로, 캐시를 비운 뒤에는
  `npm run build` 를 한 번 돌리고 verify 할 것.
- 커밋할 때 `git add -A` 를 쓰지 말 것. 사용자가 PRD 를 편집 중이면 딸려 들어간다(실제로 한 번 그랬다).

**다음**
- **WIDGET-01 이 이제 안 막힌다** — 네이티브 프로젝트가 생겼다. 다만 시작 전에
  **위젯이 어떻게 인증하느냐**를 정해야 한다(권한 범위 결정이라 임의로 못 정한다).
- 딥링크 수단 선택(Firebase Dynamic Links 종료됨) → 그다음 FCM.
- WRITE-01 손글씨 → WRITE-02 타임랩스 → DAILY-01.

---

## 2026-09-06 — 로고 재작업 준비 (Recraft로 넘김)

**한 것**
- 기존 로고 진단: 사진 카드가 주인공(제품은 목소리), 인스타 연상, 로고 색을 UI에 못 씀
  (globals.css에 "버튼에 쓰지 마세요 — AA 미달"), PNG라 단색·크기 변형 불가.
- 안드로이드 전환으로 새로 생긴 제약 3개 확정: 적응형 아이콘 **가운데 66dp 안전영역**,
  **모노크롬 레이어(단색 실루엣으로 성립)**, **48dp 가독**. → 요소 1~2개·획 굵게가 통과 기준.
- superdesign으로 3안(A 파형 / B 보관·되살아남 / C 쌓임) 생성 → **3안 전부 반려**.
  A=보이스메모, B=촛불·향로(추모 연상), C=텍스트 정렬 아이콘. 82크레딧 소모.
- Recraft 프롬프트 역설계 → `frontend/_workspace/recraft-prompting.md`

**결정과 이유**
- 조형은 Recraft, 제약검증·SVG 코드화는 이쪽. 1차 실패로 역할 분담이 실증됨.
- **브리프에 명사를 주면 그 명사의 관습적 아이콘이 나온다.** 기하 구조로 지시할 것.
- 부정어("~로 보이면 안 됨")는 역효과 — 그 단어가 형태에 반영된다. 공식 문서도 같은 경고.

**함정**
- Recraft 스타일 검색창에 `logo` → **Geometric Logo** 를 반드시 고를 것. 스타일 미선택 시
  기본값이 나오고 그게 "AI스러움"의 정체다. 프롬프트로는 못 막는다.
- negative prompt 칸은 프롬프트 패널 **설정(톱니)** 안. V4 지원 여부는 미검증.

**다음**
- Recraft에서 4안(A 호 / B 그릇+알약 / C 띠 / D 닫히지 않은 하트) 뽑아오면
  → 66dp·모노크롬·48dp 검증 → BrandMark.tsx 인라인 SVG → 적응형 3레이어 + Play 512
  → globals.css 강조색 4줄 확정(219곳 따라옴) → /start 가운데 채우기 → design.md 작성

## 2026-09-06 (밤) — 로고·강조색 확정, 코드 반영 완료

**한 것**
- Recraft에서 나온 로고(봉투에서 나오는 하트)를 원본 픽셀 기준으로 벡터화 — 일치율 98.6%.
  하트 로브 원 r74.2·d48.4, 아래 변 45°, 플랩 밴드 18px, 꼭짓점 필렛 20, 모서리는 실측 스쿼클 프로파일.
- 강조색 **선명한 번트 #BF3F0D** 확정 → `globals.css` 4줄 + 버튼 그림자 + 브랜드 토큰, themeColor, manifest.
- `BrandMark.tsx` 인라인 SVG. `public/brand/`(logo.svg·mark.svg·playstore-512·logo-512), public 아이콘 3종.
- 안드로이드: mipmap 5밀도 × 3종, `values/ic_launcher_background.xml`, 모노크롬 벡터 드로어블 + adaptive xml.
- 루트 `design.md` 신설. tsc·lint 통과. 실행 중인 dev 서버 `/start` 스크린샷으로 확인.

**결정과 이유**
- 두 톤은 앞면 밝기 −1.5(#B83D0D) — "자세히 봐야 보이는" 정도. 사용자 결정.
- 하트·플랩은 투명 컷아웃이 아니라 **크림 면**(#F8F2EC) — 안드로이드 아이콘은 배경이 뭐가 될지 모른다.
- Pro 요금제: 루틴 3종 미실행 + 실사용 $346/주 → 전환해도 됨(Opus 주간 버킷만 미지수). 사용자 결정 대기.

**함정**
- Recraft 원본은 하트가 뚫린 투명이었고 #C17753 은 AA 미달(3.48:1) — 그대로 못 쓴다.
- 시안 브리프에 명사를 주면 관습 아이콘이 나온다(파형→보이스메모). 메모리 `logo-brief-geometry-not-nouns`.
- superdesign 무료 크레딧 소진(82cr, 전부 반려). 결제하지 않기로.
- chrome-devtools MCP가 "browser already running"으로 막힘 → 헤드리스 크롬 CLI로 스크린샷.

**다음**
- DESIGN-03: 아이콘 한 벌(63개 SVG·획 9종 → 1종), 모서리 하드코딩 6종 정리 — `design.md` §5.
- 안드로이드 실기기/에뮬레이터에서 런처·테마 아이콘 확인(Gradle 빌드는 openjdk@21).
- `/start`는 웹 폐기 랜딩으로 바뀌어 있어 "가운데 빈자리" 숙제는 소멸 — 앱 안 홈(`/`) 상단 마크만 확인하면 됨.

## 2026-09-08 — 부품 캔버스: 앨범방 카드 확정, 색 어두움 질문 계산으로 답함

**한 것**
- `design/canvas/components/component-drafts.html` — 부품 5개(카드·버튼·입력칸·칩·안쪽 면) 각 3안 + 앨범방 카드는 W/F/X 계열로 여러 차례 반복. 최종 URL: https://claude.ai/code/artifact/ae8f84d1-4d66-42cf-a3dc-67746d5b6cdd
- 앨범방 카드 확정: 토스식 플랫·4:3 사진·하트 우상단(즐겨찾기 시 분홍)·이름표 좌하단에 딱 붙임(안쪽 모서리 10px, 좌우 여백 9px 대칭, 광학 보정 1px)·**새 마음은 당근마켓식 숫자 배지**(이름표 우상단 모서리에 흰 테두리로 걸침).
- 강조색(#BF3F0D) "너무 어두운가" 질문에 WCAG 상대휘도 계산으로 답함 — 색상은 당근과 거의 같고(H 17~24°) 명도만 다름(L 40% vs 55~62%). L 48% 이상 올리면 버튼 흰 글자 대비 AA 미달. `--color-primary-light`(#d2450e, 4.57:1)는 이미 통과하지만 배지에 놓아도 체감 차이는 작음 — 실제로 버튼·배지에 렌더링해서 확인.

**결정과 이유**
- 카드 새 마음 표시: 점(dot) → 숫자 배지로 **뒤집음**(2026-09-06엔 "숫자는 숙제가 된다"며 점만 쓰기로 했었음). 사용자가 토스/당근 참고 비교 후 직접 선택. 숫자가 두 자리 넘기면 재검토 여지 있음 — `design/DECISIONS.md`에 명시.
- 배지 전용 색을 더 밝게 쓰는 안은 코드에 반영 안 함 — 브랜드 토큰을 늘리는 결정이라 로고 세션 몫으로 남김.
- superdesign 대신 `design`(Claude Design 캔버스) 스킬로 전환 완료 — 전역/프로젝트 CLAUDE.md, 메모리 모두 갱신.

**함정**
- 캔버스에서 사용자가 직접 값을 조정하면(칸 폭·여백 등) 다음 코드 수정 전에 **반드시 `action:"read"`로 저장본을 다시 읽어야** 한다 — 안 그러면 옛 버전 위에 작업해 충돌한다. `left: -Npx`로 "밀어서" 여백을 줄이면 실제 레이아웃 여백은 그대로라 `...`(ellipsis)이 생긴다 — 시각 이동이 아니라 실제 padding을 고쳐야 한다.
- 카드 관련 아트보드가 10개 이상 쌓여 파일이 커짐(2.5MB) — 다음에 카드 탐색용 아트보드(CardW*, CardT*, CardU* 등)는 정리해도 됨. `Main.dc.html`이 확정본, `design/DECISIONS.md`가 결정 기록.
- 당근마켓 공식 디자인 값은 이 세션에서 웹 검색으로 못 찾음 — 캔버스 안에 "미검증" 경고로 남김.

**다음 (여기서 이어받을 때)**
- 남은 부품 4개(버튼·입력칸·칩·안쪽 면)는 아직 안 골랐다 — 캔버스 뒤 페이지에 각 3안. 토스식(②)으로 통일할지 하나씩 고를지 미정.
- 사용자가 "시니어 UX/UI 145장 PDF"를 갖고 있음 — 파일 경로 받으면 표지·목차 먼저 확인(우리 타깃은 부모 5060, **시니어 아님** — PRD §2, `oneuldo-ux-baseline-kakao` 메모리 참고). 대상이 안 맞으면 통째로 버려도 됨. 필요하면 Gemini(Antigravity)로 요약 위임 — 145장을 직접 읽지 않는다.
- 남은 규격 결정(모서리 8px/11px, 여백 반 칸)은 나머지 부품 고른 뒤 한 번에.
- 로고 세션에 넘길 것: 배지 전용 밝은 색 신설 여부.
